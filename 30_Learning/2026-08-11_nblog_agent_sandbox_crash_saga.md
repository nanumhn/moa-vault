# nblog-saas PC 에이전트 샌드박스 크래시 대장정 (2026-08-11)

## 무슨 일이 있었나

형이 nblog-saas PC 에이전트(네이버 블로그 자동발행용 설치형 앱) 설치파일을 처음 공개배포(옵션A: 자체서버 직접서빙)한 직후, 실기 테스트 중 "창이 떴다가 바로 사라짐" 크래시를 발견. 하루 종일 진단→수정→재검증을 반복하며 v0.1.0 → v0.1.4까지 총 5개 버전을 순차 배포.

## 원인 규명 과정 (오진 포함, 정직하게 기록)

1. **1차 진단(오진)**: Windows Error Reporting 이벤트로그에서 예외코드 `0x80000003`(STATUS_BREAKPOINT) + "GPU process isn't usable. Goodbye." FATAL 로그를 보고 "GPU 드라이버 문제"로 판단 → v0.1.1(`app.disableHardwareAcceleration()` 상시적용)로 수정했으나 재발.
2. **2차 진단(정답)**: agent.log 실측 결과 `gpu=off`인데도 GPU 프로세스뿐 아니라 **렌더러 프로세스까지 동일 exitCode로 크래시** — 즉 GPU 자체가 아니라 **크로미움 샌드박스 초기화가 그 PC에서 실패**하는 게 진짜 원인. `--no-sandbox`로 실행하면 정상 작동 확인.
3. v0.1.2: 시작 60초 내 자식프로세스 2회 크래시 감지 → `--no-sandbox --disable-gpu`로 자동 재시작 + 그 결정을 `compat-mode.json`에 영구 기록(재부팅해도 유지, 사용자는 평생 크래시 한 번만 목격).
4. v0.1.3: 발행용 네이버 크롬(별도 Playwright 프로세스)에도 같은 지뢰가 있었음 발견 — 동일 안전모드 로직 적용.
5. v0.1.4: 발행 버튼이 네이버 에디터의 "도움말" 팝업에 가려 클릭 실패(Playwright `locator.click: Timeout 15000ms exceeded`, "subtree intercepts pointer events"). 특정 팝업 셀렉터를 콕 집지 않고 **"가려졌는가"를 감지해서 범용으로 걷어내는 방어 로직**으로 수정(형이 "팝업이 산발적으로 뜨고 이벤트배너도 뜬다"고 알려준 게 결정적 — 1회성 dismiss로 짰으면 다음 배너에 또 죽었을 것).

## 부가 발견

- **`checkLogin()`의 `page.close()` 무조건호출 버그**: 사용자가 "네이버 로그인 확인" 버튼을 눌렀을 때 로그인 안 된 상태면 로그인폼 탭이 뜨자마자(1~2초) 자동으로 닫혀버림 — 사용자 입장에선 "로그인창이 열렸다 닫힌다"로 보임. 사용자 직접호출과 백그라운드 폴링을 구분해서, 직접호출일 때만 탭을 유지하도록 수정.
- **설치폴더 ACL 이론**: 형의 다른 AI워커("덱스"/Codex)가 "설치폴더에 BUILTIN\Users 등 샌드박스용 실행권한이 빠져서 죽는다"는 이론을 제시하고 자기 PC에 커스텀 로컬그룹(`CodexSandboxUsers`)을 만들어 RX 권한을 부여해 해결했다고 주장. cto-seojin이 자기 개발PC로 반증(그 3개 SID가 원래 없어도 정상 작동) → 근본원인으로 단정은 못 했으나, 원리 자체는 실재하고 비용 0이라 예방차원으로 설치스크립트에 반영(SID 기반, 이름 기반은 한국어 윈도우에서 조용히 실패하는 함정 확인).
- **빌드 신뢰성 함정**: 0.1.4 첫 빌드가 NSIS `Can't open output file`로 조용히 실패해 140KB짜리 깨진 설치파일이 남았는데, 백그라운드 작업 알림은 "완료(exit 0)"로 표시됨. 파일 크기를 직접 확인하지 않았으면 깨진 설치파일을 그대로 배포할 뻔함.

## 교훈

- **증상을 원인으로 단정하지 말 것.** GPU 크래시는 샌드박스 실패의 증상이었지 원인이 아니었다. 재현 가능한 환경(다른 PC, 다른 사용자)에서 반례를 찾는 게 제일 빠른 검증이었다.
- **"성공(exit 0)" 알림을 곧이곧대로 믿지 말고 산출물을 직접 검증할 것** — 파일 크기, 해시, 실제 동작까지.
- **사용자가 준 힌트(형: "팝업이 산발적으로 뜬다")가 좁은 수정과 범용 수정의 갈림길이었다.** 사용자 관찰을 코드보다 먼저 반영해야 할 때가 있다.
- **여러 독립 진단 소스(우리 cto-seojin + 형의 다른 AI워커)가 각자 다른 각도로 같은 문제를 파다 보니 교차검증이 자연스럽게 일어났다** — 한쪽이 놓친 걸 다른 쪽이 잡았다(page.close 버그는 덱스가, GPU 오진 정정은 cto가).

## 결과

v0.1.4가 정식 배포로 승격(`AGENT_RELEASE_*` 갱신+push 완료). 실제 발행은 1차 시도 실패(팝업 방해, 수정 전 버전으로 시도했기 때문) 후 재시도 대기 중(만료 16:35 KST). v0.1.5가 checkLogin 버그 수정+ACL 예방조치+버튼 다크모드 버그 수정을 담아 빌드 중.

## 이어서 (2026-08-11 오후~2026-08-12 새벽) — 실전 발행 성공, 그리고 새 버그 3종

- **15:30 KST 실전 발행 첫 성공.** `https://blog.naver.com/sky0bada/224375215042`. icacls 실측으로 "ACL 부족설"은 형PC도 개발PC와 동일 패턴(누락 SID 있어도 DENY 없음)이라 최종 기각.
- **발행 재시도가 원래 동작 안 하던 구조적 버그(SL5) 발견+수정(`3b744d3`)**: FAILED 슬롯은 slot-planner 후보에 안 들어가서 `policy.retry=true`여도 실제로는 재시도 잡이 절대 안 생기고 있었음. `maxAttempts` 기본3+무한루프방지 4겹으로 수정.
- **글감(ContentItem) 상태가 발행결과를 안 따라가던 버그**: 처음엔 PUBLISHED만 동기화(`6c09b94`)하고 FAILED는 "재시도후보 탈락 방지" 명목으로 일부러 미반영했는데, **형이 "실패는 실패지"라고 직접 지적**해서 방향 전환 — FAILED로 정직하게 반영하되 후보조건에 FAILED도 포함시켜 재시도는 유지(`b235dbe`). 어설픈 절충(예: "재시도대기중" 별도상태)보다 정직한 상태표시를 형이 더 우선시함 — [[../../.claude/projects 아님, memory: feedback_status_visibility_accuracy_first]] 원칙 재확인 사례.
- **22:55 KST 발행 실패 — NAVER_LOGGED_OUT.** 15:30~22:55 사이 개발2 PC 네이버 세션이 풀림. 조사 결과 진짜 원인은 "유휴시간 초과"가 아니라 **로그인시 "로그인 상태 유지" 미체크로 세션쿠키가 임시(non-persistent)로 저장**돼서 크롬/앱 재시작 한 번에 날아간 것으로 추정(쿠키 메타데이터 실측 — 운영 프로필은 30일 영구쿠키, 테스트 프로필은 `is_persistent=0`).
  - 부가로 **halt 후 자동복구 불가 버그**도 발견: 로그인 관련 실패로 에이전트가 "잡수령중단" 되면, 재로그인해도 자동으로 안 풀리고 사람이 앱에서 "다시 시작"을 눌러야만 재개됨.
- **형 결정: "로그인 상태 유지 체크박스는 사용자가 안 할 수도 있어 보장 못 함 — 핑으로 유지하는 방향으로 가자."** → keep-alive 기능 조사+설계+구현+배포(`45d8346`, PC에이전트 0.1.7 테스트버전). 배경탭 `fetch(credentials:'include')`로 10분±3분 지터 핑, 야간완화, 발행중스킵, 발행20분전 정밀확인 1회. 상태변화시 하트비트 즉시전송 + `naverCheckedAt` 컬럼으로 대시보드에 "로그인됨(N분전 확인)" 정직표시. **클로가 지시서에서 임의로 바꾼 것 1건**: "화면잠금시 스킵"을 "스킵 안 함"으로 변경(사람이 밤에 PC 잠그고 자는 그 구간이 정확히 지키려는 대상이라서) — 형 승인 아직 대기.
- **미디어(이미지3+영상+링크+소제목+인용구) 실전검증 시도 중 클로 실수**: 테스트용으로 준 위키미디어 이미지 URL 하나가 핫링크차단(403)이라 네이버가 본문 전체를 거부, 발행 실패. 형이 dummyimage.com으로 직접 교체해서 해결.
- **그 다음 재시도(3회)에서 또 다른 진짜 버그 발견**: 본문 중간 마크다운 링크(`[글자](주소)`) 삽입시 네이버 에디터 `applyLink` 클릭이 사이드바 패널(`se-panel-header`)에 가로채져 10초 타임아웃. 이미지 문제 해결하니 다음 관문(링크)에서 막힌 것 — 파이프라인이 레이어별로 취약점을 하나씩 드러내는 전형적 패턴.
- **부수 사고**: cto 작업 중 개발용 구글 서비스계정 키(`.env.local`)가 실수로 에이전트 트랜스크립트에 통째로 출력됨. 운영키와 분리돼있어 실피해는 없었으나 형이 재발급 결정 → GCP서 새키 발급→적용→검증→구키삭제까지 전 과정 완료.

### 이어진 교훈
- **"실패를 실패로 보여줄 것" — 재시도 편의를 위한 상태 은폐보다 정직한 표시가 우선.** 형이 명시적으로 정정한 케이스. 앞으로 비슷한 "정확성 vs 편의" 갈림길에서 기본값으로 삼을 것.
- **문제는 한 번에 다 안 보인다.** 이미지 핫링크 문제를 해결하니 링크삽입 문제가 나타났다 — 레이어드 실전테스트는 매 회차 새 취약점을 하나씩 드러낸다는 걸 전제하고 재시도 예산을 여유있게 잡을 것.
- **비밀키 노출은 트랜스크립트 경유로도 일어난다** — 파일을 통째로 cat/echo하는 습관이 원인. 앞으로 `.env*` 파일을 다룰 때는 값 대신 존재여부/길이/해시만 출력하는 습관을 표준으로.

## 현재 상태(2026-08-12 04:17 KST 기준)
미디어테스트 발행은 미완(3/3 재시도 소진, 다음 자동시도 8/13 새벽 01:25 KST). 형 결정 대기: 오늘 링크 빼고 우회 vs 근본수정 먼저. 상세는 memory `project_open_threads_2026-08-12_dawn_snapshot`.
