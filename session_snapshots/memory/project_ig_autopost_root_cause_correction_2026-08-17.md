---
name: project_ig_autopost_root_cause_correction_2026-08-17
description: "인스타 자동게시 56일 장애 진짜 원인 확정(8/18 n8n DB 직접조회) — 워크플로 active=false가 원인, facebook호스트설·PNG포맷설 둘 다 오진이었음. 8/18 08:00 정기실행 첫 성공 확인"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5b69d1f6-1f0a-499b-a3c2-cae15069bb8f
  modified: 2026-08-18T00:46:49.090Z
---

2026-08-16 오후에 "인스타 자동게시가 6/22부터 멈춘 원인 = 잘못된 API 서버 주소(`graph.facebook.com`), 정답은 `graph.instagram.com`"이라고 확인·보고돼 그날 업무일지(`70 Record/2026-08-16.md`)에 사실처럼 기록되고 git push까지 됐다.

**2026-08-17 새벽, cto(서진)가 재검증하며 이 진단이 틀렸음을 발견했다.** n8n 워크플로 `tarotDaily00002`의 IG 노드 3개를 직접 열어보니 **URL이 6/22 이후 한 번도 수정 안 된 상태 그대로 이미 `graph.instagram.com`**이었다(`updatedAt`=2026-06-22 확인). 즉 "누가 나중에 고쳤다"가 아니라 **처음부터 맞는 호스트였다.**

**새 가설[미확인]**: Meta 문서("JPEG is the only image format supported")와 실측(우리 카드 렌더는 PNG, `next/og`/Satori가 PNG만 출력)이 일치해서, **PNG 포맷이 진짜 원인일 가능성**이 높다고 보고 있다. 다만 n8n 실행 로그가 자동 정리돼 6/22 실패 4건의 실제 에러 메시지는 확인 불가 — 실제 API 컨테이너 생성 테스트로 검증 진행 중.

**Why:** 잘못된 진단이 이미 업무일지·기억(git 커밋)에 "확인됨"으로 박혀 있다. 다음에 이 주제를 참고할 때 옛 기록(facebook 호스트 오류)을 그대로 믿으면 안 된다.

**How to apply:** 향후 이 건을 언급할 때는 이 정정 메모를 우선 참고할 것. `70 Record/2026-08-16.md`의 해당 서술도 다음 일지 작성 기회에 정정 필요(작성자=archive-head-haru). PNG포맷 가설이 실증되면 이 메모도 업데이트할 것.

**진행 업데이트(같은 날 밤)**: cto가 `/api/og/daily-card-jpeg` 라우트를 신설해 JPEG 변환 수정을 커밋(`7e8c138`)했고, 클로가 배포 승인함. 다만 실제 Meta API 에러로 확인된 건 아니고(n8n 실행기록 소실+자격증명 복호화 시스템 차단으로 직접검증 불가) 여전히 문서근거+정황일치 수준 — **진짜 확인은 배포 후 첫 자동게시 실행 때 난다.** 배포 후 n8n 워크플로(`tarotDaily00002`) 이미지URL 교체+실패시 디스코드 경보 추가+활성화까지 이어질 예정.

**★2026-08-17 자동게시 성공 — 실증은 아니고 정황 보강임.** JPEG 라우트 배포 후 첫 정기 자동게시가 실제로 성공했다(archive-head-haru가 8/17 일지 보충기록에서 확인). 다만 이건 "고쳤더니 됐다"이지 "그게 원인이었다"의 증명은 아니다 — PNG가 진짜 원인이었다는 직접 에러 로그는 여전히 없다. [미확인] 라벨 유지. 8/16 일지·자산목록의 오기는 취소선+대조표로 정정 완료(83d5db2).

**★★2026-08-18 — 진짜 원인 확정. PNG 가설도 틀렸다.** cto가 n8n sqlite DB를 직접 조회해 실측: `tarotDaily00002` 워크플로가 **6/22부터 그냥 꺼져 있었다(active=false)**. 8/3~8/16 보존된 실행기록에도 이 워크플로 실행이 0건(같은 시각 다른 워크플로는 정상 실행 중이었음 — n8n 자체는 죽지 않았다). 즉 PNG든 JPEG든 상관없이 **IG API 호출 자체가 애초에 한 번도 안 나갔다.** 8/17 08:09 KST에 (JPEG 라우트 배포 작업 중) 워크플로가 켜졌고, 8/18 08:00 KST 첫 정기실행에서 8개 노드 전부 성공 + IG media_publish 응답(`18403635445087949`) 확인 — 실제 게시 성공의 직접 증거. 실패시 디스코드 경보(`igAlert0001`)도 8/17 08:30 의도적 실패 테스트로 연결·발화 검증됨.

**PNG→JPEG 전환은 무해했지만 원인 규명과 무관하다** — 애초에 실패 실행이 없었으므로 에러 로그 자체가 존재하지 않았다. facebook호스트설(8/16 오진)·PNG포맷설(8/17 새 가설, 이번에 오진으로 판명) 둘 다 틀렸다 — **진짜 원인은 단순히 "누군가 꺼놨다"였다.**

**[확인] 게시물 수 해소**: 형이 로그인 상태로 프로필 직접 캡처해 확인 — 게시물 30/팔로워 1/팔로잉 58, cto 실측치(30)와 일치. 8/16 메모리의 "게시물 7개"가 스테일/오기였던 것으로 정리(원인은 특정 안 함, 재확인 불필요).

**부수 발견**: v3 워크플로(`tarotDaily00001`)도 같은 디스코드 웹훅을 써서 매일 알림이 중복 발화 중(IG 게시는 안 함, 디스코드 전용). 급하지 않음, 추후 정리 대상.

**Why 남길 이유**: 같은 장애를 이틀 연속 두 번 오진했다(호스트→PNG→활성화). 다음에 이 주제를 다시 열 때 "PNG 원인" 서술을 절대 그대로 믿지 말 것 — 이 메모 최신 섹션이 SSOT.

**How to apply**: 8/16·8/17 일지의 "원인=PNG[미확인]" 서술은 다음 일지 작성 기회(archive-head-haru)에 "원인=워크플로 비활성(6/22~8/17)"으로 재정정 필요.
