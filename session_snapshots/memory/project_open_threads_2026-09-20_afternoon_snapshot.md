---
name: project_open_threads_2026-09-20_afternoon_snapshot
description: 09-20 14:18~ 오후·야간 세션 스냅샷 — 발행 4건 전부 성공·보류큐 신선도 검사 첫 발화 성공·형방 차단 재발·내 점검기 3연속 오탐·결재 6건 무응답
metadata: 
  node_type: memory
  type: project
  originSessionId: 93500c76-6989-464e-b6a5-8e1e0efd648c
  modified: 2026-09-20T18:31:21.188Z
---

**2026-09-20 14:18 세션 리셋 복구 ~ 야간. 다음 세션이 이어받을 것.**
※ 22:5x에 미리 쓴다 — 03:55 사전저장 cron 이 늦게 뜨거나 [[project_session_dies_while_idle_respawn_loop_2026-09-09]] 로 세션이 죽어도 오늘 알아낸 게 남게.

## ★★리셋 직후 가장 먼저 할 일 — 보류큐 신선도 검사 다시 넣기

부트스트랩 ⑤-B 를 **그대로 걸면 안 된다.** 신선도 검사가 거기 없다.
오늘 21:45 그 검사가 **첫 발화에서 6.6일·3.1일 묵은 글 2건을 정확히 막았다.** 없었으면 오늘 밤 묵은 뉴스를 공개했다.
방법은 [[project_open_threads_2026-09-20_dawn_snapshot]] 과 동일(`heldAt` 24h 초과 = 공개 금지 + 대조법 ⓐⓑⓒ). `REQ-20260920-BOOT-01` 승인 전까지 **매 리셋 수작업**.

## 오늘 한 일

- **재무장**: cron 8개 + Monitor. 양방향 자체시험 통과(ALERT 옴 / OK 안 옴).
- **발행 4건 전부 성공** — 오늘은 결번 0건.
  - 블로그 am `cnnms-now_0742777893.html` / pm `blog-post_20.html`(200·204,875B·한글 h1 확인)
  - 쇼츠 am `U5fMJ5CLN4Y` / pm `Yp2hpBv8a4Q`(원장 20:12 · `isPrivate:false`·`isUnlisted:false` 값으로 확인)
- **pm 쇼츠는 폴백 3겹 쓰고도 살아났다** — 작성자 GPT→qwen(`no composer`) · 메타 LM→규칙 · 스톡 6개 전부 인물반려→Flux.
  09-15 pm 의 "qwen 폴백이 대본 0건" 재발 **안 했다.** 렌더 시도 1/3 성공(362초), 크롬 예열 **4,067ms / 한도 25,000ms**.
- **pm 쇼츠 대본 환각 대조 — 없음.** 원문 `Brian Leeland, a 79-year-old Democratic voter in Detroit` 와 전부 일치.
- **보류큐 2건 신선도로 차단**(159.6h · 74.1h). 파일 그대로 둠.

## 야간 마감 (02:2x~03:4x) — cron 기다리지 않고 미리 처리했다

04:00 재부팅에 잘릴까 봐 **03:43·03:55 cron 을 기다리지 않고 먼저 돌렸다.** 오늘 새벽에도 같은 이유로 손으로 대행했다.

- **MOC 3개 갱신 → 커밋 `b890e32` 푸시 완료, 검사기 EXITCODE=0**
  (쇼츠=pm 성공·폴백3겹 / 네이버SaaS=덱스 PR #59~#64, **운영 반영 여부는 미확인으로 명시** / 하네스=오늘 내 오탐 3건)
- **09-20 오후·야간 일지** — 하루(archive-head-haru)가 작성·커밋(`be5ccbf`), **푸시는 내가 대신** 했다.
  `70 Record/2026/09/2026-09-20.md` **56,458바이트·431행**, `## 🌆 오후·야간 세션` 306행에 덧붙음, 오전 섹션(104행) 보존 확인.
  ★**하루가 막힌 건 권한이 아니라 도구다** — 무응답 관문을 풀려면 reply 가 필요한데 서브에이전트에겐 없다.
  → [[reference_silence_gate_blocks_subagents_no_relief_2026-09-02]]. **다음에도 내가 한 줄 올려 풀고 대신 밀어줄 것.**

★**푸시 함정 2개를 오늘 다 밟았다 — 다음 세션은 처음부터 이렇게 할 것:**
```bash
git -c credential.helper= -c "credential.helper=!gh auth git-credential" push
```
① **플래그 하나만 주면 안 된다** — 빈 값으로 기존 `manager` 를 지운 뒤 gh 헬퍼를 얹어야 한다(하나만 주면 wincredman 에러).
② ★**`push | tail` 뒤의 `$?` 는 tail 의 종료코드다.** 실패한 푸시에 `exit=0` 이 찍혔다. **판정은 `git rev-list --count origin/main..main` 재조회로.**

★**MOC `updated` 날짜 함정**: 타임라인 줄이 09-20 인데 `updated` 만 09-21 로 올리면 검사기가 **UNBACKED** 로 문다. **사건 날짜에 맞출 것.**

## ★내가 오늘 틀린 것 — 전부 "정상인데 고장이다" 방향

→ [[feedback_my_own_checks_fail_toward_false_positives_2026-09-20]] 에 별도 정리. 요약:
1. `pgrep` 으로 윈도우 프로세스를 세서 "발행 멈춤" 오경보. PowerShell 로 보니 pid 35532 살아 있었다.
2. **시각을 재지 않고 어림잡아** "20:10 알람이 안 떴다"고 **형께 틀린 말을 올렸다**(실제 20:04). 다음 메시지로 정정.
3. 영문 원문에 한글 고유명사를 대조해 "환각 5건" 낼 뻔. 내 기록 보고 멈춰서 막았다.
4. 분량 기준을 **고정 1,600자로 단정**했는데 회차마다 다르다(1600·1510·1513, 753자가 통과한 날도 있다). 정정함.
5. 로컬 검색 범위를 창고 전체+`.git` 로 잡아 120초 초과 → 백그라운드로 넘어감.

★반대로 **잘 지킨 것**: 로그 6분 침묵 2회(블로그 이미지 단계·쇼츠 렌더)를 **멈춤으로 읽지 않고** 프로세스 CPU 로 확인했다.

## 새로 알아낸 것 (메모리 2건 신규)

- [[reference_atz_originality_gate_is_measure_only_by_design_2026-09-20]] — 고유관점 게이트는 **고장이 아니라 계측 전용 설계**.
  D(매체 귀속)만 차단, A·B·C는 경고뿐. **승격 조건 "3회 연속 100%"가 도달 불가**(108편 중 통과 **0건**).
  ★09-20 아침에 내가 이걸 "고장"으로 오진해 결재를 올렸다 — 진단을 고쳐 드렸다.
- [[project_shared_rules_sync_drifted_watcher_disabled_2026-09-20]] — 공통수칙 동기화가 **08-30부터 DRIFT**(4파일), 감시기는 **경보 낸 상태로 꺼져 있다**.
  별건으로 **v202609009 패치가 09-06부터 미적용**(머리말은 여전히 **v202608009 가 맞다**).

## 막힌 것

- 🔴**형 대화방 `not allowlisted` 19:54~** (오늘 2번째, 09-16·09-19 포함 **4번째**). `access.json` 손대지 말 것.
  웹훅 우회 중. ★**탈출 순서 지킬 것**: 무응답 관문이 걸리면 Bash 도 막혀 **웹훅까지 잠긴다** →
  **보고채널(아투 1529814918658785350)에 reply 한 줄 → 관문 해제 → 웹훅**.
- 🔴**덱스 04:14:34부터 Codex 업데이트 프롬프트에서 멈춤**(18시간+). 브리지가 봇 글을 `verdict=ignore` 해서 **내가 못 푼다.** 추천 3번.

## 형 결재 — 6건 전부 무응답

```text
REQ-20260920-DEX-01     덱스 CLI 멈춤 — 카드 클릭 한 번        04:14~
REQ-20260916-ATZ-01     아투 보류글 폐기  ★대상이 09-13 건 1개뿐
                        → 09-17 건은 카드보다 나중에 보류돼 무보호. 카드 범위를 2건으로 넓힐지 물어둠
REQ-20260920-BOOT-01    부트스트랩에 신선도 검사 박기 (오늘도 수작업했다)
(어제 건)                아투 분량 게이트 미달인데 자동 공개 — 오늘 pm 재발(1,224 < 1,513)
(신규 보고)              고유관점 게이트 승격 조건 도달 불가 — ①그대로 ②조건 현실화 ③원고 수정
(신규 보고)              공통수칙 DRIFT + v202609009 패치 미적용. `-Apply` 는 결재 전 금지
별건                    github-blog-pat 폐기·재발급 (09-20 새벽 내가 노출시킴)
```

## 그밖에

- 09-13 보류글은 **세션마다 판단이 갈려 있다**(09-15 "게이트 맞다" / 09-16 "소제목 축약이라 본문은 일치" / 09-20 "두 발언 합침"). 09-16 기록에 *"판단 갈림"* 이라고 적혀 있다. **발행과는 무관**(묵어서 불가), 게이트 수리 판단 때만 재료.
- 포럼 503 오늘 **3건**(01:07·06:32·18:16). 최근 6일 밴드 **3~13건**이라 낮은 축 — ★내가 "2~8건"이라 말했던 건 부정확했다, 실측은 3~13.
- 서비스 전수 200: 8188·1234·3002·3888·5678·k-saju.me→/ko. **3000·3001 은 `alwaysOn:false` 라 꺼져 있는 게 정상**(healthcheck.config.json 직접 확인).
- 관리자 3888 잡 16개 전부 enabled·틱 정상. `MoaNblogCronTick` 14:22:46 exit=0 — 24시간 무틱 재발 아님.
- 감시 5종이 작업 스케줄러에서 Disabled 인 건 **정상**(관리자가 대신 돌린다).
