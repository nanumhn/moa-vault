---
name: project_nblog_bold_color_feature_2026-08-13
description: "nBlog 에이전트 본문 굵게·글자색 구현완료(실측기반), 0.1.13 서버업로드, 포인터승격은 형결재대기+QA검수중"
metadata:
  type: project
  originSessionId: b8dfa7f0-1191-4e2e-b45b-98b7bc1b7173
  modified: 2026-08-13T09:13:37.546Z
---

형 요청(2026-08-13)으로 cto-seojin이 실제 네이버 스마트에디터(aiislife 블로그) 실측 기반으로 구현 완료.

**결과**: 굵게=됨(Ctrl+B/툴바 둘다). 글자색=됨이지만 임의색 불가, **71색 프리셋뿐**이라 사용자가 적은 색은 파서가 가장 가까운 팔레트색으로 스냅(`snapColor()`). **숨은 함정 발견**: 서식이 다음 줄로 새는 버그(굵게/색 건 자리에서 엔터치면 이후 줄 전부 물듦) — 실측 4번째 줄까지 이어짐, `resetInlineFormatAtCaret`로 수정.

**배포상태**: 커밋 b30c4ce+efbdf4c push완료, 설치파일 0.1.13 서버업로드+sha256확인. **공식 포인터는 아직 0.1.12(승격대기, 형결재필요)** — 0.1.12는 `**굵게**`를 별표째 그대로 발행하는 구버전이라 **포인터승격+도움말배포는 반드시 같이 나가야 함**. qa-lead-jian 검수 진행 중(서식누수방지/팔레트스냅정책/승격게이트 자동화여부 검증).

**★2026-08-13 1차반려→2차PASS**: v1은 지안이 FAIL — 서식누수 수리를 검증한 실측이 실제로 그 위험경로("문장끝을 굵게")를 안 밟고 있었음(테스트가 우연히 통과), "포인터+도움말 동시배포"도 순서가 사실 반대(포인터 먼저)여야 안전한데 강제장치도 없었음. cto가 픽스처에 위험패턴 3개 추가→실제 재현→원인은 "쓴뒤치우기"가 아니라 "쓰기전 정규화+쓴뒤 화면기반 확인"으로 구조 자체를 바꿔야 했음, `formatCleanups` 카운터 신설, `deploy/deploy.sh`에 버전게이트 하드코딩(도움말에 굵게문법 있는데 AGENT_RELEASE_VERSION<최소버전이면 배포중단). **0.1.13은 누수 미수정 빌드라 폐기(서버삭제,404확인), 0.1.14로 재빌드**. 지안 재검수 PASS — 순서까지 실측검증(6개 버전으로 직접 BLOCK/PASS 확인). **결론: 포인터 0.1.12→0.1.14 승격해도 됨**, 순서=①포인터승격→pm2 restart --update-env→pm2 save→릴리스API로 0.1.14확인 ②그다음 웹배포. 형 최종승인만 남음.
경미권고 5건(다음 사이클): 배포가드가 .env.production만 읽어 pm2env캐싱 함정 재발 가능(릴리스API도 대조할 것)/가드 트리거가 문자열 하나뿐이라 문구만 바꿔도 무력화 가능(마커기반 권장)/FEATURE_MIN_VER 강제대입이라 env로 못 덮음/렌더결과 JSON이 콘솔로만 나감(파일로 남기면 검수 편함)/운영발행경로엔 formatCleanups 안 실림(실사용자환경 방어작동여부 확인불가).

**★같은 레포 병렬에이전트 git충돌 재발 확인**([[feedback_parallel_agents_same_repo_git_conflict]] 패턴 그대로): D:\Develop\nblog-saas에서 cto의 이 작업과 **동시에 다른(정체불명) 세션이 대시보드 (gated) 리네임+ExtensionRequest 모델 작업 중**(미커밋 상태로 working tree에 섞여있음). cto가 넣은 도움말 문서 2줄 수정분이 그 세션의 미완성 리네임과 얽혀 **커밋 못하고 작업트리에만 존재** — 클로가 백업 떠두고(`scratchpad/help-page-backup-bold-color-2026-08-13.tsx`) 그들만의업무 채널(1534714627383099493)에 조율요청 남김. **다음 세션은 이 파일이 무사히 커밋됐는지 확인 필요, 안 됐으면 백업에서 복구.**

**★★2026-08-13 최종 — 승격+웹배포 완료, 기능 라이브**: 형승인 후 포인터 0.1.12→0.1.14 승격, pm2 restart --update-env, **pm2 save 완료(파일·런타임·dump 3곳 전부 재부팅잠복장애 재발방지 확인)**, 릴리스API 실측(0.1.9/12/13→update, 0.1.14→latest 클라이언트판정까지 확인). 웹배포 무중단전환123초·헬스체크42회실패0. 배포게이트 실제작동 로그 남음("도움말↔에이전트 짝 확인 — 서버공식0.1.14"). 도움말파일은 다른세션 리네임과 충돌 위험 있어 **HEAD기준 깨끗한 worktree에서 원래경로로 커밋(9072666)**, 그 세션 작업트리는 안 건드림. 겸사겸사 agent/package-lock.json 0.1.11멈춤도 수정(50d89be). 테스트발행은 형 블로그에 시험글 올라갈까봐 일부러 안 함(예약슬롯 발행때 실사용).
**잔여 알림사항**: 원격main=50d89be인데 클로 세션 로컬 primary트리는 df82d7c에 멈춤(그 세션 미커밋 리네임때문에 fast-forward 불가, 안전하게 중단됨·손실없음) — 그 세션이 나중에 커밋할 때 pull/rebase 필요.

관련: [[feedback_parallel_agents_same_repo_git_conflict]] [[project_nblog_ai_content_generation_2026-08-13]]
