---
name: reference_hyung_dm_send_blocked_needs_inbound_dm_2026-09-08
description: "형 대화방(DM)으로 회신이 \"not allowlisted\"로 막히는 미제 증상의 원인 — 형이 그 DM방에 한 줄 보내면 즉시 복구된다"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 91c4952c-d451-4df0-b4d0-b59e85343053
  modified: 2026-09-08T05:35:30.469Z
---

형 대화방 `1501858476362829834`은 **길드 채널이 아니라 DM**이다. 그래서 `access.json`의 `groups` 목록에 **원래 없고, 넣을 필요도 없다.** DM은 `allowFrom`(형 user id `348731432086274049`)으로 판정한다.

**막히는 조건** (2026-09-08 14:26 재현, `server.ts` 0.0.4 코드 직접 열람)

```
409~419행  fetchAllowedChannel()
  DM이면 → userId = ch.recipientId ?? dmChannelUsers.get(id)
           그 userId가 allowFrom에 있어야 통과
225행      dmChannelUsers = new Map()      ← 프로세스 메모리. 세션마다 빈 상태로 시작
931행      dmChannelUsers.set(...)          ← 형이 그 DM방에 글을 보낼 때만 채워진다
```

즉 **형이 이번 세션에 DM방으로 말을 건 적이 없으면 ②가 비고, ①(채널 캐시의 recipientId)만으로 버틴다.** ①이 끊기는 순간 **송신·수신(fetch_messages) 둘 다** 막힌다. 둘 다 같은 게이트를 탄다.

**★해제 방법 — 형이 그 DM방에 아무 글이나 한 줄 보내면 즉시 복구된다.** `/discord:access`로 뭘 추가할 필요 없다. `access.json`은 건드리지 않는다(플러그인 규칙상 금지).

**내가 할 수 있는 게 없다.** `fetch_messages`도 같은 게이트라 캐시를 되살리지 못한다. 폴백은 ①로그채널 웹훅(`moa_webhook_send.ps1`, 로그채널 `1517010882570485871`) ②`PushNotification`(형 CLI·휴대폰) 둘뿐이다. **DM엔 웹훅을 만들 수 없다**(디스코드 제약).

**실측 경위 (2026-09-08)** — 14:15·14:19 발송 성공 → 14:26부터 4회 연속 거부. 그날 형 메시지는 전부 005-07 스레드(`1546361310671929374`)에서 왔고 **DM방엔 한 줄도 없었다.** `access.json`은 08-31 16:52 이후 안 바뀜(내가 안 건드림).

[[project_open_threads_2026-09-08_afternoon_snapshot]]에 "형대화방 송신차단·원인미확인"으로 남아 있던 미제가 이것이다. [[reference_discord_mid_session_deallowlist_2026-08-01]]의 "세션 도중 de-allowlist"도 같은 자리로 보인다(추측 — 그때 로그를 재확인한 것은 아니다).

**★2026-09-16 추가 실측 — 차단은 세션 경계에서 저절로 풀린다.**
09-16 07:31(마지막 성공 `1549548316210102334`) 이후 전 세션 내내 막혀 있었는데, 14:00 리셋으로 뜬 새 세션에서는 **처음부터 정상**이었다: 14:2x `fetch_messages` 15건 성공, 21:5x `reply` 성공(`1549763436563406951`). 그 사이 **형이 DM방에 보낸 글은 0건**(형 방 최근 15건 조회, 전부 내 글) — 즉 위에 적힌 "형이 한 줄 보내야 복구"만이 유일한 해제 경로는 아니다.

위 코드 분석과 모순되지 않는다. 막히는 건 ②`dmChannelUsers`(프로세스 메모리)가 빈 상태에서 ①채널 캐시의 `recipientId`가 끊길 때인데, **세션이 새로 뜨면 플러그인 서버도 새로 떠서 ①이 되살아난다.** 그래서 한 세션 안에서는 형이 말을 걸어야 풀리지만, 리셋(하루 2번)을 넘기면 자연 해소된다.

**How to apply:** 차단이 걸린 세션에서는 여전히 내가 할 수 있는 게 없다(로그채널 웹훅·PushNotification 폴백 그대로). 다만 **다음 세션 스냅샷에 "차단 계속"이라고 미리 적지 말 것** — 새 세션에서는 대개 풀려 있다. 새 세션은 차단 여부를 **가정하지 말고 실제 발송으로 확인**하고, 풀렸으면 전 세션에 웹훅으로만 나간 보고 중 형이 못 본 게 있는지 챙긴다. [[project_open_threads_2026-09-16_afternoon_snapshot]]에 "🔴 차단"으로 남아 있던 것이 이 건이다.

**★2026-09-19 재발 — 09-16 관찰이 그대로 재현됐다(3번째 표본).**
차단 구간 **09-19 06:22:45 ~ 14:23:32 KST = 8시간 1분**(앞 세션 기록 `4e4c77b8-6069-4696-bd47-cef3b5eb7c10.jsonl` 직접 조회 — `not allowlisted` tool_result **28건**, 첫/마지막 시각이 그것). 그 사이 앞 세션은 `moa_webhook_send.ps1`로 우회해 **48 chunk**를 내보냈다. 14:00 리셋으로 뜬 새 세션에서는 **형이 DM방에 한 줄도 안 보내신 상태로 첫 reply가 바로 성공**(`1550741531458015256`). 즉 "세션 경계에서 저절로 풀린다"가 3번째로 확인됐다.

★**이날 내가 한 실수**: 이 파일에 위 09-16 항목이 **이미 적혀 있는데도** 형께 *"제 메모리가 틀렸어요 — 형이 한 줄 보내야 푼다고 돼 있었는데 재시작으로 풀렸어요"* 라고 보고했다. 파일을 **안 열어보고** description 한 줄만 보고 단정한 것이다. [[feedback_read_own_records_before_reporting_as_new_2026-09-17]] 이 정확히 이 실수인데 이틀 만에 같은 자리에서 또 밟았다. **description은 색인이지 내용이 아니다 — 메모리가 틀렸다고 말하기 전에 본문을 연다.**
