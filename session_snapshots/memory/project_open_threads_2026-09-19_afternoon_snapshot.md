---
name: project_open_threads_2026-09-19_afternoon_snapshot
description: "09-19 14:2x~09-20 04:2x 세션 스냅샷 — 형 DM 하루 두 번 차단(8h01m+8h 진행중)·아투 4슬롯 전부 발행·블로그 2편 길이기준 미달 자동공개·클로 오보 3건"
metadata:
  type: project
---

**세션 구간**: 2026-09-19 14:2x ~ 2026-09-20 04:2x KST (14:00 정기 리셋으로 기동, `session_boot.flag` 없음 = 재부팅 아님).

## 기동 시 한 일
- `CronList` 빈 목록 → **cron 8개 재등록**: 사전저장 `55 3`·`55 13`, 세션마감 일지 `43 3`·`47 13`, 주간전략 `30 7 * * 1`, 주간수익리뷰 `0 10 * * 1`, 아투 보류큐 `15 7,21`, 인스타 확인 `20 8`.
- `Monitor` 배경 감시 재무장 + **양방향 자체시험 통과**(ALERT 줄만 전달, `OK` 줄 미전달 부정대조까지 확인).
- 저녁 결과용 **일회성 cron 2개** 추가로 걸었다(19:42 블로그 판정 · 20:17 쇼츠 판정). 30분 감시 주기에만 기대지 않으려는 것. 둘 다 제때 떴고 판정에 성공했다.

## ★형 DM방 송신차단 — 하루에 두 번
- **1차 06:22:45~14:23:32 = 8시간 1분**(앞 세션 기록 직접 조회, `not allowlisted` 28건 / 웹훅 48 chunk 우회). **14:00 세션 재시작만으로 해소** — 형 inbound 0건. [[reference_hyung_dm_send_blocked_needs_inbound_dm_2026-09-08]] 에 3번째 표본으로 기록.
- **2차 20:01~세션 끝까지 계속 차단.** 30분마다 reply 재시도했으나 전부 반려. 보고는 `moa_webhook_send.ps1` 웹훅으로 우회. 다음 세션은 **차단 여부를 가정하지 말고 실제 발송으로 확인**할 것 — 04:00 재부팅으로 풀려 있을 가능성이 높다.
- ★**결재 대기 신규**: "차단을 감지하면 세션을 스스로 재시작하는 장치"를 올렸다. 09-13·09-16·09-17·09-18·09-19(2회) 반복이고 재시작이 해소책인 것이 확인됐으므로 자동화 값어치가 있다. 형 답 없음.

## 아투 — 4슬롯 전부 발행(09-17·09-18 오전 결번 뒤 첫 풀슬롯)
| 슬롯 | 시각 | 판정 근거 |
|---|---|---|
| am 블로그 | 06:09 | 공개 URL 200 (앞 세션 확인) |
| am 쇼츠 | 06:44 | 원장, videoId `q3tHXTUinwg` |
| pm 블로그 | 19:41:52 | 공개 URL 직접 재조회 **200**, 204,697바이트 · https://www.american-todayz.com/2026/09/cnn.html |
| pm 쇼츠 | 20:15:49 | 원장 + 유튜브 재조회 **200**, videoId `vkJ5m4bGDc4` |

🟠 **길이 기준이 두 편 다 깨졌는데 자동 공개됐다.** am 본문 **1,526자** · pm **1,187자** (기준 1,600자), 둘 다 `gate.ok=false` + `originality.pass=false` 인데 `autoPublished=true`·`verify=LIVE`. **저녁이 아침보다 더 짧아졌다.** QA 12항목 중 3개 `skipped`, 인용 대조는 "검수자 확인 필요" 단서. 작성자 둘 다 gpt. **본문은 내가 안 읽었다**(200·제목까지만). 기준을 계속 넘길지 형 결재 대기.

★**원장에 blog 항목이 없는 건 누락이 아니다** — `publish_ledger.jsonl` channel 집계가 shorts 138 · atz 7 · **blog 1**. 이 원장은 애초에 블로그를 안 적는다. **블로그 판정은 state.json + 공개 URL로만** 하고, 원장은 쇼츠에만 쓴다.

## ★내가 틀린 것 3건
1. **"메모리가 틀렸다"고 형께 오보** — DM 차단이 재시작으로 풀린 걸 보고 그렇게 보고했는데, 메모리 **본문에 09-16 실측으로 이미 적혀 있었다.** 색인 한 줄만 보고 본문을 안 열었다. [[feedback_read_own_records_before_reporting_as_new_2026-09-17]] 를 이틀 만에 같은 방식으로 어겼다.
2. **로그 경보 과다 계수** — 앞 19자 슬라이스 비교가 타임스탬프 없는 옛 줄·BOM 줄을 구간 안으로 끌어들여 4건으로 부풀렸다. Monitor 전달 1건과 대조해서 잡았다. [[feedback_count_window_before_trusting_zero_matches_2026-09-19]] 에 "반대 얼굴"로 추가.
3. **웹훅 발송문 깨뜨림** — Bash 안 백틱이 명령 치환으로 먹혔다. **웹훅은 PowerShell 도구로만 보낼 것.**

또 하나: 워치독 5종의 스케줄러 `Disabled` 표기를 새로 조사했는데 이것도 [[project_unified_console_monitor_migration_2026-08-30]] 에 이미 적혀 있었다. 같은 뿌리(색인이 본문과 달랐다)라서 **MEMORY.md 색인 2줄을 실제 결론으로 고쳤다.**

## 그 외 상태
- 이슈포럼 503: 09-19 하루 **13건**(09-18 10 · 09-15 23 · 09-17 22). 새 급증 아님, 근본원인 미규명 [[reference_issue_forum_http_errors_cluster_near_reboot_2026-09-17]].
- 아투 보류큐 **2건 그대로**(09-13 am FOMC · 09-17 pm EU 관세). 막힌 건 게이트가 아니라 **날짜** — 6일·2일 전 기사라 발행 안 함. 폐기 결재 `REQ-20260916-ATZ-01` 09-16부터 무응답.
- 포트 8188·1234·3888·3002 세션 내내 200. 메모리 여유 8.34GB(새벽 0.41GB 압박과 다름).
- 보고 주기를 **30분당 2통 → 1통**으로 줄였다(재무장 알림만, 이상 있을 때만 별도). 차단 중에는 이상 있을 때만 웹훅.
- **형 무응답 계속** — 09-19 하루 종일 형 메시지 0건.

## 새벽 마감
03:43 일지·03:55 저장 cron이 **04:06까지 안 떠서** 리셋 스크립트(04:00:08 시작, 마감 04:35)가 기다리는 동안 **내가 손으로 대신 돌렸다.** 09-19 새벽과 같은 패턴 [[project_session_cron_did_not_fire_2026-09-19]]. MOC 신선도 검사는 STALE 4건(아메리칸투데이·하네스운영·k-saju·실버유튜브, 전부 09-17 기준)·BEHIND 1건(쇼츠자동화) — **시간 부족으로 갱신 못 했다. 다음 세션이 이어서 할 것.**
