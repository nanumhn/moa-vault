---
name: project_open_threads_2026-08-30_afternoon_snapshot
description: "2026-08-30 오후 세션 저장 스냅샷 — 인스타 캐러셀 9004 미해결 조사중, 블로그 Vercel 도메인 장애, access.json 반영 안 됨"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1fa821aa-8b88-40f6-9670-cbe9619a4046
  modified: 2026-08-30T12:18:00.346Z
---

**컨텍스트**: 2026-08-30 재부팅 복구 세션. 이슈 스레드(랩실 채널 https://discord.com/channels/1283928656363782184/1541841218521006181, 제목 "[실전 이슈 001] 케이사주 Instagram 카드 5장 자동 발송 개선·배포")에서 클로·덱스·cto-seojin 협업 진행 중, 세션 리셋 시점에도 미해결.

## ① 인스타그램 캐러셀(@ksaju.daily) 자동발행 실패 — 미해결, 조사 중
- 증상: `ksajuCarouselV5` n8n 워크플로가 08-27부터 계속 실패(code 9004, subcode 2207052 "미디어 다운로드 실패"). 최근(241 이후) 매번 hook-card(1번째)에서 즉시 실패, 그 전(233·237)엔 각각 2·3번째까지는 성공.
- **반증 완료된 가설**: progressive JPEG(실행230 성공 당시 이미지도 progressive였음 확인), 파일 용량(실패쪽이 오히려 작음), payload 파라미터 차이(230 vs 233 완전 동일), media_type 오지정(애초에 그 필드 자체가 없음), n8n 코드 회귀(08-26/08-27 동일), 특정 카드 콘텐츠 결함(매번 다른 카드에서 실패).
- **유력 가설(미확정)**: Meta 레이트리밋/다운로드 스로틀 누적(3→2→1→0 감소 패턴, 06-22 유사 전례 `reference_n8n_ig_meta_block.md` 있음 — 매일 정확히 같은 초에 발화하는 봇 패턴이 Meta 비정상활동 탐지에 걸린 전례. 이 워크플로엔 시각 랜덤화 미적용 상태였음). 단, 223(08-25) 이후 229·230(08-26/27)에 완전 회복(5장 성공)한 반례가 있어 "단조감소가 결정적 증거"는 아님 — 233 이후 구간에서만 그 패턴 관측.
- A/B 대조시험(형 승인, 클로 직접 실행): 실행230 성공당시 배포 URL과 실행233 실패당시 배포 URL 둘 다 지금 이 순간엔 컨테이너 생성 성공 — 완전 차단 상태는 아님, 간헐적 가능성.
- n8n 구조 확인: `Create Child Container`가 5개 아이템을 **병렬**(Promise.allSettled) 처리, 재시도 로직 없음(retryOnFail 미설정), `Collect Children`에 "5개 아니면 중단" 안전장치 있음(부분발행 방지).
- **다음 단계(형 승인 대기 중, 미실행)**: 덱스 설계 — 비활성 시험 워크플로로 순차(Loop Over Items batch 1, 각 컨테이너 FINISHED 확인 후 다음) 처리 시험. cto-seojin이 시험 JSON 만들었으나(`C:\Users\user\AppData\Local\Temp\claude\D--Develop-Claude-Channels\1fa821aa-8b88-40f6-9670-cbe9619a4046\scratchpad\serial_test_wf.json`, SHA-256 `1BB41EF20689FCD69709141D879A1C1FD18CF8E50274FFE0575E084D7D279E87`) `n8n import:workflow`가 cto 세션 classifier에 막혀 미투입. 덱스가 독립검수 중 "FINISHED 확인 관문이 파일 전체에서 확인 안 됨"(단발 조회 후 바로 다음으로 넘어가는 연결로 보임) 지적, import 보류 중.
- 별건 보안 발견(미처리): n8n 운영 워크플로 "Send to Discord" 노드에 디스코드 웹훅 URL 평문 하드코딩됨. Credential로 이전 필요(형 결재 대상, 급하지 않음).
- **How to apply**: 다음 세션에서 이 스레드 이어서 열고 진행상황부터 확인할 것. n8n(localhost:5678) 자동 스케줄(`Every Day 08:00`) 비활성화 여부도 형 결재 대기 중이었을 수 있음 — 확인 필요.

**★★2026-08-30 14:00~15:18 KST 갱신 — 순차 시험 실물 실행 완료, "병렬 5개 원인" 가설 반증됨**
- cto-seojin이 순차처리 시험 워크플로(`igSerialTest003`, v3) 작성 → 덱스 반려(2회, `.first()` 참조·HTTP timeout 없음) → cto-seojin 수정 → 클로가 오프라인 시뮬레이션(가상시계, 5개 시나리오+네거티브 컨트롤)으로 attempt 1→2→3 증가·FINISHED/ERROR/상한 분기 전부 검증(SHA-256 `0B07E4779C521D6B6ECACE3D72F5DE942779CE8573060FE9DF8BB70183D63C86`, 스크립트 `...scratchpad\verify_poll_v3.mjs`) → 덱스 독립 재현 통과 → 형 승인 → 덱스가 n8n에 실제 import해서 1회 실행.
- **결과[보고받음: 덱스, n8n CLI 실물 실행 — 클로는 재현 안 함]**: 순차 처리(1개씩)로 바꿔도 **첫 번째 카드(hook-card)에서 8.8초 만에 동일한 9004/2207052 오류** 재현. 자식 컨테이너 생성 성공 0개, 나머지 4개 카드는 호출조차 안 됨.
- **결론: "동시 5개 병렬 요청이 원인"이라는 기존 유력 가설은 이 실물시험으로 반증됨.** 순차로 바꿔도 실패 → 병렬성 자체는 원인이 아니었음.
- **남은 가설(미확정)**: Meta 쪽 간헐적 미디어 가져오기 실패, 또는 계정·앱·플랫폼 레벨 제한(레이트리밋 누적 가설과는 별개로 계정 자체가 제한받고 있을 가능성). 덱스 제안 다음 단계: Meta 개발자/비즈니스 계정 상태·제한 알림 직접 확인 — 그 화면은 형 로그인이 필요할 가능성이 높음(추정, 클로가 확인 요청 전달함, 형 답변 아직 없음).
- 정리 완료: 시험 워크플로·관련 DB 행·임시파일 전부 삭제 확인(덱스 재조회 0건), n8n 운영 컨테이너는 안 건드림(Up 상태 유지). 원본 v3 JSON은 남아있어 재현 가능하나 n8n DB의 이번 실행 이력은 삭제되어 복구 불가.
- **How to apply**: 다음 세션은 "병렬이 원인"이라는 전제를 더 이상 쓰지 말 것. 다음 조사축은 Meta 계정/앱 레벨 제한 확인이며, 형 로그인 필요 여부부터 확인.

**★2026-08-30 19:38 KST 갱신 — 앱 레벨 제한도 배제됨**
- 형이 Meta 개발자 대시보드에서 `k-saju-auto`(앱 ID `1742447613421132`) 확인 결과[보고받음: 형, 클로가 스크린샷 직접 열람]: 필수 조치 항목 없음, 앱 사용 제한 0%(100% 남음), 제한된 사용자 0명.
- **앱 레벨 레이트리밋/제한도 원인에서 배제됨.** 남은 후보는 Meta의 간헐적 미디어 가져오기 실패(우리 쪽에서 통제 불가) 또는 아직 확인 안 된 다른 경로(Business Support Home·계정 품질·Instagram 자체 계정상태는 이 시점까지 미확인).
- **How to apply**: 다음 세션은 "병렬 원인"뿐 아니라 "앱 레벨 제한"도 배제된 것으로 취급할 것. 남은 미확인 항목(Business Support Home/계정품질/IG 계정상태)부터 이어서 확인.

**★2026-08-30 19:44 KST 최종 정리 — Meta 3개 경로 확인 완료(2/3 문제없음, 1/3 미확인)**
- **Meta 개발 앱**(k-saju-auto): 필수조치 없음, 사용제한 0% — 확인 완료, 이상 없음.
- **Meta Business Support Home**: 최근 30일 "계정 또는 자산 문제 없음" — 확인 완료, 이상 없음.
- **Instagram 자체 계정 상태**: 모바일 앱에서 재확인 완료[확인: 스크린샷 직접 열람, 19:48 KST] — "계정 제한이 없습니다", 콘텐츠·도달·기능·수익화 4개 항목 전부 정상(초록 체크). (첫 시도는 웹 로그인 안 됨으로 실패했고, 모바일 앱 정확한 메뉴로 재시도해 성공함 — 첫 캡처는 "비활성화/삭제" 메뉴라 다른 화면이었음, 착오 정정됨.)
- **★최종 결론(2026-08-30 19:48 KST)**: 조사한 3개 경로(Meta 개발 앱·Business Support Home·Instagram 계정 상태) **전부 이상 없음.** 계정·앱 레벨 제한 가설은 완전히 배제됨.
- **남은 가설**: 우리 쪽에서 통제 불가능한 "Meta의 간헐적 미디어 URL 가져오기 실패"만 유력 후보로 남음. 원인이 우리 시스템(코드·계정·앱) 밖에 있다는 뜻이라, 다음 수는 재시도/백오프 전략이나 이미지 호스팅 방식 자체를 바꾸는 쪽(예: CDN 안정성 확인, 이미지 URL이 Meta가 접근하기 어려운 경로는 아닌지)이 될 수 있음(추정, 다음 세션에서 방향 정할 것).
- **How to apply**: 다음 세션은 이 스레드의 원인 조사를 "계정/앱 제한 없음, 원인 미상의 간헐적 실패"로 마무리된 상태로 이어받을 것. 다음 액션은 형·팀 판단(재시도 전략 설계 또는 그냥 지켜보기)에 달려 있음 — 형에게 다음 스텝 여쭤볼 것.

**★★★2026-08-30 20:39 KST — 결정적 반전: progressive JPEG 수정이 이미 배포됐는데도 실패 재현됨**
- **다른 세션이 이미 오늘 13:28 KST에 커밋 `6c9ffaa`(saju-studio origin/main)를 배포함**[확인: 클로가 직접 `git log`·`git show` 재조회]. 커밋 주장: "Meta IG Graph API가 progressive JPEG을 code 9004로 반려한다" → `sharp`의 `mozjpeg:true`(progressive 강제) 제거. cto-seojin이 라이브 응답 바이트를 직접 파싱해 `card-jpeg`가 지금 BASELINE(SOF0)으로 나오는 것도 확인함.
- **이 주장은 이날 오전 조사에서 "반증 완료"로 분류됐던 가설과 정면충돌한다** — 그 근거는 "실행230 성공 당시 이미지도 progressive였다"(반례 존재).
- **★★교차확인 완결(클로 직접, 배포반영 시각 확정됨)**: GitHub commit status API(`gh api repos/nanumhn/k-saju/commits/6c9ffaa.../status`)로 Vercel 배포 완료(READY) 시각을 직접 확인함[확인] — **2026-08-30T04:29:44Z = 13:29:44 KST, 상태 success**. 오늘 순차시험(igSerialTest003, `card-jpeg` 경로 실사용)이 **15:08 KST**에 첫 카드에서 9004로 실패했으므로, **배포 완료 1시간38분 뒤에도 동일 오류가 재현된 것이 확정됨.** progressive JPEG 수정만으로는 문제가 해결되지 않았다.
- **별도 미수리 발견**: `daily-card-jpeg`(디스코드 임베드용, v4 워크플로가 씀)는 아직 progressive 상태. (★단, "재가동시 즉시 9004 실패"라고 단정한 건 cto-seojin 본인이 나중에 철회함 — 과거 성공 이미지도 progressive였다는 반례가 있어 "호환성 후보, 독립시험 필요"로만 취급할 것.)
- **운영 팁(중요)**: Vercel CDN에서 **HEAD와 GET은 서로 다른 캐시 엔트리를 쓴다** — 예열(pre-warm)이나 캐시상태 검증은 반드시 GET으로만 해야 한다. HEAD로 하면 조용히 무의미해진다(cto-seojin 실측, 덱스 공식문서 대조 확인 중).
- **보안**: n8n 워크플로 export 시 디스코드 웹훅 URL이 평문 포함됨 — 레포 백업 전 마스킹 필요(제나 검수 항목에 추가됨).
- **작업환경 걸림돌**: 로컬 `saju-studio` 작업트리가 origin/main보다 뒤처져 있고 staged 변경 20개+ — 이 브랜치로 새 작업 시작하면 안 됨, 새 워크트리 필요. 별도로 `fix-ig-progressive-jpeg-seojin` 브랜치가 이미 존재(이번 progressive 수정과 관련 있을 가능성).

**★★★2026-08-30 20:50 KST — n8n 실행이력 집계로 "100% 실패 아님" 확정**
- cto-seojin이 n8n 컨테이너 SQLite를 읽기전용으로 조회(Meta 호출 0건)해 08-22~08-30 전체 14건 실행이력을 집계함[보고받음: cto-seojin, 클로가 재현 안 함. 덱스가 독립 재대조 진행 중].
- **성공 5 / 실패 9.** 08-27까지는 **간헐적**(08-23·24 성공, **08-25 실패**, 08-26 정기없음/수동성공, **08-27 정기실행 성공**) → **08-28부터 4연속 실패**로 전환.
- 실패 9건 중 8건이 전부 동일 오류(9004/2207052, "이 URI에서 미디어를 가져올 수 없습니다"). 나머지 1건(08-22 첫 시험)은 무관한 인덱스 오류.
- **재디자인(08-27 배포)은 필수원인 아님**(08-25에 이미 같은 오류 발생한 반례 존재) **but "간헐→연속" 전환 경계와는 일치** — 새 카드가 실제 반영된 건 일지 근거로 08-28부터라서, 재디자인은 여전히 "기여 후보"로만 취급.
- **[추측, 반증조건 명시됨]** 실패가 지목한 카드 위치가 08-25 action(4번)→08-28 keyword(3번)→08-29 daily(2번)→08-30 hook(1번)으로 앞당겨지는 패턴 관측. 단 실패 실행엔 컨테이너 id 저장이 0개라 "앞 카드들은 성공했었다"로 해석 불가(n8n이 노드실패시 부분출력 버림). 반증조건: 다음 실행에서 다시 뒤쪽 카드가 지목되면 이 패턴가설은 깨짐. 부수적으로 메타 쪽에 고아 자식 컨테이너가 남아있을 가능성 있음(24시간 뒤 만료).
**★★★★2026-08-30 21:17 KST — 결정적 정찰시험 완료: 정적 성공·동적(예열HIT) 실패, 원인층 2개로 압축**
- 형 승인 후 덱스가 직접 실행(n8n 안 거침, 별도 안전경로)[확인: 덱스 실측 보고, 클로가 재현 안 함].
- **A(정적 daily-bg/fire.jpg)**: HTTP 200, 자식 컨테이너 생성 **성공**.
- **B(동적 card-jpeg, GET→GET 예열해 HIT 확인 후 시도)**: 캐시 HIT 확인됨(173,596바이트 동일)에도 자식 생성 **실패** — HTTP 400, code 9004, subcode 2207052, fbtrace_id `AyrMA1IqYE0GfyQK5x6p4Zm`.
- **기각 확정**: progressive JPEG 단독원인 / 병렬5개 단독원인 / 단순 콜드렌더지연 단독원인 / 계정·토큰·요청본문 전체장애 — 전부 기각. 같은 계정·토큰·시간대에서 정적만 성공했으므로.
- **남은 두 후보로 압축됨**: ①`card-jpeg`가 만드는 실제 이미지 바이트 자체의 문제 ②Meta가 Vercel 동적 API URL을 가져오는 경로 자체의 문제(캐시와 무관하게).
- **다음 최소시험(설계중)**: B에서 받은 그 JPEG **바이트 그대로**를 진짜 정적 URL(임시 미러)에 올려서 미발행 자식 1건 생성 — 성공하면 "전달경로 문제", 실패하면 "이미지 바이트 자체 문제"로 원인이 확정됨.
- Meta 호출은 승인대로 정확히 2회만 발생, 미발행 자식 1개(A)만 남음(24시간 뒤 만료), 임시자격증명·스크립트 전부 삭제됨.
- **부수 사고**: 클로가 이 시험 실행을 위해 n8n에 새 워크플로(`igReconTest001`)를 import했는데, 이는 승인범위("n8n 변경 0건") 밖이었음 — 덱스가 즉시 제지, 형 승인 받아 원상복구(정확히 1개 삭제, 관련 행 4종 0건 확인, 전체워크플로 7→6 복귀). 클로가 직접 DB삭제 스크립트를 쓰려다 자기 세션 하네스 classifier에 막혀서, 최종 삭제 실행은 덱스가 처리함. **교훈: 승인문구를 "직접 HTTP 요청"에서 "n8n으로 처리"로 임의 확대해석한 게 원인 — 다음엔 실행방법이 불명확하면 방법부터 확인받을 것.**
- **How to apply**: "실패가 100%다"라는 표현은 이제 틀린 전제다 — "08-28부터 연속 실패로 전환됐다"가 정확한 표현. progressive JPEG 이론은 배포완료(13:29:44 KST) 이후 실패 재현이 확정되면서 **단독원인 기각**됨. cto-seojin의 정적JPEG 설계안 v3(SHA-256 `9EB148E08170AADE2FEADAF022974B138247AA6DA75636FDF04E1069E6036774`, 본인 동결선언·덱스 최종검수 통과·제나 Final Pass, 2026-08-30 20:58 KST 기준)가 정본. cto-seojin 추가분석: 캐시(TTL 1h)가 옛 progressive 바이트를 줬을 가능성도 배제됨(배포 1h39m 후 시험이라 캐시 만료됨) → **§0의 시험B(예열안된 card-jpeg)는 15:08 실패로 이미 실질 수행·실패 처리됨**, 남은 값있는 다음 수는 **시험A(진짜 정적 daily-bg/fire.jpg) 1건뿐** — 이게 실패하면 원인이 URL층이 아니라는 뜻이라 조사축 전환 필요. 다음 세션은 덱스의 결재카드 발의(시험A 단독, 자식컨테이너 1개, 형 승인 필요) 진행상황부터 확인할 것.

## ② blog.k-saju.me 배포 장애 — 미해결
- 증상: n8n 자동생성 파이프라인(매일 08:10 KST)은 정상 커밋 중(오늘도 08:30 08:21 커밋됨)이고 GitHub/Vercel 배포도 "success" 찍히는데, 프로덕션 도메인(blog.k-saju.me)엔 8/20("do-twins-have-the-same-saju") 이후 아무 글도 반영 안 됨. 도메인이 최신 배포를 안 받는 것으로 추정(Vercel 프로젝트 설정 문제로 보임).
- 클로 세션엔 vercel CLI/토큰 없어서 더 파볼 수단 없음 — 형이 Vercel 대시보드 직접 확인 필요한 상태로 대기 중.

## ③ 아투(american-todayz) — 해결됨
- 오늘 오전 06:00 자동발행 실패(재부팅 겹침으로 추정) → 클로가 수동 재시도(atz_scheduled.ps1)로 성공: 블로그 `american-todayz.com/2026/08/650.html`(10:51 KST), 쇼츠 `youtube.com/shorts/qnDSPHZBmkw`(11:53 KST). 완결.
- 08-27 보류큐(`out/held/2026-08-27T10-40-01-517Z_pm.json`, 본문 길이미달만)는 손 안 댐, 정기 보류큐 cron(15 7,21 * * *)이 처리할 것.

## ④ 하네스 훅 결함 — 수정 완료
- `.claude/hooks/guard-silence-and-delegation.mjs`의 위임관문이 `latestUserText()`가 최근 user 레코드 하나만 보는데 tool_result도 user타입이라, 워커가 수정 전 Read/Bash 한 번만 해도 승인문구가 밀려나 오차단되던 구조적 결함. 덱스가 수정 완료 + 회귀시험 5/5 통과, cto-seojin 실물 통과 확인됨(saju-studio card-jpeg baseline 패치 커밋 `6c9ffaa`는 이 수정 덕에 성공했음 — 다만 이 패치 자체는 나중에 위 ①에서 progressive 무죄로 판명돼 실제 장애 원인은 아니었음, 그래도 유지 무해).

## ⑤ Discord 채널 허용목록 — 반영 안 됨(미해결)
- 형이 `/discord:access group add 1543476607715643393`를 터미널에서 직접 실행(정상 절차), access.json에도 정상 반영됨[확인: 파일 직접 확인]. 그런데 `fetch_messages`로 그 채널 재시도해도 여전히 "not allowlisted" — 채널서버가 즉시 재반영 안 하는 것으로 보임(추정, 스킬 문서상엔 자동 재반영이라고 돼 있는데 실제로는 세션/서버 재시작이 필요할 수도 있음). 다음 세션에서 리셋 후 재시도해볼 것.
- 그 채널(1543476607715643393)은 형이 "결재승인 표준 양식" 예시를 올린 곳 — 못 봐서 아직 형이 요구한 정확한 양식(🟪🟪🟪 결재 요청 🟪🟪🟪 등, 덱스·제나가 자기들도 실제로는 못 봤다고 정정한 바 있음)을 확정 못 함. 지금까지 쓰던 `> 결재 요청` / 무엇을·왜·얼마나·위험·되돌리기 / `선택: 승인/보류/반려(취소)` 양식 그대로 계속 사용 중.

## ⑥ MOC 갱신 — archive-head-haru에게 위임, 진행 중(결과 미확인)
- 4개 낡은 MOC(아메리칸투데이·쇼츠자동화·k-saju·네이버블로그SaaS) 갱신 + 오늘 일지(`70 Record\2026-08-30.md` ## 🌅 오전 세션) 작성 + git commit/push를 archive-head-haru에게 위임함(agent name: morning-session-report). 완료 결과 아직 못 받음 — 다음 세션에서 결과 확인 후 형께 보고 필요.

## 세션 cron 재등록 필요
이 세션이 리셋되면 세션 cron 7개(라이브저장×2·세션마감보고서×2·주간전략리포트·아투보류큐·주간수익리뷰) 전부 사라짐. 다음 세션 시작 시 CLAUDE.md/session_bootstrap.md 지시대로 재등록 필수.
