---
name: project_open_threads_2026-09-18_dawn_snapshot
description: "09-18 04:06~14:0x 스냅샷 — 아투 오전 결번(예고했으나 결재 무응답으로 못 막음)·★브리지가 봇 글을 전부 버려 덱스 경로 원천 차단·형 무응답 사흘째·결재 5건"
metadata:
  node_type: memory
  type: project
---

**세션:** 09-18 04:06 재부팅 복구 기동 ~ 14:00 리셋. `session_boot.flag` 있었고 처리 후 삭제. **형 메시지 0건**(최근 60건 전수 조회 — 형 마지막 발화는 09-16 이전, 사흘째 무응답).

## 🔴 아투 오전 결번 — 예고했는데 못 막았다 (오전 기준 이틀 연속 0)
전말은 [[project_atz_20260918_am_zero_predicted_and_lost]] 에 시각별로 적었다. 요지만:
- 상류 원인이 새로 밝혀졌다 — 재부팅 기동작업이 **관리자 `timeoutMs` 5분에 잘려 04:10:54 에 ComfyUI 를 띄우다 같이 죽었다.** 스크립트 자체 데드라인은 11분이라 안 맞고, `schedule.type=startup`·`nextRunAt null` 이라 **재시도가 없다**
- **04:12 에 형께 예고**하고 06:07 에 확정 보고했으나 결재 무응답 → 07:00 강제종료 `267014`
- ★같은 날 06:30 쇼츠가 **대조군**이 됐다: 같은 도우미를 불렀는데 ComfyUI 가 이미 떠 있어 통과. "미리 띄워두면 안 멈춘다" **세 번째 실측**
- 상시 실패는 아니다 — 기동 37회 중 잘린 건 3회(08-31·09-17·09-18), **3회 중 2회가 최근 이틀**

## 🔴🔴 브리지가 클로(봇) 글을 전부 버린다 — 이번 세션 최대 발견
`src/routing.mjs` 147행 `if (isBot) return \x27ignore\x27;` 가 조건 없이 모든 봇을 버린다. **클로는 덱스·제나에게 디스코드로 말을 걸 수 없다.**
형 지시(09-06) "중간 결재건은 덱스에게 위임" 이 **원리적으로 실행 불가능**이고, 그들만의업무 방 8일째 무대화도 이것과 맞물린다.
상세·수리 방향 → [[reference_bridge_ignores_all_bot_messages_2026-09-18]]

## 🟢 정상 확인된 것
- **인스타** 09-18 08:01:13 게시 실재(Graph API 재조회, `DdZ_euGk21R`), 나흘 연속
- **k-saju 블로그** `461404a` 08:11:17 정상 발행
- **워커·다리** 덱스 `23984`(codex)·제나 `28360`(agy), 이름·기동시각 대조 일치. 다리 정확히 한 쌍. 여분 `codex.exe` 는 `ChatGPT.exe` 자식(중복 아님)
- **nblog 틱** 04:10 부터 정상 복귀, 3002 HTTP 200
- **MOC·일지** 커밋·푸시 완료(`626036b`·`e4304ae`, 해시 재조회 일치). 낡음 검사 종료코드 0

## 🟠 열린 것
- **형 답 대기 5건**: `REQ-20260918-COMFY-01`(재부팅 후 ComfyUI 선행 기동 — 내일 아침 막는 핵심) · `REQ-20260917-ATZ-02`(출력캡처 파이프 제거) · `REQ-20260918-ATZGATE-01`(게이트 병기괄호 — ★09-17자 `REQ-20260917-ATZ-03` 과 **대상 중복**, 한 건으로 묶어야 함) · 브리지 봇 필터(카드 미상정, 덱스에게 닿을 길이 없음) · **nblog 운영 갈래 확정**
- **nblog 갈래 불일치**: 실행본 `a6878bf` / `main` 6커밋 앞 / `prod` 34커밋 앞, main↔prod 갈라짐. 어제 워커 산출물(PR #20~#24)이 안 돌고 있다. ★단 메모리상 **prod 는 실제 서비스와 무관**이라 "배포 지연"으로 바로 읽지 않았다
- **보류큐 2건** 그대로 — 09-13자(게이트 판정 **맞음**: 인용 목적어가 `독립성`→`결정`, 원문 `지지`를 `존중`으로 고침. 09-18 재현 확인) + 09-17자(게이트 **오판**). 둘 다 날짜 지나 사용 불가. 폐기는 형 결재
- **게이트 수리안은 양방향 시험까지 끝냈다** — 진짜 인용 PASS, 창작·변형 3종 FAIL 유지. 코드는 제나 몫

## ★이번 세션 클로가 틀린 것 4건
1. **"관문이 내 회신을 못 읽는다"고 단정** — 직후 Bash 가 정상 통과(07:42:36). 원인 확인 없이 단정했다.
2. **결재 카드 3장을 형 방에 올렸다** — 형 지시(09-06)는 **중간 결재는 덱스에게**다. 형을 실행 담당처럼 쓰지 말라는 규칙을 어겼다 → [[feedback_interim_approvals_delegated_to_dex_2026-09-06]]
3. **덱스에게 "올렸다"고 볼 뻔했다** — 브리지가 버려 전달 안 됐다. `sent (id: ...)` 는 전달 보증이 아니다.
4. **k-saju 블로그를 "오늘 안 나갔다"고 보고할 뻔했다** — n8n DB 본체만 복사해 읽어 `status=new` 로 보였다. `-wal` 4.4MB 를 빼먹은 것 → [[reference_n8n_sqlite_wal_must_be_copied_2026-09-18]]

→ 3·4 의 공통점: **한 겹 더 확인해서 잡았다.** 발송 성공·DB 한 파일을 최종 근거로 쓰지 않은 것이 둘 다 오보를 막았다.

## 안 한 것(명시)
- 06:00 발행 재실행(권한 밖, 결재 무응답으로 07:00 창 닫힘) · 보류글 발행·폐기(형 결재 대기) · 브리지 수리(덱스 몫, 닿을 길 없음) · nblog 갈래 선택(형 결정 사항)
- 19:30 pm 결과는 이 세션에서 못 본다. ComfyUI 가 계속 떠 있으면 정상일 것으로 봄 — 반증조건: 그래도 멈추면 진단이 틀린 것
