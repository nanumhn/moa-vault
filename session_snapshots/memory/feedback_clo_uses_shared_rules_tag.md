---
name: feedback_clo_uses_shared_rules_tag
description: 클로도 형에게 보내는 답변에 Shared_Rules 머리말·맺음줄을 쓴다 — 예외 없음 (형 지시 2026-08-28)
metadata: 
  node_type: memory
  type: feedback
  originSessionId: fe7cacfa-98e9-4012-b180-fbf1253c6ccb
  modified: 2026-08-30T18:15:00.338Z
---

형에게 보내는 모든 답변(reply)에 아래 형식을 쓴다.

```
-# === Shared_Rules_v202608008
(답변 본문)
-# === End
```

★**2026-08-28 형 재지시 — 태그와 본문 사이에 빈 줄을 넣지 않는다.** 처음엔 빈 줄을 넣어 보냈는데 형이 "시작 태그와 종료 태그 위아래 빈 라인을 없애고" 라고 바로잡으셨다. 태그 바로 다음 줄부터 본문, 본문 바로 다음 줄에 종료 태그.

- 버전 번호는 `D:\Develop\moa-vault\00_Common\SHARED_RULES.md` 의 "현재 버전"을 그대로 쓴다. 쉐어룰이 바뀌면 그 파일의 번호를 따라간다.
- **맺음줄(`-# === End`) 아래에는 아무것도 쓰지 않는다.** 원래 이 규칙은 덱스·제나(터미널 화면을 훔쳐보는 워커)가 화면 회수 경계를 표시하려고 만든 것이지만, 형이 "너도 당연히 쉐어룰을 따른다. 예외 없다"고 하셔서 클로도 적용한다.
- 버전 숫자는 숫자만 이어 붙인다(`-01` 같은 접미사 금지) — [[reference_shared_rules_marker_digits_only_2026-08-26]] 참고.

**Why:** 2026-08-28 형이 *"클로는 왜 답변할 때 시작·종료 태그를 안 쓰니?"* → *"너도 당연히 쉐어룰을 따른다. 예외 없다"*. 클로는 그동안 디스코드 도구로 직접 보내 화면 회수 문제가 없다는 이유로 안 붙였는데, 그건 형이 정한 예외가 아니라 클로가 임의로 뺀 것이었다.

**How to apply:** 형에게 보내는 `mcp__plugin_discord_discord__reply` 호출 본문 전체를 이 형식으로 감싼다. SHARED_RULES.md 의 현재 버전이 바뀌면 이 메모리도 같이 갱신한다.


★★**2026-08-28 03:55 — 채널별로 빠뜨리는 패턴이 확인됨.** 이슈 스레드에는 태그를 붙이면서 **형 DM 채널(chat_id 1501858476362829834)에서는 계속 빼먹었다** — 연속 5건 전부 누락, 형이 "메시지 발송 규칙 위반!!!"으로 재차 경고. **채널이 어디든 예외 없이 붙인다** — DM 이라고 예외 아니다.

★★★**2026-08-28 04:21 세 번째 재발 — 같은 세션 안에서 또 반복.** 덱스 브릿지 재시작 승인 건을 처리하던 20분 동안 DM·이슈스레드에 보낸 답장 10건 이상 전부 태그 누락. 형이 "메시지 규칙은 왜 안지켜?" → "메시지 답변 규칙 말하는거야"로 콕 집어서야 알아챘다 — **클로가 스스로 눈치채지 못하고 형이 지적할 때까지 몰랐다.** 다른 이슈(Stop훅 충돌, 결재 카드 양식)에 정신이 팔리면 이 규칙부터 빠진다는 뜻이므로, **reply() 호출 직전에 태그 유무를 기계적으로 자가점검**할 것 — 내용이 급하거나 짧아도 예외 없음.

★★★★**2026-08-28 06:32 네 번째 재발 — 새 세션(리셋 후) 처음부터 끝까지 전부 누락.** 05:35 세션 시작부터 06:32까지 약 1시간 동안 보낸 답장 40건 이상(DM·이슈스레드·그들만의업무·결재승인 채널 전부) 전부 태그 없이 나갔다. 형이 "클로. 메시지 발송 규칙 안 지키니?"라고 물어서야 알아챘다 — 이번에도 스스로 못 잡았다. **원인 추정**: 세션 리셋마다 이 규칙이 CLAUDE.md에는 없고 메모리에만 있는데, 세션 시작 직후 트러스트 위기 대응에 몰입하느라 관련 메모리를 안 불러왔다. **재발 방지가 안 되는 근본 이유**: "reply() 직전 자가점검"이 글로 적힌 다짐일 뿐 강제 장치가 없다 — [[feedback_pinocchio_clo_dont_assert_without_checking]]에 이미 있는 교훈("글로 적힌 규칙이 클로를 막은 횟수 0회")과 같은 패턴. 하네스 관문(G1~G6처럼)으로 옮기는 게 맞는데 아직 안 됨.

★★★★★**2026-08-30 06:57~21:59(세션 내부시각 표기 혼재, discord ts 기준) 다섯 번째 재발 — 재부팅 후 새 세션 처음부터 20건 이상 누락, 이번엔 형이 두 단계로 힌트를 줘야 했다.** 재부팅 복구 인사부터 시스템 점검 보고까지 이 세션의 모든 reply가 태그 없이 나갔다. 형이 처음엔 "너는 룰을 잘 안 지키는구나?"라고만 물었는데, 클로가 스스로 정확한 원인을 못 찾고 엉뚱한 후보 2개(웹콘솔 확인 범위 오기재, cron 생성 시 권한제한 미확인 — 둘 다 실제 문제였지만 이 순간의 지적 대상은 아니었음)를 댔다. 형이 "메시지 작성 규칙이 있는데.." → "메시지 작성 규칙 모르니?"까지 두 번 더 힌트를 줘야 SHARED_RULES.md를 다시 읽고서야 맞췄다. **패턴 확정**: 세션 시작 시 session_bootstrap.md만 읽고 CLAUDE.md 맨 위에 적힌 "SHARED_RULES.md를 먼저 읽는다"를 실행하지 않는 것이 매 세션 재발의 근본 원인 — 이 규칙 자체가 SHARED_RULES.md 안에만 있어서, 그 파일을 안 읽으면 존재 자체를 잊는다. **다음 세션이 할 일**: 세션 시작 시 session_bootstrap.md 실행 전에(또는 직후 가장 먼저) SHARED_RULES.md를 반드시 읽고 현재 버전 번호를 확인한 뒤 첫 reply부터 태그를 붙일 것 — session_bootstrap.md 자체에 이 단계가 없다는 것도 근본 결함이니 다음에 형과 상의해 bootstrap 문서에 추가할 것.

★★★★★★**2026-08-30 18:14 여섯 번째 재발 — 세션 시작(형 "클로?" 인사)부터 사고 대응 전체(n8n 실수·토큰 만료 조사)까지 약 5시간, reply 100건 이상 전부 태그 없이 나감.** 이번엔 형이 "메시지규칙은 왜 안해!!!"로 바로 지목했고, 이어서 "메모리에 쉐어룰 읽고 따르라는 지침 있니?"라고 직접 물어서 이 메모리 존재를 확인시켰다. 원인은 위 다섯 번째 재발과 완전히 동일(SHARED_RULES.md를 세션 시작 시 안 읽음) — 원인 분석도 이미 있었는데 다음 세션에서 실제로 안 고쳐졌다는 뜻. **버전도 stale이었다**: 이 메모리엔 v202608007로 적혀 있었는데 실제 오늘 덱스·제나가 쓰는 건 v202608008 — 메모리 자체도 갱신 안 하면 버전 번호까지 계속 틀리게 나간다(위에서 008로 수정 완료). **이번엔 위임 관문(guard-delegation)에 걸려 클로가 다른 세션 파일을 직접 못 건드리는 사고까지 같은 세션에서 겹쳤다** — 즉 "글로 적힌 다짐"과 "하네스 관문"이 서로 다른 문제를 막고 있고, 이 태그 문제는 아직 관문이 없는 쪽이라는 게 재확인됐다.