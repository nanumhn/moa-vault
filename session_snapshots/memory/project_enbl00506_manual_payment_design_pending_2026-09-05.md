---
name: project_enbl00506_manual_payment_design_pending_2026-09-05
description: "[엔블 005] 주문신청→수동결제완료→회원ACTIVE전환 파이프라인 설계 합의완료, 형 결정 5가지 대기중(요금/계좌 등)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2ab22672-428f-4355-bec6-61395ab2f76e
  modified: 2026-09-04T18:25:42.181Z
---

형이 [엔블 005-03] 이후 "연장신청·결제·회원상태가 연결되게 작동해야 한다"고 지적 → 이슈처리-2026 스레드([엔블 005] 운영 테스트·개선·베타 배포, channel_id `1544531657795829923`)에서 클로·덱스·제나 3인 토론으로 설계 합의됨(2026-09-05 00:48~00:53 KST).

**제나 실측 진단(핵심 결함 2건)**:
- `Payment` 모델(schema.prisma 520행)은 필요 필드가 이미 다 있는데 실데이터 0건 — 결제 원장 자체가 안 쓰이고 있었다.
- `members.ts`의 `planAccessChange()`가 `currentPeriodEnd`가 null인 계정은 연장해도 `status`를 계속 `TRIALING`에 묶어둔다 — 관리자가 연장해줘도 정식 `ACTIVE`로 안 넘어가는 로직 결함.

**합의된 설계**:
- 새 `Order` 모델 안 만듦. `ExtensionRequest`에 `depositorName`(입금자명)·`amountKrw`(금액) 2필드만 경량 추가 + 기존 `Payment` 모델을 결제원장으로 활성화.
- `/api/admin/users/[id]/route.ts`에 이미 있던 "기간연장↔신청연계" 트랜잭션(2026-09-03 구축, [[project_enbl00503_deployed_2026-09-04]]와 무관한 별도 기능)을 `settleManualPayment` 공용 함수로 추출 — 회원표 연장 버튼과 새 주문승인 모달 양쪽에서 재사용. 승인 시 Payment생성+Subscription ACTIVE전환(TRIALING 결함 동시수리)+ExtensionRequest DONE+AuditEvent를 트랜잭션 하나로.
- UI(★수정, 제나 지적으로 클로 원안 폐기): 별도 "주문" 표를 새로 안 만든다 — 스키마를 ExtensionRequest 확장으로 정한 이상 표도 하나로 통합. 기존 "연장신청" 표·`ExtensionRequestModal`을 그대로 확장해 입금자명·입금금액 입력란과 "결제완료 승인" 버튼만 추가, 상태값 4단계(접수대기/처리중/결제완료/반려)로. 새 컴포넌트 없음. 사용자 쪽은 `/expired` 화면 내 접수증/입금안내 카드로(별도 페이지 대신, 빠른 적용 위해 추천).

**형 결정 대기 5가지(구현 보류 사유)**:
1. 요금표(베이직/프로 월요금 + 1·3·6·12개월 금액)
2. 입금계좌(은행·계좌번호·예금주 — 화면 노출)
3. 데이터 방식 확정(경량확장 추천안 vs Order 신모델) — 사실상 경량안으로 합의됨, 형 최종 승인만 필요
4. 사용자 동선(화면 내 카드 추천 vs 별도 페이지)
5. 승인 트랜잭션 규칙(REQUESTED→PAID/CANCELLED, PAID 재처리 차단)

이 요청은 형 DM 채널(1501858476362829834)에도 링크와 함께 전달됨(message_id 1545461525890998463).

**★2026-09-05 완료**: 이슈 [엔블 005-04]로 정식 상정(REQ-20260905-ENBL00504-01/-02), 클로가 구현(커밋 `4595bb4`, 브랜치 `clo/enbl-005-01-extension-status`)·제나 독립검수 PASS·형 승인·운영 배포까지 전부 끝남. 릴리스 `20260905015320`, 무중단 전환, 운영 Neon DB 마이그레이션 `20260905010000_extension_request_manual_payment` 적용 완료, 헬스체크 전부 200. 관리자 쪽(연장신청 표 확장·결제완료 승인 모달·`settleManualPayment` 트랜잭션·TRIALING 결함 수리)은 운영 반영됨.

**남은 것(별도 후속)**: `/expired` 사용자 화면(입금자명 입력·계좌안내 카드)은 요금표·계좌정보가 여전히 미확정이라 의도적으로 미착수. 형이 요금/계좌를 확정하면 그 부분만 이어서 구현할 것 — 이때는 새 이슈 없이 이 파일이 계속 참고선례.

**How to apply**: 다음 세션은 이 이슈를 "구현 대기"로 취급하지 말 것 — 관리자 쪽은 완료·배포됨. 남은 건 /expired UI 하나뿐이고 요금/계좌값 나오면 착수.

**★★★2026-09-05 [엔블 005-04] 공식 종료(형 확정)**: 커밋 `1942625`(전화번호 검수수정)·`64448b5`(입력칸 IME/숫자 정규화)까지 전부 배포·검수·형 최종확인 완료. 최종 릴리스 `20260905032001`. 후속은 [엔블 005-05](결제관리 메뉴·관리자 직접주문·Plan/Option A/B 상품체계, 형 가격정책 확정 대기 중, 별도 스레드 channel_id `1545495282102435972`)로 이관됨 — 다음 세션은 005-04를 완전히 닫힌 것으로 취급할 것.

**★2026-09-05 추가 완료 + 배포까지 끝남**: 형이 실제 운영화면에서 테스트하다 "입금자명만으론 식별 어렵다"고 지적 → 전화번호 3단 분기 추가. `User.phone`은 ①비어있으면 자동 ②같으면 그대로 ③다르면 운영자가 모달에서 명시적으로 "덮어쓰기" 체크해야만 갱신 — 다른 사람이 대신 입금했을 때 회원 연락처가 조용히 바뀌는 사고 방지. `AuditEvent`엔 마스킹(`010-****-5678`)만, `Payment.memo`엔 관리자용 원문 보존.
- 커밋 `4b2e71a`(구현) → 덱스 검수에서 결함 2건 발견(normalizePhone 문자혼입 우회 가능, 모달 conflicts 판정이 빈 문자열을 "번호있음"으로 오판) → 수정 커밋 `1942625` → 재검수 PASS.
- 배포 완료: 릴리스 `20260905024541`, 운영 Neon DB 마이그레이션 `20260905020000_extension_request_depositor_phone` 적용, 무중단 전환, 헬스체크 전부 200. 테스트 최종 40/40.

**★같은 세션에서 튀어나온 후속 요구(전부 설계만, 미구현·별도 이슈 필요)**: 관리자가 신청 없이 직접 주문 추가하는 기능(설계안은 클로가 제시함 — ExtensionRequest를 그대로 재사용, `[관리자 직접 등록]` 마커로 출처 구분), 상품 체계 통일(`Plan`=구독제, `Option A`=AI, `Option B`=Blog3, 가격 미확정), 결제취소 시 구독기간·애드온 회수(제나가 안전 회수 규칙 설계 — 원장기반 grantDays 차감 + 다른 PAID건 floor 가드 + 애드온 회수, 부분환불 정책만 형 확정 대기). **교훈**: 이 세션에서 요구사항이 실시간으로 계속 늘어나 위임관문 승인·범위가 계속 리셋되는 혼란이 있었다([[feedback_delegation_gate_error_marker_format_2026-09-05]] 참고) — 다음 세션은 이 4가지(관리자직접추가/상품체계/취소회수/전화번호) 중 전화번호만 끝났고 나머지 3개는 설계만 있고 구현 0%임을 명확히 인지할 것.
