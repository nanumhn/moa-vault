---
name: project_atz_gate_dual_path_disparity_2026-09-06
description: "아투 게이트가 발행 때와 재검수 때 서로 다른 평문을 봐서 통과할 글을 막는다 — 2026-09-07 기준 수리는 작업트리에만 있고 커밋 안 됨, 회귀 테스트는 이 결함을 못 잡음"
metadata:
  type: project
---

**한 줄**: 아투 블로그가 매일 빠지던 진짜 원인은 글 품질이 아니라 **게이트가 같은 글을 두 번 다르게 판정하는 것**이다. 2026-09-06 확정. **2026-09-07 정정: 수리 코드는 작업트리에 있으나 커밋되지 않았고, 이를 지킬 회귀 테스트는 구버전에서도 통과해 결함을 못 잡는다.**

## 확정된 사실

`qa-gate.mjs` 692행이 갈림길이다.

```js
const plainText = bodyMd ? bodyMd.replace(/\s+/g, ' ') : htmlToPlain(html);
```

| 경로 | bodyMd | 평문화 | 결과 |
|---|---|---|---|
| 발행(`run.mjs` 474행) | 있음(`art.bodyMd`) | 공백 하나로 뭉갬 | 소제목이 다음 문장에 붙음 → 오탐 |
| 재검수(`--from`) | 없음 | `htmlToPlain` 이 블록태그를 ` . ` 로 | 정상 통과 |

보류 payload 키는 `title, html, labels` 뿐이라 **`bodyMd` 가 저장되지 않는다.** 그래서 재검수는 항상 아래쪽 길로 간다.

**실측 예 (2026-09-06 저녁)**: `### 📰 사실 확인` + `관세청은 …` 이 한 문장이 되어 `ACTOR_RE` 의 `[가-힣]{1,6}\s?` 가 `확인 ` 을 삼켜 **`actor = "확인 관세청"`** 을 만들었다. 원문엔 `관세청` 이 단독으로 1회 있을 뿐이다. 발행 11/12 보류 → `--dry` 재검수 12/12 통과.

같은 유형을 하루에 두 번 봤다: 오전 `2026-09-04T10-37-04-199Z_pm`(`"전환 정부"`,`"거론 정부"`), 저녁 `2026-09-06T10-03-45-623Z_pm`(`"확인 관세청"`).

## 왜 아무도 몰랐나 — 3중 침묵

1. **보류는 웹훅을 안 쏜다** (2026-07-27 형 지시로 그렇게 설계됨)
2. **블로그는 원장에 아무것도 안 쓴다** — `publish_ledger` 는 쇼츠 전용. 07-27 이후 블로그 기록 0건
3. **보류는 정상 흐름이라 `exit=0`** — 스케줄러 실패 알림도 안 울림

그래서 하루 2회 발행이 통째로 빠져도 겉으로는 정상이었다. 형이 물어봐야만 드러났다.

## 증거 위치 (레포 밖 — 리셋 `git add -A` 회피)

`C:\Users\user\.moa\evidence\atu001_20260906\` · README에 재현 명령 포함

```
cd D:\Develop\moa-studio\tools\atz-pipeline
bun run.mjs --from=...\out\2026-09-06T09-45-10_pm_payload.json --dry
```

## ★2026-09-07 07:50 재확인 (클로 직접 실행) — 위 "미수리" 서술 정정

**① 수리 코드는 있는데 커밋이 안 됐다.** `git status` = ` M tools/atz-pipeline/qa-gate.mjs`, HEAD 최신 커밋은 여전히 `b4f33e4`(수치 계열).

```diff
- const plainText = bodyMd ? bodyMd.replace(/\s+/g, ' ') : htmlToPlain(html);   // HEAD(커밋본)
+ const plainText = htmlToPlain(html);                                          // 작업트리에만 있음
```

지금 발행이 도는 건 작업트리 코드 덕이다(2026-09-07 06:06 블로그·06:45 쇼츠 자동 발행 성공, 원장·공개URL 200 재조회). **누가 `git checkout` 한 번 하면 어제 증상이 그대로 돌아오고, 보류는 경보를 안 울리니 아무도 모른다.**

**② 회귀 테스트 `gate-heading-bleed-20260807.test.mjs` 가 이 결함을 못 잡는다 — 헛통과다.**
HEAD 코드를 임시 폴더에 복사해 돌려도 **16 passed / 0 failed**. 원인은 시험 픽스처 문구:

| 시험이 쓴 문구 | 실제 기사 문구 |
|---|---|
| `미국 관세청은 …` → 오탐 **안 남** | `관세청은 …` → 오탐 **`"확인 관세청"` 발생** |

`ACTOR_RE` = `([가-힣]{1,6}\s?(?:기관목록))\s*(?:는|은|이|가)`. 기관명 앞에 `미국 `이 붙으면 그 자리를 국가명이 차지해 소제목 꼬리말(`확인 `)이 못 들어온다. **픽스처에 "미국"을 붙인 것이 시험 자신을 면역시켰다.** 재현: 스크래치패드 `atzhead/repro2.mjs`.

→ 수리 시 **픽스처를 접두사 없는 기관명(`관세청은`)으로 바꿔 먼저 실패시키고**, 그다음 통과시킬 것. [[feedback_check_tool_can_false_pass]] 그대로의 사례.

## 다음 세션이 알아야 할 것

- 2026-09-06 커밋 `b4f33e4` 는 **수치 계열** 오탐 수리이고 이 건은 **기관 행위 계열**이다. 섞지 말 것
- 수리 방향: 게이트 입력 단일화 + live/`--dry` 동일 payload 회귀 테스트
- ★내가 추가 요청한 항목: **보류 저장 시 `bodyMd` 도 같이 저장**. 지금은 판정에 쓴 입력이 안 남아 보류 파일만으로는 재현이 원리적으로 불가능하다
- 쇼츠 결번은 이 건의 **결과**다. 별도 고장이 아니다 — `drafts[슬롯]` 이 오늘 날짜 + 발행완료여야 쇼츠가 도는데, 블로그가 막히면 재료가 없다

## 보류큐 실측 (2026-09-07 07:45, 클로 직접 조회)

`out/held/` 3건 전부 `awaiting-qa-review` 였으나 실제로는:

| 파일 | 제목 | 실제 상태 |
|---|---|---|
| `2026-09-04T21-08-21-074Z_am` | 5억달러 투입 | **이미 발행됨** `/2026/09/5.html` (200) — 잔해 |
| `2026-09-06T10-03-45-623Z_pm` | AI 붐·무역적자 | **미발행**, `--dry` 재검수 12/12 통과 |
| `2026-09-06T11-30-03-032Z_pm` | 이란 반발 | **이미 발행됨** `/blog-post_677.html` (200) — 잔해 |

**보류 파일의 `status` 필드는 현재 상태가 아니라 보류된 시점의 상태다.** 발행 여부는 `state.json`의 `published` 제목 대조 + 공개 URL 200으로만 판정할 것. 결재 `REQ-20260907-ATZHELD-01` 로 형께 상정함.

관련 [[reference_atz_gpt_fallback_quality_risk_2026-08-07]] [[reference_qa_gate_rule_inversion_blindspot_2026-08-07]] [[feedback_check_tool_can_false_pass]]

---

## 2026-09-09 07:5x — 원인 확정. 2줄이고, 아직 미커밋이다.

`git diff tools/atz-pipeline/qa-gate.mjs` 직접 열람 결과 **작업트리에만 있는 2줄 수리**가 그것이었다(커밋 안 됨, `git status` = `M`):

```diff
-  const plainText = bodyMd ? bodyMd.replace(/\s+/g, ' ') : htmlToPlain(html);
+  const plainText = htmlToPlain(html);
```

**왜 두 경로가 갈렸나 — 이게 정확한 기전이다.**
`qaGate({ ..., bodyMd })` 에서
- **생성 경로**(`runGenerate`)는 `bodyMd` 를 넘긴다 → 옛 코드가 `bodyMd.replace(/\s+/g,' ')` 를 쓴다 →
  **소제목 경계 처리가 없다** → 소제목이 다음 문단 첫 문장에 붙는다.
- **재발행 경로**(`runFromPayload`)는 `bodyMd: ''` 를 넘긴다 → `htmlToPlain(html)` 로 떨어진다 →
  소제목 경계가 살아 있다 → 통과.

즉 "같은 원고, 같은 근거, 다른 판정"의 원인은 게이트 로직이 아니라 **호출자가 넘기는 인자**였다.

**실측 증거 1건**: `out/2026-09-06T09-45-10_pm_payload.json` (제목 "트럼프의 AI 붐…").
09-06 19:03 생성 경로에서 `기관 행위 주장 근거` FAIL — 사유 `원문에 없는 기관 행위 주장 1건: "확인 관세청"`.
본문 실제 문구는 `📰 사실 확인` (h3) + `관세청은 올해 누적 수출액이…` 이고, **"관세청"은 원문에도 있다**(`grep -c` = 1).
소제목 꼬리 `확인` + 다음 문장 머리 `관세청` 을 이어 읽어 없는 기관을 만들어낸 것.
**payload 파일은 손대지 않았는데**(mtime 09-06 19:03, result.json과 동일 · `.orig-held.json` 없음)
09-09 `--from --dry` 로 돌리면 **12/12 통과**한다.

**★위험**: 이 2줄이 커밋되지 않은 채로 있어서 누가 `git checkout`/`git restore` 한 번만 하면 오탐이 되살아난다.
회귀시험 픽스처가 `미국 관세청` 이라 자기면역인 문제도 그대로다 — 실제 문구 `관세청` 으로 바꿔야 실패한다.
**형 결재 올림**: `REQ-20260909-ATZHOLD-01` (09-09 07:5x, 메시지 `1547017862617632871`). 담당 = 제나.
관련: [[project_atz_qwen_fallback_hold_blocked_blog_2026-09-09]]
