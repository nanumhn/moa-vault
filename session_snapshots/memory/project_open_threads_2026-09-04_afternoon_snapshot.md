---
name: project_open_threads_2026-09-04_afternoon_snapshot
description: "2026-09-04 14:00 세션종료 스냅샷(최신) — IG캐러셀 사고 수리완료, 블로그 08:10 미실행 발견(원인미확인), 엔블005-01 커밋브리지 스코프 원인 확정·클로 승인대기"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5878feeb-303f-44fb-a7a2-9817e684cdc4
  modified: 2026-09-04T05:28:59.931Z
---

**2026-09-04 14:2x KST, 오후 세션리셋 직전 저장.**

## 오늘 일어난 일 (우선순위순)

1. **★블로그 08:10 자동발행이 오늘 아예 실행 자체가 안 됐다 — 원인 미확인, 형 어제 확인요청분.**
   - `[확인: n8n execution_entity 직접 재조회]` 08:10 KST 전후 시간대(23:00~23:40 UTC 09-03) 전체 실행기록에 `blogAutoPost001` 항목이 하나도 없다. IG 캐러셀(08:00)은 "실행됐다 실패"였는데, 이건 "트리거 자체가 안 울렸다"는 점이 다르다.
   - 워크플로우는 04:31 재부팅 때 정상 활성화(`n8n.audit.workflow.activated`, activationMode:init)로 로그엔 남아있다 — 그런데도 08:10에 안 울렸다. 원인 미확인.
   - 클로가 이 08:10 체크를 어제 형께 약속했는데, IG 캐러셀 조사에 밀려 14시경(사후)에야 뒤늦게 확인해 형께 사과함.
   - **다음 세션 할 일**: 원인 규명(스케줄 트리거 등록 실패인지, cron 표현식 문제인지, 다른 워크플로우와 충돌인지) + 오늘자 글 수동발행 여부 형 판단 대기(14:2x경 클로가 형께 "수동발행 vs 원인규명 먼저" 물어봄, 답변 대기 중 세션 종료 가능성 있음 — 그 경우 다음 세션이 형 답 확인부터 할 것).

2. **★IG 캐러셀 자동발행 사고 — 실측·수리 완료.** `[확인]`
   - 09-03 밤 IG 장기토큰 교체 시 워크플로우(ksajuCarouselV5) 노드 12개 중 4개(Carousel1·2)에 반영이 빠져 있었음 → 09-04 08:00 자동실행이 "Get IG User ID"에서 Bad Request로 실패, 알림도 도달 안 함(igAlert0001 발동은 됐으나 도달경로 미확인) → 형이 "인스타 자동발행 체크해"라고 직접 물어서 발견.
   - 수리: 4개 노드 자격증명 통일 → `N8N_RUNNERS_BROKER_PORT` 환경변수로 포트충돌 우회해 수동 재발행 성공(media id `18009170114763036`, https://www.instagram.com/p/Dc2LQzPE6t0/, Facebook Graph API 재조회로 라이브 확인) → `docker restart n8n`으로 재활성화(다른 워크플로우 12개도 전부 정상 재활성화 확인, blogAutoPost001 포함).
   - 재발방지: `session_bootstrap.md`에 ⑤-C(매일 08:20 자동확인 크론) 신설. 단 세션cron이라 리셋마다 사라지는 구조 — ⑤-A·⑤-B와 같은 취약점, 완전한 재발방지 아님.
   - 형이 "기억했다가 내일 체크해. 오늘도 체크 안됬어."라고 재차 지적함(오늘 체크도 형이 직접 물어서 한 것이었다는 지적) — 클로가 인정.
   - 하네스 운영 MOC·덱스제나 워커 MOC에 전체 경위 반영, 랩실 commit+push 완료(`da16055`).

3. **[엔블 005-01] 커밋 안 되던 진짜 원인 확정 — 형 승인 대기 중.**
   - 덱스가 원인 찾음[보고받음: 덱스]: 덱스의 커밋 브리지가 `D:\Develop\dex-workspace`만 대상이라, `D:\Develop\nblog-saas-clo-enbl00501` 워크트리 커밋 요청이 애초에 반영될 수 없는 구조였다(9/2~9/4 내내 "제출했다"고 보고했지만 실제로 반영 안 됐던 이유).
   - 덱스가 클로에게 "그 워크트리에서 같은 8개 경로 커밋 가능한지" 요청 → 클로는 직접 git 접근 가능하지만 [[feedback_temporary_restricted_authority_2026-08-28]] 때문에 형 승인 없이 커밋 안 함 → 형께 승인 요청 보냄(14:2x), **답변 대기 중 세션 종료될 수 있음** — 다음 세션이 형 답변부터 확인할 것.
   - 워크트리 상태(재확인, 14:2x): `D:\Develop\nblog-saas-clo-enbl00501` 여전히 커밋 0건(수정 7파일 + 신규 마이그레이션 1건 워킹트리에만). 메인 저장소 HEAD `f0c7084`(09-02) 그대로.

4. **nblog DB 이전(Neon 탈출)** — 오늘 별도 확인 안 함, 09-03 상태(후보 조사 중, 미완료) 그대로일 가능성 높음. 다음 세션이 덱스에게 재확인할 것.

5. **Jade Almanac 백링크 메일** — 형이 스크린샷으로 문의, 클로가 링크 내용까지 확인해 "인용해도 될 만하나 자사 계산기 포함 비교"라고 보고. 답장/인용 여부 형 판단 대기, 급하지 않음.

## 오늘 재부팅 복구 (04:30경) 정상 완료
session_boot.flag 확인 → 다운타임 미응답 형 메시지 0건 확인 → 세션 cron 7개 재등록 → 외부 워치독 3종 Disabled 상태 확인(08-30 통합콘솔 이관과 일치, 정상) → Monitor 배경감시 재무장+자체시험 통과.

## 다음 세션 시작 시 할 일
1. session_bootstrap.md 순서대로 부트스트랩(cron 7개+⑤-C 포함 재등록).
2. **형이 블로그 08:10 미실행 건에 뭐라고 답했는지 먼저 확인** — 수동발행 지시가 있었으면 그것부터.
3. **형이 엔블005-01 커밋 승인했는지 확인** — 승인됐으면 `D:\Develop\nblog-saas-clo-enbl00501`에서 그 8개 경로 커밋(덱스가 지정한 정확한 파일 목록은 이슈 스레드 chat_id `1544551867214733312` 확인).
4. 블로그 08:10 미실행 원인 규명(스케줄 트리거 로그 재조회부터).
5. nblog DB 이전 진행상황 덱스에게 재확인.
