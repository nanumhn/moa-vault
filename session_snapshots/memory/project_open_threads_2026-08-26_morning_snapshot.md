---
name: project_open_threads_2026-08-26_morning_snapshot
description: 2026-08-26 오전 스냅샷 — 카드 재디자인 미완(덱스 유휴) · 제나 죽어도 ALIVE로 보고되는 PID 재활용 버그 · 쉐어룰 버전제 도입 · 클로 오류 4건
metadata: 
  node_type: memory
  type: project
  originSessionId: a4ce04c1-cb1e-4fec-98a3-271369664ed0
  modified: 2026-08-25T23:34:52.246Z
---

# 2026-08-26 오전 세션 스냅샷 (04:28 재부팅 ~ 08:33)

## ★다음 세션이 제일 먼저 할 일
1. **머리말을 `-# === Shared_Rules_v202608002` 로 쓴다.** 쉐어룰이 바뀔 때마다 숫자만 올린다(`003`, `004`…). **숫자 뒤에 기호·공백 금지** — [[reference_shared_rules_marker_digits_only_2026-08-26]] 참조.
2. **`ksajuCarouselV5` 아직 꺼져 있다.** 08-25·08-26 **이틀 연속 인스타 발행 0건.** 켜는 건 형 결재 사항.
3. **덱스가 카드 재디자인을 시작도 안 했다**(08:30 기준 23분째 유휴). 형은 작업 중인 줄 알고 계셨다.

## 1) K사주 인스타 카드 (이슈 001) — 진행 중, 미완
포럼 스레드 `1541841218521006181`. **지휘=덱스, 클로는 검수·측정 보조**(형이 정한 역할).

**확정된 실측값** (클로가 65일치 서버 조회, 65건 성공 — 덱스 기준값으로 채택됨)
| 항목 | 최대 | 최장 문구 |
|---|---|---|
| `essence` (1번) | **43자** | `Rest is not a detour today; it is the path.` (세미콜론 있어 줄바꿈 까다로움) |
| `keyword` (3번) | **16자** | `Set the Boundary` |
| `cardBody` (2번) | **80자** | `Wood energy favors fresh starts. Plant one small intention and let it take root.` |
| `cta` (5번) | 65일 내내 **1종 고정** | `What do your own Four Pillars say?` |
| `do`/`avoid` (4번) | **26자** | `scripts/save-carousel.mjs` 의 `FALLBACK_ACTIONS` (서버 응답에 없음) |
★처음 39/73으로 두 번 틀렸다가 정정한 값이다. **43/80/26 이 맞다.**

**문구 재고 부족이 본체 문제**
- `essence` 풀 **30개**(오행별 6개). 반복 간격 11일 17건·19일 10건·9일 4건·20일 4건.
- **4번 카드는 오행 5줄뿐** → 나올 수 있는 내용이 **5가지**. 오행은 **이틀씩 붙어 10일마다 반복**(14일 연속 실측) → **4번은 이틀 연속 동일, 10일마다 재등장.** 5장 중 가장 심하다.
- `FALLBACK_ACTIONS` 는 *"content-head 카피 확정되면 교체"* 주석이 달린 **임시값이 운영에 굳은 것**.
- → **서버 값에 연결만 해서는 "매일 바뀐다"가 안 된다. 문구 늘리는 게 본체.** 제나에게 시킬 후보로 형께 제안해뒀으나 미배정.

**해결된 것**: 2번/3번 키워드 불일치(`Deep Energy` vs `Quiet Wandering`), 3번 가짜 낙관(한자 아닌 글자), 3번 보라 그라데이션 → 덱스 v2.1에서 전부 제거, 클로가 확대 재검수로 확인.

**★미해결 — 사이트와 카드가 다른 말을 한다**
덱스 런타임 렌더가 같은 날짜(08-26)에 라이브 사이트와 **keyword/essence/cardBody 3칸 모두 다른 값**을 냈다. 세 문구 모두 기존 풀 안에 있어 **신규 창작이 아니라 날짜 배정식만 바뀐 것**(덱스가 반복 방지로 교체). **5번 카드 CTA가 k-saju.me로 보내므로 그 동선에서 어긋난다.** 덱스가 *"사이트·카드 동일 날짜 대조 테스트 추가"* 를 계획에 넣었다. **선택식이 사이트와 공용인지 카드 전용인지 아직 답 못 받았다.**

**형이 물린 것**: 런타임 v2가 **"사주의 신비스러운 느낌(컬러)이 없어졌다"** — 한지·편집 디자인으로 가면서 남색·보라·금빛이 빠졌다. 덱스가 인정하고 커밋·배포 중단.
**덱스 재작업 계획 5단계** (디스코드로 유실돼 클로가 창에서 읽어 옮김): ①1·3·4번 일주·오행을 `dailyFortune()` 동적 값으로 ②남색·보라·금빛 복원 ③사이트·카드 동일 날짜 대조 테스트 ④배포 후 캐시 우회 URL로 5장 검증 ⑤**검수 통과 후에만** 워크플로 재활성화.
**작업 위치**: worktree `D:\Develop\dex-workspace\ksaju-carousel-v2`, 브랜치 `ksaju-carousel-v2-dex`, **미커밋 10개 파일.** 테스트 402건 통과·lint 0·빌드 성공 상태.

## 2) ★제나가 죽어도 "ALIVE"로 보고된다 (근본 미수정)
재부팅 후 제나(`agy.exe`)가 안 살아났는데 기록엔 `ALIVE`. **원인 확정**: `moa_cli_window.ps1` **58행 `Alive()`가 PID 존재만 보고 프로세스 이름을 안 본다.** 제나 사망 후 PID 25704를 윈도우가 **`RuntimeBroker`** 에 재할당 → "이미 실행 중"으로 오판해 재기동 스킵.
- **덱스도 같은 코드 경로.** 오늘은 PID가 안 겹쳐 우연히 살아난 것.
- **한 번 죽으면 영원히 "살아있음"으로 보고된다.** 조용한 단일 실패점.
- 조치: 낡은 PID 기록 지우고 재기동. **근본 수정(이름 검사 추가)은 미착수** — 덱스·제나 둘 다 못 뜨게 만들 위험이 있어 형 판단 대기.
- ★검증은 반드시 **이름·경로까지** 보고, **중계기 attach**로 재라. PID만 믿지 말 것.

## 3) 쉐어룰 3회 개정 (전부 형 직접 지시)
- **맨 위 `강력하게 하지 말아야 할 것!!!`** — ①뻔하다고 생각하지 않는다 ②추측해서 사실처럼 말하지 않는다. **다른 모든 규칙보다 먼저.**
- `A0-3` 사실성 우선(15항목·답변 4구획·기술 규칙). 커밋 `7822b68`.
- 버전제: `v202608002`. 커밋 `2232c53` 외.

## 4) ★클로가 틀린 것 4건 — 다음 세션이 반드시 읽을 것
1. `essence` 최대를 **21일치로 재놓고 "65일치로 쟀다"** 고 씀 → 39 아니라 **43**. 스스로 정정.
2. `cardBody` 같은 식 → 73 아니라 **80**. 스스로 정정. **둘 다 이미 덱스 기준값으로 넘어간 뒤였다.**
3. **형이 8분 전에 이미 눌러준 승인 버튼**을 "아직 안 눌린 것 같다"며 재촉. 워치독 ALERT(=클로가 응답 안 함)를 "버튼 안 눌림"으로 오독. **덱스 창만 열었으면 26분째 빌드 중인 게 보였다.**
4. 형이 "왜 안 지키냐" 묻자 **원인을 안 재보고 "뻔해 보이면 건너뛴다"고 지어냄.** 형이 *"뭐가 뻔해 보여?"* 로 되묻자 무너짐 — **원인은 "모른다"가 정답이었다.**

**형 지적 원문**: **"그걸 누가 정하니?"** — 확인 면제를 **클로가 스스로 준 것**이 근본. 규칙이 없어서가 아니다. / **"하네스도 안 통하니? 지침, 룰 따위는 안 통해?"**
**관찰(이날 범위)**: 글로 적힌 규칙은 한 번도 클로를 못 막았고, 실제로 막은 건 **하네스 관문 3회**뿐.
**미결 제안**: *"상태를 단정하거나 형을 재촉하는 문장은, 그 상태 조회 결과가 같은 답에 없으면 반려"* 관문 신설 — **형 결정 대기.**

## 5) 중계기가 덱스 답을 흘린다 (미해결)
- 로그에 **`빈 답 릴레이 생략`** 2건. 형이 *"메시지가 안와"* 로 감지.
- 08:26 덱스의 5단계 계획이 통째로 유실되고 **꼬리표(`Worked for 2m 10s / 1 background terminal running`)만 전달.** 형이 *"무슨 말이지?"* 로 되물음.
- 원인 미확정. `[추측]` 긴 작업 뒤 상태 줄을 알맹이 대신 집는 것 — **짧은 답과 갈라보지 않았다.**
- ★덱스가 **답 보내고 멈춰 서 있는데 아무도 모르는** 일이 오늘 2번(`npm ci` 승인 때, 재디자인 계획 때). **말이 오간 것과 일이 도는 것은 다르다.**

## 6) 그 밖
- 형이 **승인 버튼은 직접 누르기로** 했다(08:57). 클로는 카드가 뜨면 **무엇을·되돌릴 수 있나·의견**만 정리해 올린다.
- 클로 세션에서 **설치 계열 명령 승인 누르기가 분류기에 막힐 수 있다**(`npm ci` 때 실제로 막힘). 우회하지 말고 형께 넘길 것. `bun install` 은 안 막혔다.
- 아투 정상 — 아침 쇼츠 06:43 발행(`j5IaT95PGMI`), 보류큐 비어 있음.
- 이전 스냅샷: [[project_open_threads_2026-08-26_dawn_snapshot]]
