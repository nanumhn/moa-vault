---
name: project_open_threads_2026-08-11_afternoon_snapshot
description: "2026-08-11 14시 오후 세션리셋 스냅샷 — nblog-saas PC에이전트 크래시 대장정 진행중(0.1.5 빌드중), icacls 확인 대기, 발행 재시도 만료 16:35 KST"
metadata:
  node_type: memory
  type: project
  originSessionId: 59f7ac6b-8eb3-4afa-aa9d-e98d370d0373
  modified: 2026-08-11T05:26:03.813Z
---

**세션리셋 시각**: 2026-08-11 14:00 KST 리셋 예정분, 실측 저장 14:25 KST.

## 최우선 — nblog-saas PC 에이전트 크래시/발행 대장정 (형이 실시간 실기테스트 중이었음)

상세 이력은 [[project_open_threads_2026-08-11_dawn_snapshot]] 1번 항목에 시간순으로 전부 기록돼있음(0.1.0 크래시 발견→GPU오진(0.1.1)→샌드박스 진짜원인 발견→0.1.2/0.1.3→발행버튼 팝업방해 발견→0.1.4). 여기는 **오후 세션 마감 시점 최신 상태만** 정리.

**지금 상태(14:25 KST)**:
- v0.1.4가 공식 배포 버전(`AGENT_RELEASE_*` 갱신+push 완료, `/onboarding/agent` 다운로드 버튼이 이걸 줌)
- 형의 발행 테스트: 페어링·네이버로그인·글작성까지 성공, **발행 버튼 클릭이 "도움말" 팝업에 막혀 1차 실패**(FAILED, 재시도 대상). **재시도 만료 16:35 KST** — 이 세션 안에 재시도가 자동으로 돌 예정, 성공여부 다음 세션이 확인해야 함.
- 형의 다른 AI워커("덱스"/Codex)가 추가 진단 2건 제시:
  1. **[반영 완료]** `checkLogin()`의 `page.close()` 무조건호출 버그 — 사용자가 버튼으로 직접 확인할 때만 탭 유지하게 수정(백그라운드 자동폴링은 그대로 조용히 닫힘, 안 그러면 탭이 계속 쌓이고 포커스 뺏김)
  2. **[반례 확보, 근본원인 아님으로 판정됨]** 설치폴더 ACL(`BUILTIN\Users`/`ALL APPLICATION PACKAGES`/`ALL RESTRICTED APPLICATION PACKAGES`에 `(OI)(CI)(RX)`) 누락 이론 — cto 개발PC엔 이 권한이 원래 없는데도 정상 작동 확인(반례). 그래도 예방차원으로 설치스크립트에 SID기반으로 추가는 해둠(이름 아니라 SID로 — 한국어 윈도우에서 이름 매칭은 조용히 실패하는 함정 있었음, 검증됨).
  3. **형에게 확인 요청 보냈으나 응답 대기 중**: `icacls "$env:LOCALAPPDATA\Programs\nblog-agent"` 결과 — 형 PC가 cto 개발PC보다 더 조여진 ACL(상속차단/DENY)이 있었는지 확인해야 ACL이론의 진짜 여부가 가려짐.
- 버튼 흰바탕 가독성 문제: 원인 확정됨(다크모드에서 배경색 미고정 → 흰바탕/흰글씨). 밝은테마 고정+그린(#03c75a) 통일로 수정 완료.
- **0.1.5 빌드 중**(cto), 완료되면 업로드 지시 대기 상태로 둘 것(형 승인 없이 정식배포 안 함, 이번엔 발행검증까지 통과 후 승격 원칙 유지).

**다음 세션이 할 일 (우선순위순)**:
1. 형의 icacls 결과 확인 → cto 개발PC 것과 비교해서 ACL이론 최종 판정
2. 16:35 KST 재시도 만료 전에 실제 발행 성공 여부 확인(로그인/작성은 이미 성공 확인됨, 발행버튼만 문제였음 — 0.1.4의 범용오버레이 방어가 통했는지가 핵심)
3. 0.1.5 빌드 완료 확인 → 형 발행 성공 재확인 후 업로드+공식포인터 갱신+push
4. 코드서명 인증서 구매여부 형 결정(아직 미결) — signed=false라 SmartScreen 경고 계속 뜸
5. UX 백로그 13건 — [[project_open_threads_2026-08-11_dawn_snapshot]] 1번 항목 하단 목록 참고(설치아이콘·버튼색·로그인확인문구·발행이력모달·상단바초록점·로고이동·온보딩뒤로가기·예약설정범위/8시간/사용안함옵션·시트바로가기링크·폴링램프 등). 형이 "왕복테스트 끝난 뒤 착수" 지시함 — 이번 발행 성공 확정되면 착수 시점.
6. 오전 세션 업무일지 이미 작성완료(`70 Record/2026/08/2026-08-11.md`), 오후분 이어서 작성 필요(다음 크론 03:44 KST).

## 기타 열린 항목 (dawn 스냅샷과 동일, 변화 없음)
- ssky.park 3일체험 만료(2026-08-13경) 관찰 대기, 개발용 구글시트 편집자 공유 요청 응답 대기, "개발은 사본시트로" 전환 형 결정 대기.
- 3일 체험 티어 유지 확정, 결제는 무통장입금 수동처리로 확정(사용자 많아지면 자동결제 재검토).
- 랜딩페이지 "14일→3일" 문구 수정+push 완료. "카드 등록 없이 시작" 문구는 형이 "사실이니까" 그대로 두기로 확정.

## 오늘 배운 것 (재발방지)
- **원인 오진 후 정정 사례**: GPU 크래시가 실제로는 샌드박스 실패의 증상이었음 — 첫 관찰(exitcode/증상)을 원인으로 단정하지 말 것. cto가 이번엔 스스로 "지난 커밋 진단이 틀렸다"를 커밋 메시지에 명시하며 정정함 → [[reference_agent_selfcheck_completeness_gap_2026-08-11]]
- **디스코드 ts는 UTC, +9 필수** — 이번 세션에서 실수함(형이 직접 정정) → [[feedback_discord_formatting]]에 반영해둠
- **빌드 성공 알림(exit 0)을 못 믿을 때가 있음** — 0.1.4 첫 빌드가 NSIS 실패로 140KB 깨진 파일이었는데 "완료"로 잘못 보고됨, cto가 파일크기 직접확인으로 잡음. 배포 전 크기+해시 검증을 항상 병행할 것.
- 같은 레포에 여러 cto 에이전트를 병렬로 안 굴리고 순차 위임한 덕에 이번엔 커밋 충돌 없었음(오전엔 실측 충돌 있었음) → [[feedback_parallel_agents_same_repo_git_conflict]]

관련: [[project_open_threads_2026-08-11_dawn_snapshot]] [[project_nblog_saas_account_domain_decision_2026-08-10]]
