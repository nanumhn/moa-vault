---
name: project_open_threads_2026-08-21_afternoon_snapshot
description: "2026-08-21 오후 열린 작업 — 띠별운세 라이브 배포 완료, 가격 $29 반영중, 별카드 교체중, 타로이미지 미배포"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6d0fd7dc-6be8-489e-85ee-07f9adb386dc
  modified: 2026-08-21T07:34:51.621Z
---

2026-08-21 16:20 KST 기준. 형이 결재 3건을 한꺼번에 승인했고 그 실행 중이다.

## 완료 — ① 라이브 배포 (16:10 KST)
PR #13 → main 병합(머지커밋 `f5f4dd7`), Vercel 프로덕션 반영. **라이브 재조회로 확인한 것만**:
- `k-saju.me/ko/zodiac` 허브 + 열두 띠 개별 페이지 **전부 200** (배포 전엔 404였음)
- 절기 표기 라이브 확인 — 호랑이띠에 "입춘 ~ 경칩", **"음력 1월" 0건** (형이 짚은 윤달 건)
- `/ko/daily` 200 + 한국어 title (배포 전 통째로 영어였던 화면)
- `/ko/terms`에 Gumroad 16회 언급
- ko/en 양쪽 주요 경로(home·zodiac·daily·fortune·terms·privacy·compatibility) 전부 200
- 로케일 없는 `/zodiac` → `/ko/zodiac` 리다이렉트 정상

**확인 안 한 것**: 홈 용량 15.7MB→0.4MB 수치. HTML 117KB·0.46초만 쟀고 총 전송량은 안 쟀다. 형에게도 "안 쟀다"고 그대로 말했다.

## 진행중 — ② 가격 $29
형 확정: **단건 $29** (원문 "2. 29로 가자"). 월 구독 $7.99는 **지시 없었음 — 건드리지 말 것.**
cto-seojin이 전수조사→수정→PR 작업 중(머지는 형 결재 대기).
★**형이 직접 해야 하는 것**: Gumroad 상품 가격. 코드에 $29 써도 결제창은 Gumroad 설정값이 뜬다 — 어긋나면 소비자 기만. 서진이 어느 상품 어느 칸인지 특정해서 보고 예정.

## ★내 2차검수가 잡은 것 — 별(17)과 절제(14)가 구도 중복
22장 격자(`shots/star-recheck/tone-22-with-v1.jpg`)를 직접 보다 발견. **둘 다 "그릇에서 액체를 따라 동심원 물결에 떨어뜨리는" 같은 구도**다(색만 다름 — 절제는 흰물, 별은 금물). 스프레드에서 두 장이 같이 뽑히면 손님이 같은 카드로 오인한다.
시우는 **별 한 장만 놓고 톤 적합성만 봐서 못 짚었다.** 개별 검사가 아무리 촘촘해도 **격자로 늘어놓고 보는 층**이 따로 필요하다는 사례. 동물안(사슴)은 그릇이 빠져서 이 중복이 자동 해소된다 — 형 질문("동물 없나")의 답과 중복 해결책이 같은 선택지가 됐다.

## 진행중 — ③ 별 카드
형 승인 원문 "타로 이미지 잘 나왔던데.. 고고" = 교체 승인. media-head-siwoo가 3안으로 교체 + 검사 3종·4구역 재실행 중.
형 추가 질문 — *"인물이나 동물 같은 건 원래 없는 건가? 기술적인 문제인가?"* → 답변함: **반반**. 사람 얼굴은 정책적 금지(인종·나이·성별 고정되면 영어권 이입 안 됨) + 생성모델이 얼굴을 제일 못 그림. **동물은 가능**(띠 그림 12개가 이미 동물). 동물 시안 2~3장 뽑아서 선택지로 드리는 중. 형 답 대기.

## 타로 22장 배선 — 브랜치엔 됨, 라이브엔 아직 (16:26 갱신)
16:20 시점엔 미배선이었고 그래서 서진에게 시켰다. 16:26 커밋 `58a5bba feat(tarot): 메이저 22장 그림을 스프레드에 붙인다`로 `public/tarot/` 22장+썸네일 22장 + `tarot.ts` 배선 + `CardArt.tsx` 신규가 들어갔다.
★**단 브랜치 `feat/tarot-art`에만 있다. 원격 main은 f5f4dd7 그대로고 라이브 `/tarot/*.webp`는 404 — 배포 안 됨.** 시우가 "이제 사이트에 붙었다"고 보고했는데 **붙은 건 브랜치지 사이트가 아니다** — [[feedback_running_screen_is_not_commit_state]]의 그 구분. 이런 표현 오차를 그대로 형에게 옮기지 말 것.
★**배선 방식 함정**: 카드마다 `image`를 적는 게 아니라 `tarot.ts` 340번 줄에서 **slug로 만들어 낸다.** 그래서 `image: "/tarot/"`로 grep하면 **0건이라 안 붙은 것처럼 보인다.** 이걸로 오진하지 말 것.
**별 카드는 이미 새 그림으로 들어갔다** — 시우가 16:19 교체, 커밋이 16:26이라 새 파일이 실렸고 해시 대조로 확인됨(교체본=커밋본=`b066e342…`, 폐기한 옛 평면본=`9f457e38…`). 형이 동물안을 고르면 같은 파일명으로 갈아끼우기만 하면 된다.
※ `/tarot`은 404가 정상 — 그런 주소가 없다. 실제 주소는 **`/spread/four-pillars`**(ko/en 둘 다 200). 이걸 모르고 "타로 죽었다"고 오진하지 말 것.

## 관련
[[project_ksaju_menu_expansion_2026-08-20]] [[reference_ksaju_lichun_regression_scope_2026-08-21]] [[reference_image_gen_glyph_traps_2026-08-21]] [[reference_ksaju_live_verification_method]] [[feedback_report_only_100_percent_done]]
