---
name: project_atz_image_mismatch_after_regen_2026-08-05
description: "아투 블로그 QA재생성시 이미지가 옛 소제목 기준으로 매칭돼 일부 소실 — 원인·수리·검증·커밋 전부 완료(2026-08-05), push는 형 결재 대기"
metadata: 
  node_type: memory
  type: project
  originSessionId: 05eeca17-d953-4fd5-9b1a-332286749eca
  modified: 2026-08-05T00:55:35.723Z
---

2026-08-05, [[reference_atz_report_channel_2026-08-05]]에서 8/4·8/5 AM 슬롯 둘 다 발행 후 재조회 카드에 "본문 이미지 N장(보낸 것 4장)" 경고 발견(8/4=1장만, 8/5=2장만 실제 삽입).

**원인 [확인 — 코드 직접 확인]**: `D:/Develop/moa-studio/tools/atz-pipeline/run.mjs`.
1. 최초 생성 글의 소제목(`<h3>`)을 뽑아 이미지 3~4장을 그 소제목에 맞춰 만든다(219~225번 줄).
2. 이미지 삽입은 `<h3>${si.heading}</h3>` **정확한 문자열 일치**로만 동작(84~90번 줄) — 안 맞으면 조용히 버려짐(에러 없음).
3. QA 게이트가 내용 문제로 막히면 글을 통째로 재생성(`art2 = writeArticle()`, 249~262번 줄)하는데, **이미지는 옛 글 기준 그대로 재사용**된다. 재생성된 글은 소제목이 달라지므로 매칭 실패 → 일부/전부 소실.

재생성이 안 도는 슬롯은 소제목이 원본 그대로라 100% 삽입 성공하는 구조라, 재조회에서 4장 미만이 나온 이상 이 경로 외엔 설명이 안 됨(로그에 재생성 여부가 안 남아있어 직접 확정은 못 했지만 구조상 이것 외 다른 원인 불가).

**수리 완료(2026-08-05, cto-seojin)**: 저장된 페이로드 직접 대조로 실측 확정(8/5=소제목 3개 중 1개만 생존, 8/4=0개 생존/히어로만). 방식은 (b)의 개선판 — ①정확 소제목 일치 먼저 시도 ②실패한 사진만 남은 자리에 문서 순서대로 위치기반 폴백. 재생성 없는 정상 경로는 100% 그대로 동작. `image.mjs`에 `insertSectionImages()` 신설, `run.mjs`가 재생성 시 `♻️ 재생성본 채택 — 삽화 재매칭` 로그+`result.json`에 `regenerated`/`imageInsert` 기록하도록 개선. 삽입 실패시 `⚠️ N장 누락`+누락 소제목 로그.

**덤 발견·동시수리**: `style.mjs`가 발행 직전 `<h3 id="sec-1">`처럼 속성을 붙이는데 `extractHeadings`는 속성 없는 `<h3>`만 인식 — `reimage-post.mjs`(발행된 페이로드 재조회해서 이미지만 재작업하는 도구)가 소제목을 0개로 읽어 히어로만 교체하고 있었음(같은 계열 조용한 버그). 정규식 통일.

**검증**: 신규 회귀테스트(8/4·8/5 실제 소제목 데이터 고정, 25케이스) + 기존 27개 테스트 파일 전부 통과. 실데이터 리플레이로 8/5(1→3장)·8/4(0→3장) 둘 다 누락 0 확인(히어로 포함 4장).

**★커밋 보류 — 중요 위험 발견**: `tools/atz-pipeline/` 워킹트리에 이번 작업과 무관한 **미커밋 변경 약 1,500줄**(curate·discord·generate·image·run 등 9개 파일, 자동발행 기능 자체 포함 추정) 존재 — **현재 운영 중인 코드가 git에 저장 안 된 상태**로 추정됨. 이걸 그대로 두면 디스크 사고 시 파이프라인 전체 유실 위험. 이번 이미지 수정 커밋에 통째로 딸려 들어가게 생겨서 형에게 판단 요청함(①통째로 커밋해서 안전확보 먼저 vs ②내용 정리 후 결정). 기존 미푸시 커밋 5개도 대기 중.

**남은 액션(형 결재 대기)**: 8/4·8/5 이미 발행된 글 2편은 사진 빠진 채 라이브 — `reimage-post.mjs --post=<id>`로 백필 가능(수리로 정상 동작 확인됨), 라이브 수정이라 여쭤봄.

**★커밋 완료(2026-08-05, 형 지시 "커밋은 바로바로 해야지")**: `D:/Develop/moa-studio` 저장소, 브랜치 `feat/youtube-publish-wiring`에 56개 파일 7845줄 커밋(04a8ce7). 잡동사니(`nul` 스트레이 파일, `*.bak.*` 스냅샷, mutation-check 로그 json, `out/`)는 의도적으로 제외 — 새 `.gitignore`가 `out/`을 감사증거만 남기게 바꿔놨어서 그 규칙 그대로 따름. **이 브랜치 자체가 origin에 12커밋 밀려있음(push 안 됨)** — 로컬 디스크 안전은 확보했지만 원격 백업은 아직. push 여부 형에게 재확인 중.

[[reference_owenlab_git_push_gh_credential]] 우회법으로 커밋/푸시 진행(같은 wincredman 문제 있으면 참고).

**How to apply**: "재생성/재시도 경로에서 이전 단계 산출물(이미지·메타 등)을 재사용하는 코드"는 재생성된 본체와 그 산출물이 여전히 서로 맞는지 반드시 의심할 것 — 이번처럼 정확 문자열 매칭이면 특히 취약.

관련: [[reference_atz_report_channel_2026-08-05]] [[project_atz_image_overhaul_2026-07-26]]
