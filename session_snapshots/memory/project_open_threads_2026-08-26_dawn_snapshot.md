---
name: project_open_threads_2026-08-26_dawn_snapshot
description: 2026-08-26 새벽 스냅샷 — 이슈 포럼 체제 가동·승인 카드 근본원인 확정·인스타 08:00 발행 준비 중(워크플로 꺼둔 상태 ★재활성화 필수)
metadata: 
  node_type: memory
  type: project
  originSessionId: 06b01da7-6387-44be-be40-ec14040c1a26
  modified: 2026-08-25T19:26:05.095Z
---

# 2026-08-26 04:30 새벽 스냅샷

★**다음 세션이 제일 먼저 볼 것 — `ksajuCarouselV5` 가 꺼져 있다.** 안 켜면 08:00에 인스타에 아무것도 안 나간다.

## 지금 진행 중인 일 (형 목표: 오전 08:00 발행)

**이슈 포럼 `이슈처리-2026`(1541616859064041502) 에서 덱스 지휘로 돌아간다.**
- **[실전 이슈 001]** 케이사주 Instagram 카드 5장 — 스레드 `1541841218521006181`
- **[실전 이슈 002]** 덱스 승인 카드 미발행·중복 — 스레드 `1541861023848333392`
- 역할(형 확정): **덱스**=지휘·기획·개발(3)·이미지·최종검수 / **클로**=개발 자문·개발(1) / **제나**=개발(2)·이미지
- ★형 지시: **"클로는 덱스의 지시를 따른다, 임의로 활동하지 않는다"** · **"해당 스레드에서는 클로가 승인 대행한다"**
- 스레드 안 질문은 **따로 말 없으면 그 스레드 주제**다(형 지적).

## 인스타 카드 — 확정된 사실

- **8/25 08:00 발행 실패.** 인스타가 `action-card` 이미지를 못 가져감(code 9004, n8n 실행 223 error). 8/23·8/24는 성공.
- **콜드 렌더 실측**: 첫 렌더 2.7~4.9초 / 두 번째 0.02~0.25초 — **약 20배**. 08:00은 날짜가 바뀌어 5장이 다 "처음"인 순간.
- **개발(1) 결과 — 5장 중 3장이 매일 안 바뀐다**
  - 1 후킹: 서버에 **오늘 한 줄(`essence`)이 있는데 안 쓰고** 고정 문구 사용 → **있는 값만 연결하면 됨**
  - 2 오늘의 카드 / 3 키워드: **매일 바뀜(정상)**
  - 4 오늘 할 일: `do=`/`avoid=` 를 **n8n이 안 넘겨** 매일 같은 4줄 → **그날 문구를 만들 자리 신설 필요**
  - 5 안내: 고정(형 지시로 5번만 고정 유지)
- **형 지시: 1~4번은 매일 바뀌어야 한다.** 형 디자인 피드백 = 1번 텍스트 불만·3번 옛 이미지·4·5번 디자인 → 덱스가 재시안 작업 중.
- **수리안(클로 제출)**: ①발송 전 5장 미리 데우기 ②인스타 전달 단계 재시도 ③워밍업 실패 시 중단(반쪽 게시 방지)
- ★**A(발행 되게)/B(내용 매일 바뀌게) 분리 제안** — 덱스 결정 대기. B는 k-saju 서버 수정+배포라 크다.

## 승인 카드 — 근본원인 확정(002)

- **미발행 원인**: 화면 위에 남은 **덱스 글의 번호 목록(3.~8.)** 이 선택지로 함께 집계 → "1부터 연속" 필터에서 **전부 탈락 → 0개 → 카드 미생성**. 실측 9개→0개.
- **수리**: `src/approval.mjs` 의 `parseOptions` 가 **물음줄 아래만 먼저 세고, 0개면 화면 전체**를 센다. 여러 개면 **마지막 물음줄**을 쓴다(위쪽 옛 승인창을 잡으면 엉뚱한 걸 승인하게 됨).
- ★**커밋 안 했다.** 백업 `src/approval.mjs.bak-20260826-0242`. 덱스 검수 후 커밋할 것.
- **중복 발행(별건, 미수리)**: 카드 경로가 둘인데 중복 검사가 **`channel.send()` 완료 뒤에야** 등록된 카드를 본다 → 동시 출발하면 두 장. 오늘만 4회(1~2초 간격, 한 번은 **둘 다 형 멘션**).
  형 확정 규칙: **방이 다르면 방마다 한 장 / 같은 방에 여러 명이면 카드 한 장 + 멘션 여러 개.** ★"보내기 시작할 때 표시" 없이는 같은 사람 두 장이 안 막힌다.

## 하네스 — 오늘 바뀐 것

- **디스코드 플러그인 봇 필터 수정**(`server.ts` 886행) — 덱스·제나만 통과, **봇은 멘션 필수**(gate 286행). ★**세션 재시작해야 적용** — 이 재부팅으로 적용될 것이다. **다음 세션은 덱스 글이 실시간으로 오는지 확인할 것.** 백업 `server.ts.bak-20260826-0050`. ★플러그인 업데이트되면 조용히 지워진다.
- **세션 내 배경 감시(Monitor)** — 워치독 로그의 `ALERT` 줄을 tail 해서 클로가 스스로 깨어난다. **세션 리셋에 사라지니 부트스트랩 ⑧대로 다시 걸 것.**
- **워치독 알림 분리**(형 승인) — **기록 1분(클로 깨우기) / 형께 웹훅 10분.** 상태 표시를 둘로 나눠야 10분 안전망이 산다.
- **포럼 게시 도구** `C:\Users\user\.moa\moa_forum_thread.mjs` — 덱스·제나가 **자기 봇 열쇠로** 글·그림을 올린다(작성자 표기·원장 `forum_post_ledger.jsonl`·이미지 10장/9MB/합20MB).
- 브리지 재시작 3회(16:19 `01bcf9d` → 00:39 `650253f` → 02:29·02:43). 지금 도는 커밋 **`650253f`** + 미커밋 approval.mjs 수리.

## ★클로가 틀린 것 (오늘 밤)

1. **임의로 워치독 기록 방식 변경 → 감시 4분 실명**(01:59~02:03). "실패하면 조용히 넘어가라"로 만들어 **고장이 스스로를 숨겼다.**
2. **1분마다 도는 스크립트를 세우지 않고 편집** → 반쪽 상태로 실행돼 형께 헛알림 4건.
3. **"워크플로 꺼졌다"를 저장값만 보고 단정** — 실행 중 n8n은 그대로였다. **덱스가 CLI 경고 문구로 정정.**
4. **원인 3연속 오답** — 승인 경유 → 늦은 답 → 실제는 **너무 빨리 나온 답**.
5. **반증 시험 통과를 "맞았다"로 단정** — 형이 "어떻게 증명하지?"로 교란변수를 짚었다. → [[feedback_mark_confirmed_vs_guess_with_falsifier]] 규칙 5·6.
6. **역할이 두 번 좁아짐** — "직접 수정하지 않는다" → "덱스 지시를 따른다, 임의로 활동하지 않는다". → [[feedback_clo_reviews_dex_edits_bridge]]
7. **스레드 주제 이탈**(001에서 002 보고), **"방마다 한 장" 오설명**(옛 주석을 그대로 옮김), **MOC 편집 2회 실패**(CRLF·표 오삽입 — 검사기가 잡음).

## 형 결재·결정 대기

1. **★04:00 재부팅을 오늘만 건너뛸지** — 물었으나 답 없음. 04:35 그대로 진행됨.
2. **`ksajuCarouselV5` 재활성화** — 지금 꺼짐.
3. 시험용 스레드 2개 삭제 여부(`1541618763361820827`, `1541796712933499004`)
4. 002 수리 커밋 시점(덱스 검수 후)

관련: [[project_open_threads_2026-08-25_morning_snapshot]] [[reference_discord_bot_messages_never_reach_session_2026-08-25]] [[feedback_clo_reviews_dex_edits_bridge]] [[feedback_running_screen_is_not_commit_state]]
