---
name: project_open_threads_2026-09-09_afternoon_snapshot
description: "2026-09-09 14:00 리셋 직전 스냅샷 — 아투 GPT 작성 복구 완료, 19:30 첫 실전 회차 확인이 다음 세션 몫"
metadata:
  node_type: memory
  type: project
---

## ★다음 세션이 반드시 할 일 — 19:30 / 20:12 결과 확인

오늘 오전에 아투 GPT 작성 실패를 수리했다(커밋 `ea2b2ed`). **19:30 pm 블로그 자동발행이
그 수리가 실제로 도는 첫 회차**다. 이 세션은 14:00 리셋으로 그걸 못 본다.

```text
19:30  아투 pm 블로그 자동발행   ← 수리 후 첫 실전
20:12  아투 pm 쇼츠
```
**판정 방법**: `C:\Users\user\.moa\publish_ledger.jsonl` 에서 오늘 날짜 pm 이 published 인지,
그리고 `atz_pipeline.log` 에서 **작성자가 gpt 인지 qwen 인지**를 본다. qwen 이면 수리가 덜 된 것이다.
성공 판정은 로그가 아니라 **공개 URL 재조회**로 한다.

## 오늘 끝낸 것

| 건 | 결과 |
|---|---|
| 아투 게이트 이중경로 오탐 | 수리·커밋 `45e8120` 푸시. 자기면역이던 회귀시험도 실제 사고 문구로 교체 |
| 보류큐 5건 | 발행 1 · 폐기 4 · 대기열 0 |
| 블로그 pm 발행 | https://www.american-todayz.com/2026/09/blog-post_09.html (HTTP 200 확인) |
| 쇼츠 pm 발행 | https://www.youtube.com/shorts/dGEM-Gp_LBE (공개 재조회 확인) |
| 인스타 카드 | https://www.instagram.com/p/DdC0UlmE-Xr/ (Graph API 재조회 확인) |
| 아투 GPT 작성 복구 | 커밋 `ea2b2ed` 푸시. dry 전구간 통과(작성자 gpt, 59초) |

**결번**: 09-09 am 블로그·쇼츠 둘 다. GPT 실패 → qwen 원고가 발행 불가 수준이라 폐기.

## 넘기는 미해결

1. **`shorts-script.mjs` 미커밋** — 프로젝트 202609 전환을 넣었으나 그 파일에 **남의 미커밋 작업**
   (2026-09-08 언어 가드)이 섞여 있어 커밋 안 했다. 작업트리에는 반영돼 동작한다. 그 사람이 커밋해야 한다.
2. **`gpt-retry.mjs` 미커밋 수정이 규칙을 깼다** — "로그인 만료는 재시도 없이 즉시 폴백" 이 안 먹는다.
   HEAD 판으로 돌리면 `gpt-retry.test.mjs` exit=0, 작업트리판은 exit=1. 오늘 매번 3회 헛재시도한 원인.
3. **`tools/gpt-article` 17개 중 3개, `tools/gpt-image` 7개 중 1개만 git 추적** — 작성기 핵심이 버전관리 밖.
4. **IG 토큰 자동갱신이 2026-08-31부터 계속 실패** — 갱신 스크립트는 자격증명 `igGraphTokenKS1`(IGAA…)을
   보는데 워크플로가 실제로 쓰는 건 `VCULFgF3wJiZOpmu`(EAAY…, 208자). 토큰은 **10-22 만료**라 그 전에 고쳐야 한다.
5. **IG 캐러셀 워크플로가 하루 3번 돈다** — 성공 1·실패 2, 실패 알림 2건이 헛경보로 나간다.
6. **claims-audit 기관명 정책** — 홀로 선 "관세청은 …" 은 검사 대상이 아니다(코드 주석에 명시된 정책 판단).
   넓히면 오탐=발행중단이라 형 결재 사항. 임의로 안 바꿨다.
7. **세션 자살 루프 원인 미확정** — 오늘은 04:29~10:06 무사(관찰기 655줄 전부 claude=1).
   관찰기 `moa_session_death_probe.ps1` 는 6시간 후 자체 종료됨. 재가동 여부 형께 물어둠.

## 오늘 내가 틀린 것 (기록 의무)

- 형 스크린샷에 없는 "형이 직접 여셨다"를 근거로 삼아 보고했다.
- "아투 시험 46개 전부 통과" 오집계 — 실제로는 2개 실패. 요약줄 없는 파일을 통과로 셌다.
- "기관명 사전에 관세청이 없다" — 사전에 있다. 명시된 정책을 결함으로 오독했다.
- 프로세스 종료 조건을 넓게 잡아 내 셸 여러 개를 같이 죽였다(쇼츠·다리 피해는 없었다).
- 검증 실행을 쇼츠와 동시에 돌려 로컬 모델을 물고 쇼츠를 지연시켰다. 위험을 미리 말해놓고 안 끊었다.
- 디버그 크롬에 탭을 여러 번 열어 형 화면에 창이 튀어나오게 했다.

관련: [[project_atz_gpt_project_home_broken_2026-09-09]] · [[project_atz_qwen_fallback_hold_blocked_blog_2026-09-09]] · [[project_atz_gate_dual_path_disparity_2026-09-06]]

---

## 13:47~13:55 세션마감분 추가 (리셋 직전 최종)

- **일지·MOC 완료** — 랩실 커밋 `a5dcfe2` 푸시 완료.
  일지 `70 Record/2026/09/2026-09-09.md`(73줄, 하루 작성) · MOC 3건 갱신(아메리칸 투데이·쇼츠 자동화·하네스 운영).
- **손대지 않은 MOC 3건**: 모아 스튜디오 · k-saju · 네이버 블로그 SaaS.
  신선도 검사에는 걸렸지만 **내가 직접 확인한 근거가 없어** 추측으로 채우지 않았다. 다음 세션이 근거를 갖고 갱신할 것.
- **모아 스튜디오 레포 커밋 2건**(`45e8120`·`ea2b2ed`)은 브랜치 `feat/youtube-publish-wiring` 에 푸시돼 있다. main 아님.
