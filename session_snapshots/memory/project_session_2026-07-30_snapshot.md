---
name: project_session_2026-07-30_snapshot
description: "2026-07-30 세션 저장 — 아투 글쓰기를 GPT로 이관(진행중·로그인 대기), 광고 과다 수정 완료, 검수 게이트 3건 수정"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1abd98e1-7238-40c4-a185-a5b5eecbd0a4
  modified: 2026-07-30T00:26:04.774Z
---

**재개용 스냅샷.** 2026-07-30 오전 (04:00 재부팅 복구 → 형과 장시간 작업).

## 형 지시로 진행 중인 것

### ★① 아투 기사 작성을 qwen → GPT로 이관 (미완 — 형 로그인 대기)
형 확정: **A방식 = ChatGPT 구독을 브라우저로 조종**(추가비용 0). API(B)는 보류.
형 설계: **ChatGPT 프로젝트 폴더**를 만들고 매 실행마다 `제목 + 기사 링크 + 프롬프트`를 넣어 본문을 받는다.

만들어 둔 것 (`D:\Develop\moa-studio\tools\gpt-article\`):
- `cdp.mjs` — 의존성 0 CDP 클라이언트. **왜 필요한가**: 기존 gpt-image는 chrome-devtools MCP로 몰아서 내가 붙어 있을 때만 된다. 아투는 06:00/19:30 **무인** 실행이라 bun이 스스로 크롬에 붙어야 한다.
- `gpt_write.mjs` — `askGpt()`(주문→응답 회수) + `probe()`(로그인·셀렉터 점검). 사람속도 타이핑·전송 전 랜덤정지·rate limit/정책 감지 시 중단.
- `project_instructions.md` — 프로젝트 지침 초안(형이 붙여넣을 문구). 규칙마다 "막으려는 실제 사고"를 표로 남김.
- `C:\Users\user\.moa\moa_debug_chrome_up.ps1` + `atz_scheduled.ps1`에 배선 — 무인 실행 시 디버그 크롬 자동 기동.

**★막힌 지점**: 디버그 프로필(`C:\chrome-debug-profile`)에 **ChatGPT 로그인이 없다**. 형이 일반 크롬에 로그인해서 여러 번 어긋났다. 계정 = `ssky.park@gmail.com`, 구글 SSO 연결이라 SSO로만 로그인 가능.
**로그인되면 남은 순서**: 프로젝트 폴더 생성 → 지침 붙여넣기 → `askGpt` 셀렉터 실측(응답 턴 셀렉터는 아직 미검증) → 시험 1판 → run.mjs 연결(실패 시 qwen 대체).

### ② 광고 과다 — 완료
[[reference_adsense_ad_density_controls]] 참조. 본문 안 광고 4→2개, 페이지 비중 29→22%.
**미해결**: 정적 위젯 3개(사이드바1+푸터2) 숨기기가 테마>맞춤설정>가젯에서 **저장이 반영 안 됨**. 3회 시도 후 중단(엉뚱한 위젯 위험). 본문 아래라 읽기 방해는 아님.

## 오늘 고친 코드 (atz-pipeline, 테스트 162개 통과)

1. **분량 기준 어긋남** — 생성부는 근거 비례(자연분량 780자)인데 검수부만 1000자 고정이라, 정직하게 짧게 쓴 글(845자)이 반려됐다. **게이트를 통과하려면 없는 사실 155자를 만들어야 하는 구조**였다. `qaGate({minBodyLen})` 신설(하한 700·상한 1000), `generateArticle`이 `minLen` 반환, run.mjs 3곳 배선. ★`CONTENT_FIXABLE`의 `'본문 1000자 이상'` 문자열도 정규식으로 바꿔야 자동 재생성이 안 죽는다.
2. **카드 제목 분리**(형 지시) — `📰 [아투 PM 프리뷰]` + `제목: {art.title}` 별도 줄. 비면 `⚠️ 제목 없음`.
3. **빈 검사가 초록으로 찍히던 것**(형 지적) — `✅ 인용 근거 대조 (직접인용 없음)`은 "대조할 게 없었다"인데 통과처럼 보였다. `skipped` 플래그 + `➖` 표기 + 머리줄 "N개 통과 · M개는 검사 대상 없음".

새 테스트: `qa-minlen.test.mjs`(7) · `qa-skipped.test.mjs`(15).

## 오늘 06:00 자동 실행 결과
**발행 안 됨(보류).** 제목 "트럼프, 이란에 강력 보복 예고…국제 유가 급등". 11개 중 2개 실패 = 본문 845자 + **원문에 없는 인용 3건(창작)**. 게이트는 제대로 작동했다. `out/held/2026-07-29T21-11-36-668Z_am.json`. **아직 살려서 발행하지 않았다 — 남은 일.**

## 내가 오늘 틀린 것 (반복 금지)
- **"21시 실행"** — 슬롯은 06:00/19:30 두 개뿐. 없는 시각을 형에게 말했다.
- **"제목이 안 보임"의 원인을 전면광고로 추측** — 실제로는 형이 어제 승인 카드를 보고 계셨다. **형이 무엇을 보고 있는지 묻지 않고 짐작했다.**
- **"확장 패스 첫 줄이 소제목이라 제목이 됐다"** — 그 글에 그 소제목이 없다(라이브 h3 전수 확인). 실제로는 **프롬프트에 적힌 문구를 모델이 복사**한 것으로 보인다(같은 날 지시문 예시 문장이 본문에 복사된 것과 같은 증상).
- **형 말에 맞장구** — "제목만 추가하면 되지?"에 "맞습니다"라고 했는데, 제목은 이미 카드에 있었고 값이 틀린 것이었다.
- **reply chat_id에 message_id를 넣어 발송 실패** 1회.

## 형 대기/결정 항목
- ChatGPT 로그인 (디버그 크롬 창)
- `nanumn.com@gmail.com` 용도 — 유튜브 3채널 계정으로 확인됨. 정리 여부는 형 판단
- "해의 이름"이 블로그(info.nanumn)/유튜브(nanumn.com)로 갈라져 있음 — 통합 여부

관련: [[reference_adsense_ad_density_controls]] [[reference_google_accounts_by_purpose]] [[reference_atz_evidence_never_reached_model]] [[feedback_verified_facts_only]]
