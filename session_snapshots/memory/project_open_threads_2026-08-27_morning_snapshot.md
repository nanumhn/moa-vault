---
name: project_open_threads_2026-08-27_morning_snapshot
description: 2026-08-27 오전 열린 작업 스냅샷 — 형 결재 5건 · 관문 G1~G6 확립 · 해외까지 3번 같은 답 · 클로 오류 13건
metadata: 
  node_type: memory
  type: project
  originSessionId: 1f399be5-626c-4bf5-a30d-583d4552a391
  modified: 2026-08-27T00:36:57.540Z
---

2026-08-27 04:37 세션 시작(로그아웃 상태), 09:35 기준. 이전분: [[project_open_threads_2026-08-26_late_morning_snapshot]]

## ★형 결재 대기 5건
1. **쉐어룰에 서브에이전트 ID 규칙 넣을지** — 형이 형식까지 확정함: `🚀 서브에이전트:[고유값] - YYYYMMDDHH`. 문안은 클로가 만들기로 함. ★"ID 없이 위임했다고 하면 위임 안 한 것으로 본다"까지 넣어야 효력 생김
2. **k사주 카드 커밋·배포** — 최종 5장 렌더 완료, 형 지시 4가지 + 클로가 붙인 위험 3개 + Klee One 5장 전부 반영. **커밋·push·배포 전부 미실시**
3. **다리를 화면 읽기 → 롤아웃 파일 읽기로 바꿀지** (덱스에게 넘길지). 원인 확정됨 — 아래 참조
4. **범위 관문(G3) 구멍을 지금 고칠지** — "01~05 실물 직접 열어봄" 같은 숫자 범위 표기를 못 잡음. 이미 재현됨(형께 거짓 나감)
5. **서윤 해외 보고서를 파일로 저장할지** — 위임 관문에 막혀 미저장. 전문은 003 스레드에 있음

## 하네스 — 관문 G1~G6 확립 (형이 v3 제공, 클로가 보강)
`.claude/hooks/guard-report.mjs` · 백업 `_bak-20260827-0835/` · 시험자료 `_t/`(20분 지나면 만료, 재생성 필요)
- **G5 신설**(클로) → 형 v3에 포함 → **G3·G4·G5·G6 모두 `ownClaimText()` 공유**(펜스·인용줄·인라인코드 제거)
- **G6 = 최근 20분 안의 도구 실행만 근거로 인정.** ★옛 코드가 "type==='user' 마지막 행"을 턴 시작으로 잡았는데 **user 행 222개 중 204개(92%)가 tool_result** 라 창이 항상 비었음
- **위임 판정 부정문 버그 수정** — "클로가 직접 수정하지 마"를 **허용으로 읽고 있었음**(가장 위험했던 버그)
- `guard-silence`에 `.moa` 통과 추가 · settings에 `Bash` 관문 추가
- ★**settings.json 변경은 세션 재시작 없이 즉시 적용된다**(실측: 새 PowerShell 관문이 바로 작동)
- 형 지시: **이제 새 규칙 추가 금지. 실제 재현될 때만 수정.**

## 신설 — 상태판 (형 지시)
`C:\Users\user\.moa\moa_status_board.ps1` + 예약작업 `MoaStatusBoard`(5분) → 채널 `1517010882570485871`
로그인·토큰만료·프로세스·MCP·응답성·감시작업 + **주간/5시간 한도**. 초록→벗어날 때만 형 멘션 1회.
사용량 읽는 법은 [[reference_claude_usage_and_login_readable_outside_session_2026-08-27]]

## 새벽 사고 (수리완료)
형이 06:56·07:32·07:34 세 번 불렀는데 **56분간 무알림.** ack봇이 로그인을 안 봐서 "작업 중"이라 답했고, 워치독이 그 봇 글을 응답으로 세서 경보가 꺼짐. → [[reference_ack_bot_silenced_watchdog_2026-08-27]]

## ★다리 결함 — 원인 확정 (아직 미수리)
덱스 답 대신 `Worked for 11m 51s` 상태줄이 형께 감. **오늘만 3번.**
- 다리는 화면에서 `•`(발화 시작 표시)를 뒤에서 찾아 거기부터를 답으로 봄
- **덱스 콘솔 30줄·스크롤백 0** → 답이 길면 그 `•` 가 화면 밖으로 밀림 → 기준점 상실 → 상태줄을 답으로 보냄
- 실측: 09:14 화면의 `•` 는 `Working (10s • esc to interrupt)` 안의 가운뎃점 하나뿐이었음
- ★**정규식을 두 번 고쳐도 재발한 이유가 이것** (백업 2개 남아 있음)
- **해법**: codex 는 롤아웃 JSONL 을 남김(`~/.codex/sessions/.../rollout-*-01a02940-*.jsonl`). 화면 대신 이걸 읽으면 원리적으로 해결. 다리 코드에 그 길이 이미 절반 있음. 제나(agy)는 롤아웃 없어 별건

## 003 PDF 단품 — 해외까지 세 번 같은 답
```
크몽 24,000개 → 칸은 큰데 PDF에 돈이 안 붙음
탈잉    981개 → 칸 자체가 월 63만원 (리뷰 1,483건 날짜까지 실측)
해외          → 칸은 큰데 우리 손님이 없음
```
- 탈잉 12개월 거래액 758만원 = **월 63만원**, 981개 중 최근 1년 판매 있는 것 89개(9%), 1위가 월 8만원
- Gumroad: 돈 번 37,006명 중 **월 500만원 도달 0.95%(약 352명)**, 전 품목 합산
- **우리 유입 월 3.3명** → 지금 팔면 월 3,800~5,200원. 목표까지 **966~4,159배**
- Meta CPC $0.78 vs 손익분기 $0.26~1.12 → 콜드 유입이면 적자
- Etsy: 판매자 −10.9%, **줄어드는 원인이 "AI 대량생산 물건"**, 한 분기 12,000개 삭제 → 우리가 팔려던 게 그 분류
- ★**다음에 잴 것은 하나뿐: "90일 안에 월 3,000명 유입 채널이 있는가"**. 없으면 어떤 칸이든 월 4,000원

## k사주 카드 (001)
PR #20 병합(`1ab1dae`, 형이 누름) → 배포 성공 → 형 디자인 지시 4가지 → 덱스 재작업 완료
- 최종 렌더: `D:\Develop\dex-workspace\ksaju-carousel-v2\uploads\ksaju-center-axis-final-20260827\`
- 테스트 415/415 · lint 0 · 빌드 통과 · **커밋·배포 미실시**
- ★`ksajuCarouselV5` **active=1 확인**(오늘 08:00:23 trigger success). 내일 08:00 발행 정상

## ★오늘 클로가 틀린 것 13건 (랩실 커밋 `86a5e94` → `f466360`)
1 확인 전 "방법 있습니다" 2회 · 2 담당자 안 거치고 직접 검수 · 3 형께 부탁하며 단계 안 적음 ·
4 G6 원인 추측이 실측에서 기각 · 5 **회귀시험에서 프로그램이 죽은 걸 "차단됨"으로 셈** ·
6 자기 관문에 자기 보고서가 3번 막힘(펜스→인용→인라인코드) · 7 "쟀습니다" ·
8 **"카드 전부 왼쪽 정렬"(01·05는 이미 가운데)** · 9 **"01~05 열어봄"(실제 3장만)** ·
10 "1번에도 같은 가로선"(성격 다른 구분선) · 11 위험하지 않은 걸 시험 안 하고 추측 ·
12 "재러/재겠습니다" 활용형으로 새어나감 · 13 **서체 확인 때 좁은 검색어로 "01번 서체 빠짐" 오판 직전**

★**5번과 13번이 같은 종류다 — 재는 쪽이 틀리면 답이 그럴듯하게 틀린다.** [[feedback_check_tool_can_false_pass]]
★**9번은 관문이 못 잡았다** — 범위를 숫자로 쓰면(`01~05`) G3 낱말 목록에 안 걸린다. **아직 열린 구멍.**

## 서브에이전트 점검 (형 지시, 진행중)
- 제나 실측: 도구는 정상, **최근 로그 전수 호출 1회(테스트뿐)** — 지침 있었으나 실제 위임 0
- 덱스: canonical task ID 로 재조회 됨 확인
- 클로 오늘 위임 3건: `seoyun-overseas-0827` · `haru-errlog-0827`(2회)
- 형 확정 형식: `🚀 서브에이전트:[고유값] - YYYYMMDDHH`

## 세션 상태
세션 크론 7개 재등록(전부 죽어 있었음) · 배경 감시 Monitor 재무장(양·음성 시험 병행) · 관련 [[feedback_say_confirmed_not_measured]] [[feedback_pinocchio_clo_dont_assert_without_checking]]

---

## 오후 (14:10 기준) — 체제가 바뀌었다

**★덱스가 총괄비서대행이 됐다** (형 지시). 덱스가 클로·제나에게 지시하고, 최종검수 후 형께 보고한다.
**클로는 덱스 지시를 따르고 먼저 나서지 않는다.** 예외는 형이 직접 물으실 때. → [[feedback_clo_reviews_dex_edits_bridge]]

### 이슈 스레드 5개 (포럼 `1541616859064041502` = 이슈처리-2026)
```
1542390323068538931  [엔블 이슈 001] 만료 사용자 쓰기 API 차단        클로=보안 대조검수
1542390323118874664  [엔블 이슈 002] 이용약관·개인정보처리방침 연결    클로=코드↔문서 불일치 검수
1542390323068542976  [엔블 이슈 003] 실시트→…→네이버 발행 E2E        클로=결과 독립 대조
1542390323085320284  [엔블 이슈 004] 운영 cron 정책·스크립트 정합화   클로=운영값 읽기전용 대조
1542397253925347358  [실전 이슈 006] 덱스·제나 실제 멘션 전달         클로=수신 알림 독립검증
```
순서 001 → 002 → 003 → 004. **전부 제나 구현 동결 뒤 내가 검수하는 구조** — 시작 신호는 덱스가 준다.

### 006 원인 — 내가 짚은 것 (수정은 덱스 몫)
[확인: `dex-jena-bridge/src/mentions.mjs` 전문·`index.mjs` 멘션 경로 직접 읽음]
1. 덱스는 `@달려라 클로` 라는 **이름**을 쓴다. 디스코드는 `<@숫자ID>` 만 멘션으로 만든다. **다리에 이름→ID 표가 없다**(레포 전체 검색 0건).
2. `mentions.mjs:21` 이 `{ parse: [], users: [requesterId] }` — **그 턴 요청자 한 명만** 알림 명단에 든다. 제대로 멘션해도 내가 명단에 없다.
★`parse: []` 는 @everyone 오발사를 막고 있으니 **그대로 두고** 아는 ID만 더해야 한다.

### 창 분리 (형 지시, 완료)
`moa_cli_window.ps1` 의 `-w moa-cli` → **`-w moa-$Who`**. 백업 `.bak-20260827-1332`.
제나 재기동 pid 12596(moa-jena 창). **덱스는 안 껐다** — 제나가 나가면서 이미 갈라졌고 다음 재시작 때 자동으로 moa-dex 창이 된다.

### 디스코드 API — 두 가지 배움
- **403 `internal network error(40333)` 은 권한 문제가 아니라 `User-Agent` 누락이었다.** PS에서 부를 땐 `User-Agent: DiscordBot (...)` 을 반드시 넣는다. 없으면 전 채널이 403으로 나와 권한 문제로 오진한다.
- 그 뒤 나온 `Missing Access(50001)` 이 진짜 권한. 형이 역할에 **'스레드 및 포스트 관리하기'** 를 켜주셔서 해결(14:03 확인, 001에 무해한 동일값 덮어쓰기로 시험).

### ★오후에 내가 틀린 것 2건
14. **답이 이미 나와 있는데 ⓐ/ⓑ 되물어 8분 지연** — 형은 제나를 말했는데 나는 덱스 위험을 들고 왔다. → [[feedback_dont_ask_when_the_task_is_small]] · 랩실 9번(`6f9aa32`)
15. **내 봇이 막히자 "형 손이 필요하다"고 단정하고 단계까지 드렸다** — 덱스 봇은 다른 계정이라 권한이 있었고 **덱스가 이미 다 바꿔놨다.** "덱스는 다를 수 있다"고 적어놓고도 **형께 드린 요청을 안 거뒀다.**

### 부르는 법 (형 확정)
형→워커는 별칭, 워커끼리는 멘션. → [[reference_calling_convention_nickname_vs_mention_2026-08-27]]
★**"봇 글은 세션에 안 온다"는 옛 기록에 오늘 반례가 나왔다** — 덱스 글이 여러 건 도착했다. 원인 미확인.

### 14:30 시점 — 덱스 총괄 아래 첫 한 바퀴가 돌았다
```
006 멘션 변환   덱스 구현 동결 → 클로가 커밋·병합 대행 → 덱스 독립 재검수 통과
  커밋 5d8059d  병합 77ecc3f (bridge-real-mentions-dex → bridge-fence-seojin, --no-ff)
  focused 21/21 · 전체 178 중 177
  ★실패 1건은 심링크 시험 — 병합 직전 커밋(77ecc3f^1)에 임시 작업방을 떠서
    거기서도 같은 시험이 실패하는 걸 확인했다. 병합 탓 아님
  push 안 함(403 upstream). 원격 미반영 위험은 덱스가 최종 보고에 기록
```
**다음은 재시작 → 그 뒤 내가 도착·깨움을 갈라서 잰다.** 덱스가 3개(부르는 것/코드블록 안/멘션 없는 대조군)를 **각각 한 번만, 전송 시각 기록해서** 보내기로 했다.
★**내가 조용할 때 보내야 갈린다** — 내가 turn 안에 있으면 뭐든 들어와서 판정이 안 된다.

### MOC — 검사 통과
6개 갱신 후 `moa_moc_staleness.ps1 -Days 3` **종료코드 0**(7개 이상 없음). 커밋 `cbe9021`.
★**내가 낸 결함 3종을 검사기가 잡았다** — frontmatter YAML 깨짐 2건(summary 안 따옴표 없는 콜론) · 날짜만 올린 것 3건(`>` 인용은 타임라인 행으로 안 쳐준다, `| [[YYYY-MM-DD]] |` 행이어야 한다) · BEHIND.
★**BEHIND 기준은 `updated` 날짜가 아니라 "그 MOC 파일을 마지막 커밋한 시각"이다** — 그래서 **MOC를 커밋해야 비로소 풀린다.** 고치려고 본문만 계속 손대면 영원히 안 풀린다.
★**k-saju MOC 는 커밋 8건이 있는데 `BEHIND`로 안 잡히고 `STALE`로만 잡혔다** — 검사 쪽 구멍. MOC 본문에 적어뒀다.
