---
name: reference_dex_agents_md_drift_2026-08-21
description: 덱스 AGENTS.md가 2주간 구버전이었음 — 복사 배포는 통지가 없어 조용히 어긋난다
metadata: 
  node_type: memory
  type: reference
  originSessionId: 6d0fd7dc-6be8-489e-85ee-07f9adb386dc
  modified: 2026-08-21T08:18:42.382Z
---

**2026-08-21 하네스 감사 중 발견·수리.** 공통 규칙 원본 `D:\Develop\dex-jena-bridge\templates\AGENTS.md`(2,935B, 8/7 13:13)가 덱스·제나 워크스페이스로 **복사 배포**되는 구조인데, **제나만 최신이고 덱스는 8/6 10:00자 구버전(898B)에 머물러 있었다.** 해시 대조로 확인(`1b226f23`=최신, `aac6039f`=구버전).

**덱스가 2주간 못 받은 섹션 2개:**
- **`## 오디오·영상 파일`** — mp3/wav/mp4를 열려는 시도(ffmpeg·STT 포함)가 응답 시간제한을 넘겨 **턴 전체가 실패**한다. 첨부뿐 아니라 **대화 맥락에 경로만 텍스트로 섞여도** 재현된다(실사례 8/6, 8/7). 그 사고 때문에 만든 규칙인데 정작 덱스가 못 받았다.
- **`## git 커밋 (브리지 대행)`** — 샌드박스가 매 세션 `.git`을 잠가서 덱스의 `git add`/`commit`은 `index.lock` 권한 거부로 **반드시 실패**한다. 대행 문법 `[[커밋: 메시지]] 경로1 경로2`(경로 필수, push 없음, 최대 50개, 와일드카드·워크스페이스 밖 거부)가 이 섹션에 있다. → [[project_dex_git_lock_root_cause_2026-08-06]]의 그 문제를 해결한 규칙이 **덱스에게만 배달되지 않았다.**

**수리**: 백업(`dex-workspace\AGENTS.md.bak-20260821`) 남기고 템플릿으로 덮어씀. 세 파일 해시 일치 확인. 덱스에게 업무채널로 두 규칙을 직접 통지(세션에 옛 규칙이 로드돼 있을 수 있어서).

**Why 이런 일이 나나:** 복사 배포는 **배포 후 원본이 바뀌어도 아무도 통지받지 않는다.** 사람이 기억해서 다시 뿌려야 하는데 안 한다. 그리고 **어긋나도 아무 증상이 없다** — 덱스는 "모르는 규칙"을 어기는 것이라 스스로 이상을 못 느끼고, 우리는 규칙을 줬다고 믿는다.

**How to apply:**
- 규칙·설정을 **복사해서 나눠주는 구조가 있으면 반드시 해시 대조로 정기 점검**하라. 3자 이상이면 하나는 반드시 뒤처진다.
- 새 규칙을 템플릿에 넣었으면 **그 자리에서 전 대상에 뿌리고 해시로 확인**하라. "템플릿에 넣었다"는 배포가 아니다.
- 에이전트가 규칙을 반복해서 어기면 **그 에이전트가 그 규칙을 실제로 받았는지부터** 확인하라. 태도 문제로 오진하기 쉽다.

**하네스 3자 현황(같은 감사에서 확인):** 클로=에이전트 13 + 스킬 11 + settings/MCP/플러그인. 덱스·제나=**AGENTS.md 산문 1장이 전부, 스킬 개념 없음.** `dex-jena-bridge/src/`를 `ENGINE`(codex/agy) 환경변수로 갈라 쓰는 게 유일한 진짜 공유 자산. 셋은 대등한 통합 대상이 아니다.

관련: [[project_dex_git_lock_root_cause_2026-08-06]] [[project_dex_jena_daemon_silent_death_2026-08-06]] [[reference_dex_jena_workdir_scope_2026-08-08]] [[feedback_delegate_to_dex_jena_proactively]]
