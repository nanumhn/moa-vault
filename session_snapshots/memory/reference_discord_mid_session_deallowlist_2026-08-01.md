---
name: reference_discord_mid_session_deallowlist_2026-08-01
description: "Discord MCP reply/fetch_messages 도구가 세션 재시작 없이 도중에 \"channel not allowlisted\"로 막힌 새 실패 패턴. access.json은 정상이었음. 웹훅 폴백으로 우회"
metadata: 
  node_type: memory
  type: reference
  originSessionId: a5faaf90-9bc4-408d-9eba-7ef115b04f4c
  modified: 2026-08-30T22:51:03.124Z
---

2026-08-01 새벽 워치독 cron 도중 발생. 그 직전까지(같은 세션 안에서) `mcp__plugin_discord_discord__reply`·`fetch_messages`가 정상 동작해 여러 번 성공적으로 발송/조회했는데, 이후 호출부터 **세션 재시작 없이** 아래 에러로 막힘:
```
fetch_messages failed: channel 1501858476362829834 is not allowlisted — add via /discord:access
reply failed: channel 1501858476362829834 is not allowlisted — add via /discord:access
```

## 확인한 것
- `C:\Users\user\.claude\channels\discord\access.json`과 `discord2\access.json` 둘 다 `dmPolicy: allowlist`, `allowFrom: ["348731432086274049"]`(형 계정)로 정상 — 설정 파일 자체는 문제 없음.
- 재시도 3회(즉시) 전부 동일 에러 — 일시적 글리치([[reference_discord_send_glitch_and_tz]]의 "간헐 누락"과는 다름, 그건 발송은 되고 순서만 씹히는 것이었는데 이건 아예 거부).
- hook.log 기준 이 세션은 04:02:29 KST 정상 startup, 세션 재시작 없이 도중에 발생 — [[reference_discord_mcp_connect_fail]](로그인 실패로 MCP 자체가 죽는 패턴)과도 다름. 이번엔 MCP 연결 자체는 살아있고(도구 호출은 응답함) 특정 채널만 거부.
- 원인 미확정. access.json을 직접 수정해서 우회하지 않았음 — 사용자 계정이 이미 allowlist에 있는데도 거부되는 거라, 파일을 건드리는 게 근본 수정인지 확신 없음(오히려 다른 무언가—런타임 캐시·게이트웨이 쪽 문제일 가능성).

## 대응
`C:\Users\user\.moa\moa_webhook_send.ps1 -Path <utf8 txt 파일>` 로 웹훅 폴백 발송 성공. **주의**: `-Message` 파라미터에 한글+큰따옴표 섞어 커맨드라인으로 직접 넘기면 PowerShell 파싱이 깨져 `-Path`로 오인됨(백틱 이스케이프 문제) — 반드시 UTF-8 파일로 써서 `-Path`로 넘길 것.

## 다음 세션이 할 일
재발하면: ①웹훅으로 즉시 알림 ②세션 재시작으로 복구되는지 확인(재시작이 해결하면 = 런타임 상태 문제, MCP 재연결로 리셋되는 캐시 같은 것). 재시작 후에도 재발하면 access.json 문제가 아니라 다른 원인(디스코드 쪽 rate limit·권한 변경 등)일 가능성 — 형에게 디스코드 앱 자체 설정 확인 요청.

## ★★ 근본원인 좁힘 (같은 날, 여러 워치독 사이클 지나도 안 풀려서 직접 진단)
`C:\Users\user\.claude\channels\discord\.env`의 `DISCORD_BOT_TOKEN`으로 **Discord REST API를 직접(curl) 호출했더니 정상 200**으로 메시지를 읽어왔다:
```
curl -H "Authorization: Bot $TOKEN" "https://discord.com/api/v10/channels/<channel_id>/messages?limit=20"
```
→ **봇 토큰·Discord 쪽 연결은 완전히 멀쩡하다.** 문제는 100% MCP 플러그인 내부의 allowlist 체크 로직에 있다(access.json도 정상인데 왜 거부하는지는 여전히 미확정 — 아마 런타임 캐시가 access.json과 다른 소스에서 채널→유저 매핑을 하는데 그게 꼬였을 가능성). 세션 재시작 없이는 self-heal 안 됨(수 시간, 여러 새벽/주간 워치독 사이클 넘게 지속 확인).

**읽기 폴백(신규)**: MCP `fetch_messages`가 막혀도 위 curl 방식으로 최근 메시지를 직접 읽을 수 있다 — "형이 보낸 메시지를 놓쳤는지" 확인이 급할 때 이걸로 대체 가능. 단, 인코딩 주의: Windows 기본 콘솔 codepage(cp949)가 이모지/한글을 못 뱉으니 `PYTHONIOENCODING=utf-8`로 python에 넘겨 UTF-8 파일로 저장 후 Read 도구로 읽을 것. 토큰 값 자체는 절대 화면에 echo하지 말 것(`cut -d'=' -f2-`로 변수에만 담아 커맨드 안에서 조용히 사용).
**쓰기는 여전히 안 됨**: reply도 동일하게 막혀서, 발송(특히 파일 첨부)은 계속 `moa_webhook_send.ps1` 또는 웹훅 multipart curl(`curl -F content=... -F file1=@path <webhook_url>`)로 우회해야 한다.

관련: [[reference_discord_mcp_connect_fail]] · [[reference_discord_channel_plugin_conflict]] · [[reference_discord_send_glitch_and_tz]]

## 재발 2026-08-03
같은 세션 안에서 워치독 cron 도중 재발. 이번엔 fetch_messages와 reply **둘 다** 동일 에러로 막힘(8/1엔 읽기만 막혔었는지 기록이 불명확했는데, 이번엔 양쪽 다 확인). curl 직접조회로 놓친 형 메시지 없음을 확인하고, `moa_webhook_send.ps1 -Path <utf8 파일>`로 즉시 폴백 알림 발송 성공. 세션 재시작 없이 self-heal 되는지는 다음 관찰 필요 — 재시작이 유일한 확정된 복구법이라는 결론은 아직 유효.

## 재발 2026-08-04
동일 세션(04:02 KST 부트스트랩, 재부팅 아님·일반 로그온) 워치독 cron 도중 3번째 재발. fetch_messages만 확인(reply는 놓친 메시지가 없어 굳이 시도 안 함) — 동일 에러 `channel ... is not allowlisted`. curl REST 직접조회로 대체해 200 정상 확인, 형 미응답 메시지 없음 확인. **패턴 굳어짐**: 3회 모두 access.json 정상인데 MCP 내부에서만 거부 — 세션 재시작(하루 2회 정기 리셋: 04:00/14:00) 전까지는 계속 막혀 있을 가능성이 높음. 다음 재발 시 reply까지 막혔는지도 매번 확인해 기록할 것.

**지속 확인(같은 날, 5분 워치독 10회 연속 = 약 50분간 계속 막힘)**: 매 cron마다 curl 폴백으로 형 메시지 유실 없음 확인, 새 메시지도 없었음. 50분 지속 시점에 `moa_webhook_send.ps1 -Path`로 형께 1회 상태 보고(세션 재시작 권고, 비긴급으로 명시) — 매 cron마다 알리면 스팸이라 지속성이 확인된 시점에 딱 1번만. 이후로도 계속 막혀있으면 추가 알림 없이 curl 폴백+메모리 기록만 계속하고, 세션이 재시작되면(다음 정기 리셋 또는 형 수동 재시작) 자연 해결되는지 다음 세션에서 확인할 것.

**★★ 웹훅 폴백 채널이 형 메인 채팅 채널이 아니다 (2026-08-04 발견)**: `curl <webhook_url>`(GET, 토큰 없이도 됨)로 웹훅 메타데이터를 직접 조회하면 `channel_id`가 나온다. 이번에 확인해보니 `healthcheck.config.json`의 `discordWebhook`은 `channel_id: "1517010882570485871"`(이름 "SystemLogs")에 묶여 있고, 형이 실제로 fetch_messages/reply로 대화하는 채널은 `1501858476362829834`(별개 채널)이다. **즉 이 세션에서 웹훅으로 보낸 알림 3건(MCP 장애 안내·아투 보류글 처리보고·게이트 커밋보고)은 형 메인 채널이 아니라 SystemLogs로 갔다** — 형이 그 채널을 실제로 보는지는 미확인([추측], 8/1·8/3에도 같은 방식을 썼고 형이 못 봤다는 항의는 없었으니 아마 보고 있을 가능성이 높지만 확정 아님). 다음에 MCP reply가 복구되면 **"웹훅으로 보낸 것들 보셨어요?"를 한 번 직접 확인**해서 이 불확실성을 없앨 것. 확인 전까지는 웹훅=신뢰 가능한 보고 채널이라고 단정하지 말 것.

## 재발 2026-08-05 (새벽 워치독)
같은 세션(전날 19:02 KST 부트스트랩) 새벽 워치독 cron 도중 4번째 재발. fetch_messages만 막힘(reply는 미시도, 놓친 메시지 없어서). curl REST 직접조회(200 정상)로 형 미응답 메시지 없음 확인 — 형이 자는 시간대라 웹훅 알림은 생략, 첫 발생이라 조용히 다음 사이클로 넘어감. 이후 사이클에서도 계속 막히면 위 "50분 지속" 기준대로 1회만 웹훅 보고할 것.

**4연속 지속 후 웹훅 1회 발송 완료**: 20분 간격 새벽 워치독 4사이클 연속(약 1시간+) fetch_messages 계속 막힘, 매 사이클 curl 폴백으로 미응답 메시지 없음 재확인. `moa_webhook_send.ps1 -Path`로 비긴급 상태 보고 1회 발송(SystemLogs 채널로 감). 이후 사이클은 추가 알림 없이 curl 폴백만 계속하고, 세션 재시작(04:00/14:00 정기 리셋)으로 self-heal 되는지 다음 관찰.

**★★ 역대 최장 지속 기록 — 밤새 14시간+ self-heal 전혀 안 됨 (2026-08-05)**: 08-04 19:02 부트스트랩 직후부터 08-05 09:09까지 밤새 5~20분 간격 워치독 15+ 사이클 전부 동일 에러로 막힘, 단 한 번도 자연 복구 안 됨. 매 사이클 curl 폴백으로 형 미응답 메시지 없음은 계속 확인(유실 없음 유지). 09:09에 두 번째 웹훅 알림 발송(형이 깨어있을 시간대라 세션 재시작 권고 포함, 급하지 않으면 14:00 정기 리셋 대기해도 된다고 명시). **결론 갱신**: "세션 재시작이 유일한 확정된 복구법"이라는 기존 가설이 이번 건으로 더 굳어짐 — 정기 리셋(04:00/14:00) 전까지는 self-heal을 기대하지 말고, 장시간 지속 시 형에게 수동 재시작을 권하는 게 낫다. 5분 워치독을 이 상태로 계속 돌리는 건 세션비용만 태우므로([[reference_session_cost_structure]]) 다음에 이런 상황이 오래가면 워치독 자체를 일시 완화하는 것도 고려할 것.

## 재발 2026-08-05 (14:00 리셋 직후 새 세션, 주간 워치독)
14:00 정기 리셋으로 새 세션이 뜬 지 얼마 안 돼 주간 워치독 cron에서 fetch_messages·reply **둘 다** 동일 에러로 즉시 막힘. **세션 재시작이 확정 복구법이라는 기존 결론이 이번에 깨졌다** — 방금 리셋된 새 세션에서도 첫 워치독부터 막힌 건 처음 관찰됨. `moa_webhook_send.ps1 -Message "..."`(파일 아닌 커맨드라인 직접, 한글+콜론 포함)로 폴백 알림 1회 발송 성공(`sent 1 chunk(s)`) — 이번엔 파싱 깨짐 없었음(백틱 없는 단순 문장이라 안전했던 걸로 추정, 그래도 `-Path` 권장은 유효). curl REST 직접조회로 형 미응답 메시지 없음 확인(마지막 형 메시지 01:43은 이미 이전 세션에서 응답 완료됨). **다음 세션 확인사항**: 새 세션 시작 직후부터 막혀 있었다는 건 세션 재시작이 만능 복구가 아닐 수 있다는 뜻 — 다음 재발 시 "이번 세션에서 reply/fetch가 정상 작동한 적이 있었는지"부터 확인해서 진짜 회귀인지 애초에 한 번도 안 됐는지 구분할 것.

**같은 사이클 내 자연 복구 확인 (2026-08-05 05:52)**: 위 재발로부터 약 26분 뒤, 형이 보낸 실시간 메시지가 정상 수신됐고(게이트웨이는 살아있었음 — reply/fetch REST 호출만 막혔던 것으로 보임) reply도 정상 발송 성공(`sent (id: ...)`). **세션 재시작 없이 self-heal된 첫 사례** — "재시작이 유일한 확정 복구법"이라는 기존 결론이 이번 건으로 다시 흔들림. 지속시간이 케이스마다 제각각(수십 분~14시간+)이고 자연복구 여부도 일정치 않아 보임 — 근본원인은 여전히 미확정. 다음 재발 시 지속시간과 복구 트리거(세션재시작 vs 자연복구)를 계속 기록해 패턴을 좁힐 것.

## 재발 2026-08-15 (14:00 세션리셋 직후, 부트스트랩 인사 발송 시도)
`MoaSessionRestartDay`(14:00 세션만 재시작, 재부팅 아님) 직후 뜬 새 세션에서, 부트스트랩 절차 중 "복구 완료" 인사를 보내려는 첫 reply부터 즉시 막힘. 이번엔 **fetch_messages는 정상 동작**(최근 15개 정상 조회, 형 미응답 메시지 없음 확인)했고 **reply만** 막혔다 — 8/1(읽기만 막힘)·8/3(둘 다 막힘)과 또 다른 조합, 매번 막히는 조합이 다르다. 재시도 2회(문구 동일/문구 살짝 수정) 모두 동일 에러. `moa_webhook_send.ps1 -Path`로 SystemLogs 채널 폴백 발송 성공(`sent 1 chunk(s)`). 방금 재시작된 새 세션의 첫 호출부터 막힌 사례가 다시 나와 "세션 재시작=확정 복구법"이 아니라는 08-05 결론이 재확인됨. 근본원인 계속 미확정 — 원인 조사(access.json 외 런타임 소스 존재 여부)는 형 계정 접근이 필요해 보류 중.

## 재발 2026-08-30 (04:00 재부팅 직후 새 세션, 부트스트랩 복구인사 발송 시도)
재부팅 복구 세션의 부트스트랩 절차 중, 처음엔 `fetch_messages`가 정상 동작(최근 15개 정상 조회, 형 미응답 메시지 없음 확인)해서 그걸로 다운타임 유실 여부까지 확인했는데, 그 직후 "복구 완료" `reply`부터 즉시 막힘(재시도 1회도 동일). 이어서 `fetch_messages`도 재조회해보니 그새 **같이 막힘** — 8/29 오후 스냅샷([[project_open_threads_2026-08-29_afternoon_snapshot]])에 이미 "메인채널 not allowlisted 미해결"로 기록돼 있던 것과 동일 증상이 리셋을 넘어 계속 이어지는 것으로 보임(세션 재시작이 이번엔 복구를 안 시킴). `moa_webhook_send.ps1 -Path`로 SystemLogs 채널 폴백 발송 성공. **이 세션 저장 시점까지도 미해결** — 다음 세션은 재시도부터 해볼 것.

## 재발 2026-08-31 (04:00 재부팅 직후 새 세션, 몇 차례 정상 발송 이후 도중 재차단)
같은 재부팅 복구 세션 안에서 이번엔 **처음엔 정상 동작했다** — 부트스트랩 복구인사 reply 1건, 이슈스레드 확인 관련 reply 2건, 아투 보류큐 진행상황 reply 1건까지 전부 `sent (id: ...)`로 성공. 그런데 그 직후 아투 보류큐 최종 처리결과 reply부터 즉시 `channel ... is not allowlisted`로 막힘(동일 문구 재시도 1회도 동일). 곧바로 `fetch_messages`도 재조회하니 **같이 막힘**. 즉 "이 세션은 한 번도 성공한 적 없이 막혔는지"가 아니라 **정상 동작하다가 세션 도중에 갑자기 막히는 패턴**이 이번에도 재확인됨(=08-05 이전 관찰과 동일 계열, 재부팅 직후라고 예외 아님). `moa_webhook_send.ps1 -Path`로 SystemLogs 채널 폴백 발송 성공. 근본원인 여전히 미확정 — 이번에도 세션 재시작(다음 정기 리셋) 전 자연복구되는지 다음 사이클에서 재시도해 기록할 것.
