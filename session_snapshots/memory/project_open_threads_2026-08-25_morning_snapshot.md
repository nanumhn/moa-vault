---
name: project_open_threads_2026-08-25_morning_snapshot
description: 2026-08-25 오전 스냅샷 — 브리지 답 전달 결함 3겹 수리(마지막 1건 재시작 대기) · 봇 글이 세션에 안 닿는 원리 확정 · 워치독 BOM 고장 · 클로 9회 오류
metadata: 
  node_type: memory
  type: project
  originSessionId: 9ddbd072-62d3-4765-8383-8eab34251e4d
  modified: 2026-08-25T05:26:29.064Z
---

# 2026-08-25 오전 스냅샷 (최신)

★ 이 세션의 성과는 **덱스가 검수자로 실제 작동한 것**이다. 덱스가 클로 진단을 3번 정정했고 그게 다 맞았다.

## 형 결재 대기

1. **브리지 채널 관문 수정 허락** — 덱스·제나가 새 포럼 `이슈처리-2026`에서 대화하려면 `.env`의 `CHANNEL_IDS` 판정이 **스레드의 부모 방을 보게** 고쳐야 한다. 지금은 글이 올라온 자리 번호를 그대로 봐서 스레드가 안 걸린다. ★이 설정은 8/23에 잘못 건드려 **형 DM이 통째로 막혔던 자리**라 손 안 댔다.
2. **덱스가 제안한 클로 권한 정지** (8/24 이월, 형 결정 대기)
3. **쇼츠 12분 대기 구조 수리** (이월)
4. **브리지 로컬 전용 커밋 32개** — origin이 남의 레포(`netwaif/codex-discord`)라 push 불가. 우리 계정 미러 저장소 만들지 판단 필요.

## 덱스 대기

- **`01bcf9d`(빈 답 차단) 재검수 → 재시작 지시.** 지금 도는 브리지는 **`855bdc1`** 이라 빈 답 증상이 아직 난다.

## 브리지 수리 — 오늘 한 일 (전부 `bridge-fence-seojin`)

| 커밋 | 무엇 | 검사 |
|---|---|---|
| `c478063` | 서진 변경 3파일(agy 샌드박스 분리·AGENTS.md 공통규칙 조항·index) | — |
| `a0bc038`→`bb3827e` | **답 잘림** — 4.5초 무변화 판정 → 맺음줄 판정, 폴백 12초 | 11/11 |
| `325df91`→`855bdc1` | **답 겹침** — `extractAnswer`가 질문 경계 못 찾으면 화면 전체 반환하던 폴백 제거(덱스 작성) | 17/17, 전체 148중147 |
| `0dd991c`→`01bcf9d` | **빈 답** — 머리글만/상태줄만인 답 차단(덱스 작성) | 20/20, 전체 151중150 |

- 재시작 2회: 덱스 6020→2200→17664 / 제나 25784→26996→12768. **두 번 다 `Stop-ScheduledTask` 후 자식 node 생존** — 따로 끊어야 한다.
- 전체 검사 실패 1건(`첨부 - 심링크로 밖을 가리키면 거부`)은 `a91ecdb`에서도 재현 = 기존 윈도우 심링크 권한 문제.
- **남은 구멍**: 형이 터미널 창에 직접 입력한 뒤 나온 답은 **회수 경로가 없다**(15분 14초짜리 답이 화면에만 남아 있었음). 덱스가 별도 과제로 분리.
- **12초 넘는 정지는 여전히 잘린다** — 일부러 남긴 선.

## 감시 — 신설·수리

- **★원리 확정**: `server.ts` **887행** `if (msg.author.bot) return` 이 `gate()`(892행)보다 먼저 → **봇 글은 클로 세션에 절대 안 온다.** 멘션·허용목록·@everyone 전부 무효. → [[reference_discord_bot_messages_never_reach_session_2026-08-25]]
- **★기존 워치독이 처음부터 반쪽이었다**: BOM 없어 PS5.1이 한글을 CP949로 오독(`실패`→`?ㅽ뙣`) → **에러 낱말 감시 무효**, 알림 문구도 깨짐(ALERT 45건). BOM 부착으로 수리, 옛/새 파일 대조 + 음성 대조로 증명.
- 확장: `-ChannelId/-Label/-StateFile/-AlertThresholdMinutes/-AliasPatterns/-WatchAuthorIds/-ForumGuildId`. 별칭(`달려라 클로`·`클로`)·덱스(`1531914747069599855`)·제나(`1531914807341486184`) 감지. 포럼은 **활성 스레드를 열거해** 훑는다(포럼 방을 그냥 훑으면 0건 — 조용한 실패가 될 뻔했다).
- 신규 예약: `MoaCommandChannelWatchdogExternal`(그들만의대화, 3분) · `MoaIssueForumWatchdogExternal`(이슈처리-2026 포럼, 3분).
- **실제 알림 2회 발송 확인**: 09:55:04 · 10:37:03(둘 다 덱스 글 무응답). 한글 정상.
- ★**이 알림은 형께만 간다. 클로를 못 깨운다**(웹훅도 봇). 클로를 자동으로 깨우려면 **세션 내 배경 감시(Monitor)** 가 따로 필요 — 형이 요구했고 **아직 미구축**. 다음 세션이 만들 것.

## 디스코드 접근

- 포럼 `이슈처리-2026` = `1541616859064041502`(서버 `1283928656363782184`). 형이 터미널에서 `/discord:access group add … --no-mention` 실행 → `access.json` 갱신.
- 클로 첫 스레드 `1541618763361820827` 생성 + 재조회 + 정식 도구 글쓰기 검증 → **부모 방만 넣으면 하위 스레드 전부 열린다.**
- ★`/discord:access`는 **터미널에서 형이 직접 친 요청에만** 반응한다(채팅 경유는 거부). 클로가 대신 못 한다.

## ★클로가 틀린 것 (오늘만 9건)

1. **"DM 차단 원인 확정(메모리맵)"이 틀렸다** — 반례 2개. ⓐ04:25 재부팅 직후 맵이 빈 상태인데 04:29 DM **성공** ⓑ8/24 13:03~13:06 형이 DM 보내던 중인데 **5회 연속 거부**. 실제 실패는 195건이 아니라 **230건(DM 219)**. **원인 현재 미상.** 8/24 스냅샷 메모리에 정정 반영함.
2. 덱스 지시를 절반만 이행하고 **조사 결과를 검수자에게 제출 안 함**.
3. **08:30에 본 워크트리 상태를 09:05에 그대로 보고** — 그사이 덱스가 08:43·08:49에 고쳐놨다. 덱스가 잡음.
4. **덱스 작업을 덮어쓸 뻔** — 도구 가드("읽지 않은 파일은 못 덮는다")가 막았다. 내 판단이 아니었다.
5. **"9/9 통과"를 내 공으로 적음** — 실제로는 덱스가 고친 코드를 잰 값.
6. **"워치독 걸면 형 손이 안 갑니다"는 과장** — 웹훅도 봇이라 887행에 막힌다.
7. **"스스로 답 회수 경로가 없다"는 범위 과대** — 덱스 정정: 디스코드 요청은 회수한다, 터미널 직접입력만 아니다.
8. **"자동 확인 안 되니"에 "형이 불러주셔야 한다"고 답함** — 도구를 다시 뒤지니 가능했다. 안 찾아본 것.
9. **채널 이름 오기** — `1531912848433741825`는 **`그들만의대화`** 인데 오전 내내 "업무보고방"이라 부름. 하루가 자산 목록 쓰다 발견.

부수: 내가 쓴 MOC frontmatter의 `blocked` 값에 따옴표 없는 콜론이 있어 **YAML이 깨졌다**(검사기가 잡음).

## 기록

- 랩실 커밋 `4e9eaa7` 푸시 완료. MOC 검사기 종료코드 **0**.
- 하네스 변경 원장에 워치독 건 1행.
- 신규 메모리: [[reference_discord_bot_messages_never_reach_session_2026-08-25]]

관련: [[project_open_threads_2026-08-25_dawn_snapshot]] [[feedback_pinocchio_clo_dont_assert_without_checking]] [[reference_ps51_cp949_breaks_korean_files_2026-08-24]] [[reference_dex_jena_bridge_silent_loss_2026-08-24]] [[feedback_verify_measurement_before_declaring_failure]]
