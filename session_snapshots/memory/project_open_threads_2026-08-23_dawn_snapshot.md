---
name: project_open_threads_2026-08-23_dawn_snapshot
description: "2026-08-23 04:00 재부팅 직전 스냅샷 — 캐러셀 08:00 첫 실전 대기, 타로 22장 완료(2장 미확인), 형 결재 4건 모두 소진"
metadata: 
  node_type: memory
  type: project
  originSessionId: 89a3f818-94be-41ca-9b73-e70966618f5f
  modified: 2026-08-22T19:25:44.064Z
---

2026-08-23 04:20 재부팅 직전. 세션 = 08-22 14:26 ~ 08-23 04:20(형이 자정 넘게 함께함).

## ★오늘 아침 반드시 확인할 것 3개 (이게 1순위다)
1. **08:00 인스타 캐러셀 첫 실전** — `ksajuCarouselV5` ON / v4 OFF. dry-run 에서 자식 5 + 부모 컨테이너 생성까지 확인했고 **`media_publish` 만 미검증**. 안 올라갔으면 즉시 형에게 보고. 실패 알림은 `igAlert0001` 이 담당.
2. **06:00 아투 글** — 삽화 수리(슬롯 밀림·지오 문턱)가 반영되는 **첫 회차**. 사진이 달라졌는지 눈으로 확인. ★어제 형에게 "저녁 발행분에서 확인 가능"이라 했다가 42분 차이로 틀렸다.
3. **타로 미확인 2장** — `정의`(생성됐으나 스크롤 밀려 내가 못 봄) · `운명의 수레`(아예 안 뽑혔을 가능성). [[project_tarot_22_hanji_regen_2026-08-22]]

## 어젯밤 끝낸 것
- **MOC 낡음 검사기 근거 대조로 교체** — 4검사 전부 깨뜨려 검증. 오늘 04:13 첫 자동 실행에서 **실제로 k-saju 를 잡아냈다**(옛 방식이면 통과). [[reference_moc_staleness_evidence_contract_2026-08-22]]
- **아투 삽화 수리 2건 배포** — 슬롯 밀림(`문단 무대 미반영` 반려 6→0) + 지오 2등 문턱 0.3(12건 실측으로 정함). 커밋 `38ce078`·`82224a3`
- **캐러셀 5장 배포·전환** — PR #17(`595c544`), 5장 라이브 200/jpeg 확인. [[project_ig_carousel_v5_pending_switch_2026-08-22]]
- **타로 22장 재생성** — 20장 육안 확인, 화풍 확정(조선 관복·해태)
- **저녁 쇼츠 유실 회수** — 20:17 실패 → 23:03 공개(`Rbxo8JV2xII`). **1차 실패 원인 미확정**, 재발 시 로그 보강 필요

## ★형 결재 4건 — 어제 전부 소진됐다 (오래 정체하던 것)
①타로 22장 재생성 "진행" ②사람 그려도 됨 "오케이" ③짐승 얼굴 완화 "오케이" ④데일리카드 = 캐러셀 5장으로 "배포해"
**새로 생긴 대기 1건**: 달 카드만 배경이 밤 — 22장 나란히 두면 튄다. 그대로 둘지 재생성할지.

## 형이 준 권한 (새로 열림)
`Bash(git push:*)` · `Bash(docker exec:*)` 를 Allow 에 추가해줬다. **콜론 없는 `Bash(git*)` 형식은 작동하지 않는다**(목록에 있었는데도 막혔던 이유).

## ★어제 형이 한 말 — 잊지 말 것
> *"뭘 믿어야 할지 너무 혼란스럽네.."*

하루에 원인·숫자를 세 번 뒤집어서 나온 말이다. 정정한 게 문제가 아니라 **재보지도 않고 확신해서 말한 게** 문제다. 특히 **시간을 감으로 세서 "한 장에 40~50분"이라고 보고했는데 실제 평균 11분**이었다 — 백그라운드 대기는 나를 안 막는데 기다린 줄 알았다. **시각이 필요하면 반드시 시계를 찍어라**(PowerShell `Get-Date` 또는 페이지 `new Date()`). [[reference_bash_date_clock_offset_2026-08-16]]

## 그 외 열린 것
- 아투가 **곁가지 브랜치 `feat/youtube-publish-wiring`** 에서 돌고 있다. 최근 수리 5개가 본류에 없다 — 본류로 되돌리면 통째로 사라진다. 머지 여부 형 판단
- PR #15(타로 배선)·#16 동결 중 — 타로 22장이 나왔으니 #15 는 이제 풀 수 있다(파일만 교체, 코드 0줄)
- 8/15 유출 운영 DB 비밀번호 **로테이션 실행 여부 미확인**(스크립트 `7e3e149` 는 커밋돼 있음)
- 아투 `promptSource` 기록이 실제 발행 기록에 찍히는지 **코드 흐름만 확인, 실물 미확인**
- 덱스: nblog 안정화 1~2번 작업 중(20:24 `media-store.ts` 수정 확인). **시작·완료 한 줄 보고**를 요청해 뒀고 최종 검수는 내 몫
