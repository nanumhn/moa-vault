---
name: project_open_threads_2026-08-24_afternoon_snapshot
description: 2026-08-24 오후 열린 작업 스냅샷 — 규칙 3층 재편·덱스 인코딩 수리·브리지 마커 사고, 형 결재 대기 4건
metadata:
  type: project
---

**2026-08-24 14:25 KST 기준.** 이전분: [[project_open_threads_2026-08-24_morning_snapshot]]

## ★형 결재 대기 (4건)

1. **브리지 재시작** — 마커 오실행 수리(커밋 `a91ecdb`, 브랜치 `bridge-fence-seojin`, push 안 함)가 **디스크에만 있고 라이브 미반영**. 재시작 대상은 **2개**(제나용 pid 13888 / 덱스용 pid 5284, 둘 다 옛 코드). ★**재시작하면 형 미승인 중계기 수리 3건도 같이 켜진다**(승인카드 채널 라우팅·채널별 카드·중복 방지) — 같은 프로세스라 분리 불가. **안 하면 "지침 보여줘" 할 때마다 계속 터진다.**
2. **덱스를 새 대화로 띄울지** — "11:25에 왜 못 읽었다고 말 안 했나"를 규명하려면 필요. `resume` 으로는 원리적으로 못 잰다(그 창이 이미 학습했다). 대신 **오늘 형이 덱스와 쌓은 맥락이 사라진다.**
3. **B1~B3(대화·첨부·커밋) 자리** — 덱스·제나 **둘 다** 해당이라 3층에 담을 자리가 없다. 클로 추천 ①쉐어 룰에 두되 "덱스·제나만 해당" 명시(sync 스크립트가 `SECTION-B` 를 못 찾으면 `throw` 로 죽는다) / ②`BRIDGE_RULES.md` 신설 / ③두 지침에 복사(8/21 드리프트 재현 위험).
4. **k-saju 게스트 체크아웃 배포** — `guest-checkout-seojin` `35dc77f` 미배포. + Gumroad 대시보드 리디렉트(ⓑ) + 기존 2건 구매자 이메일 확인(1분).

## 오늘 오전에 끝난 것

- **규칙 3층 재편**(형 지시 11:33): 공통=`SHARED_RULES.md` / 작업=`WORK_ORDER_CHECKLIST.md` / 개별=각 워커 지침. 11:17에 각 지침의 규칙 **복사본 제거**(백업 `moa-vault\00_Common\_backup_20260824\`), `MoaSharedRulesWatch` 11:24 Disabled. 머리말을 현재 구조에 맞게 재작성 + `SECTION-A:END` 를 A11 뒤로 이동(그전엔 A10·A11이 배포 구간 **밖**이라 "어긋남 0"이 두 규칙을 안 센 값이었다).
- **덱스 한글 깨짐 확진·수리** → [[reference_ps51_cp949_breaks_korean_files_2026-08-24]]
- **지침 주입 시점 발견** → [[reference_agents_md_only_injected_at_session_start_2026-08-24]]
- **무응답 훅이 서브에이전트를 오인하던 버그 수리**(`.claude\hooks\guard-silence-and-delegation.mjs`). 원인: `mcp__plugin_discord_discord__reply` 라는 **글자**가 CLAUDE.md에 있어 모든 서브에이전트가 "형 대화 세션"으로 판정. 형이 A11을 넣은 날이라 **지킬수록 방해받는 구조**였다. 부수 발견: 채널 입력 판정이 **한 번도 참이 된 적 없었다**(transcript는 따옴표를 이스케이프해 저장하는데 생 따옴표로 찾고 있었다).
- **검수 판정(서지안)**: 덱스·제나 둘 다 규칙 파일을 **실제로 열어 읽고** 정확히 답함(제나는 행 번호까지 맞힘 = 복사본엔 없는 정보). **조건부 통과.**

## 미해결·미확인

- **11:25 미해명** — 덱스가 왜 "못 읽었다"고 말 안 했나. 대조군에서 덱스는 **새 규칙 없이도** 깨짐을 보고했다 → 원인은 "규칙이 없어서"가 **아니다.** 진짜 원인 모름.
- **BOM 범위** — 창고 `.md` 638개 중 BOM 있는 건 **2개뿐**. `.moa` `.ps1` 45개 중 한글 포함 27개, **BOM 없음 7개**(`moa_cli_window.ps1` 포함) — **잠재 위험, 실제 오작동은 안 쟀다.**
- **브리지 커밋 기준 폴더 버그** — `index.mjs:181` 이 `WORKDIR`(`D:\Develop`)을 본다. 첨부는 창 폴더로 고쳤는데 커밋만 옛 기준. ★**고쳐도 커밋 안 된다** — 두 워크스페이스에 `.git` 이 없다.
- **브리지 동시 질문 유실** — 창 하나에 두 사람이 물으면 한쪽 답만 나가고 다른 쪽은 조용히 사라진다. 형 11:26 질문이 실제로 유실됐다.
- ★**무응답 훅 수리가 반쪽이다** — `isSidechain` 예외가 **팀 에이전트에는 안 걸린다**. 하루가 일지를 쓰는 동안 **10회 이상** 차단당했다(2026-08-24 14시경, 하루 보고). 내가 오전에 "고쳤다"고 형께 보고한 것은 **Agent 도구로 띄운 서브에이전트 기준**이었고, 팀 에이전트는 안 쟀다. **원인 미확인** — `isSidechain` 표식이 안 붙는지, `transcript_path` 가 메인 세션을 가리켜 `sawChannelInput` 이 참이 되는지 둘 다 가능. **다음 세션에서 실측할 것.**
- **4층 가능성** — `.claude\agents\*.md` 15개와 스킬 문서에도 규칙 문구가 있다. 아직 안 열어봤다.
- 브리지 레포 미추적 파일 **11개**(`templates/AGENTS.md.bak-shared-*`). 형 결재분과 같은 폴더라 안 건드림.

## ★내가 오늘 틀린 것 (7건, 상세는 랩실 `99 피노키오 클로/2026/08/2026-08-24.md`)

형: *"실망이 너무 커구나."* / *"담당자에게 배정 안하니? 직접하는거야?"*

1. **첨부 실패를 "제나 잘못"으로 오진** — 에러 한 줄과 그 에러 내는 코드 한 줄만 보고 단정. **"저 값이 어디서 왔는가"를 안 따라갔다.** 그 오진으로 **브리지 진짜 결함이 3시간 방치**됐다.
2. 파일 크기 **두 번** 틀림(15,753→실제 15,930 / 4,426→실제 4,487). 파이썬 텍스트 모드가 CRLF를 접는다. **크기는 디스크 실제 바이트로 재라.**
3. 창 기동 시각 틀림(12:06→실제 11:59:48). **시각은 `Get-Date`.**
4. 직원과 약속한 **순서를 어김** — "측정 끝나면 붙이겠다"고 해놓고 신호 없이 붙였다. 1분 차이로 그 측정이 무효가 될 뻔했다.
5. "파일 **1개** 고치겠다"고 하고 2개 고침.
6. **직원 배정 때 맥락을 안 줘서 오진 유발** — "내가 규칙 블록을 제거했다"를 안 알려줘 담당자가 "배포가 한 번도 안 돌았다"로 결론냈다.
7. **A11 위반** — 규칙이 생긴 날 본인이 직접 작업.

관련: [[feedback_pinocchio_clo_dont_assert_without_checking]] · [[feedback_ask_before_touching_what_hyung_uses]] · [[reference_dex_jena_bridge_silent_loss_2026-08-24]]
