# 세션 스냅샷 2026-08-06 (04:00 재부팅 전)

04:25 KST 저장. 대상 세션은 2026-08-05 14:26 KST 부트스트랩~2026-08-06 04:00 재부팅 직전.

## 이번 세션 성과

### 1. 수신 워치독 하트비트 도입 → 형 지시로 즉시 중단, 외부 스크립트로 대체
형 지시로 "조용히 종료" 워치독 사이클에도 시각+토큰 하트비트 reply를 매번 보내도록 바꿨는데, 5분 간격으로 거의 동일한 메시지가 채널에 쌓이는 걸 형이 스크린샷으로 보고 "워치독은 문제가 있는것 같아 / 토큰 소진이 너무 심하네 / 중단하자"라고 지시. 세션 크론 2개(주간·새벽) 즉시 삭제, `session_bootstrap.md`에 재등록 금지 반영. 삭제 후 8분간 자동 발화 0건으로 검증 완료.

대신 세션 밖에서 도는 `MoaDiscordWatchdogExternal`(윈도우 작업 스케줄러, 5분 간격, 순수 REST 폴링) 신설 — 형 메시지 10분+ 무응답이면 웹훅 알림만(직접 응답은 못함). 실측 비교: 세션 워치독 조용한 사이클 1회 = 약 20~250만+ 토큰(세션 길어질수록 증가) vs 외부 스크립트 = 0토큰.

**만드는 과정 함정 2개**: ①PowerShell 기본 User-Agent를 Discord/Cloudflare가 403(`internal network error`, code 40333)으로 거부 — curl류 UA로 위장해야 통과 ②PS5.1이 BOM 없는 UTF-8 `.ps1`을 cp949로 오인해 한글이 깨짐 — UTF-8 BOM 추가로 해결.

### 2. 토큰 비효율 2건 — 형이 직접 잡아냄
- "890만이었는데 갑자기 3237만이야, 맞는거니?" → 세션 로그 분석해서 답변: 무거운 멀티스텝 작업(59단계) 하나가 1281만 토큰을 혼자 먹은 게 원인(버그 아니었음)
- "체크" 한 단어 메시지 처리에 301만 토큰 — 원인은 그 한 마디 처리에 도구 호출을 10번(시간확인·토큰계산·파일읽기·답장을 따로따로)이나 나눠 부른 것. **클로의 비효율이었고 형이 정확히 원인을 짚음** → 앞으로 간단한 확인은 도구 호출 묶어서 처리하기로 함

### 3. 프롬프트 캐싱 관련 미해결 질문
형이 Anthropic 공식 캐싱 문서 공유. 지금까지 보고한 "토큰 수"는 캐시 할인(read=1/10) 반영 안 된 순수 개수라는 게 확인됨 — 세션/주간 사용한도가 순수 개수 기준인지 할인반영 비용 기준인지는 **여전히 미확인**, `/usage` 화면 요청함(형 응답 대기 중).

### 4. Discord mid-session de-allowlist 재발 — 이번엔 자연 복구
reply·fetch가 세션 도중 "not allowlisted" 에러로 막힘(8/1·8/3·8/4에도 있었던 기존 이슈). 이번엔 **세션 재시작 없이 26분 만에 자연 복구** — 기존 "재시작만이 확정 복구법"이라는 결론이 이번 건으로 깨짐.

### 5. 클로 실수 — reply 도구 미사용 재발 (2회)
형이 물은 질문에 대한 답을 텍스트로만 쓰고 진짜 reply 도구 호출을 안 해서 형한테 전달이 안 됨. "???" → "답변이 안 왔어" 지적받고서야 알아채고 재발송. **오늘만 두 번**(누적 12번째 기록). 재발 패턴을 좁힘: 조사성 도구(Bash/PowerShell/Notion 검색)를 마지막으로 부른 직후에 발생 — "reply가 그 턴의 마지막 도구 호출인지" 자체점검 필요.

### 6. 아투 보류큐 확인
`out/held/` 폴더 비어있음 — 처리할 것 없음.

## 다음 세션 확인사항
- `/usage` 화면 재요청
- `MoaDiscordWatchdogExternal` 상태(`Get-ScheduledTaskInfo`)·로그(`discord_watchdog_external.log`)에 실제 ALERT 발생했는지 — 있으면 진짜 게이트웨이 놓침 사례라 세션 워치독 재설계 논의 필요
