# 2026-09-20 오후·야간 세션 (14:18 리셋 복구 ~ 04:00 재부팅)

## 결과
- **발행 4건 전부 성공(결번 0)** — 블로그 am `cnnms-now_0742777893.html` / pm `blog-post_20.html`(200·204,875B), 쇼츠 am `U5fMJ5CLN4Y` / pm `Yp2hpBv8a4Q`(원장 20:12 + 유튜브 200 + `isPrivate:false`·`isUnlisted:false`).
- pm 쇼츠는 **폴백 3겹**(작성자 GPT→qwen `no composer` / 메타 LM→규칙 / 스톡 6개 전부 인물반려→Flux)을 쓰고도 성공. 렌더 시도 1/3(362초), 크롬 예열 4,067ms/한도 25,000ms. 09-15 pm 의 'qwen 폴백 대본 0건' 재발 안 함.
- 대본 환각 대조 **없음**(원문 `Brian Leeland, a 79-year-old Democratic voter in Detroit` 와 일치).
- **보류큐 신선도 검사가 첫 발화에서 묵은 글 2건 차단**(159.6h·74.1h). 없었으면 6.6일 묵은 기사를 공개할 뻔했다.
- MOC 3개 갱신 커밋 `b890e32`, 오후·야간 일지 커밋 `be5ccbf` — 둘 다 푸시 확인. MOC 검사기 EXITCODE=0.

## 새로 확정한 것
- **아투 고유관점 게이트는 고장이 아니라 계측 전용 설계.** `run.mjs` 546행에서 D(매체 귀속)만 차단, A·B·C는 경고. 승격 조건 "3회 연속 100%"가 **108편 중 통과 0건**이라 영구 유예. 09-20 아침 오진을 오후에 정정.
- **공통수칙 동기화가 08-30부터 DRIFT, 감시기는 경보 낸 채 꺼져 있다.** `-Check` 재실행 EXITCODE=2, 4파일. 단 네 파일 모두 SSOT 를 가리키므로 "워커가 규칙 없이 돌았다"는 근거는 없다. 별건으로 v202609009 패치가 09-06부터 미적용(머리말은 v202608009 가 맞다).
- **결재 범위 오류**: `REQ-20260916-ATZ-01` 이 집은 건 09-13 파일 하나뿐. 09-17 건은 결재 없이 떠 있다.

## 내가 틀린 것 (8건, 전부 "정상인데 고장" 방향 — 8번만 반대)
1. `pgrep` 으로 윈도우 프로세스를 세서 "발행 멈춤" 오경보(실제 pid 35532 생존)
2. 시각을 재지 않고 "20:10 알람 미발화"라고 **틀린 보고를 올렸다**(실제 20:04). 다음 메시지로 정정
3. 영문 원문에 한글 고유명사를 대조해 "환각 5건" 낼 뻔(`Leland` vs 원문 `Leeland`)
4. 분량 기준을 고정 1,600자로 단정(실제 1600·1510·1513 가변, 753자 통과일도 있음)
5. 포럼 503 밴드를 "2~8건"이라 말함(실측 3~13건)
6. 로컬 검색 범위를 창고 전체+`.git` 로 잡아 120초 초과
7. MOC `updated` 를 09-21 로 올려 UNBACKED 유발(타임라인은 09-20)
8. **푸시 실패를 성공으로 읽을 뻔** — `push | tail` 뒤 `$?` 는 tail 의 코드

★반대로 **로그 6분 침묵 2회를 멈춤으로 읽지 않고 프로세스 CPU 로 확인**한 것은 잘 지켰다.

## 막힌 것 / 결재 대기
- 형 대화방 `not allowlisted` 19:54~ (오늘 2번째, 통산 4번째). 재시작으로만 풀린 전례.
- 덱스 CLI 04:14:34~ Codex 프롬프트에서 멈춤(18h+). 브리지가 봇 글을 무시해 클로가 못 푼다.
- 결재 6건 전부 무응답: DEX-01 / ATZ-01(대상 1건뿐) / BOOT-01 / 분량게이트 / 고유관점 승격조건 / 공통수칙 DRIFT. 별건 github-blog-pat 재발급.

## ★다음 세션이 리셋 직후 할 일
부트스트랩 ⑤-B 를 그대로 걸지 말 것 — **보류큐 신선도 검사(`heldAt` 24h 초과 = 공개 금지)를 손으로 다시 넣어야 한다.** `REQ-20260920-BOOT-01` 승인 전까지 매 리셋 수작업.
