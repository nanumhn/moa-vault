# meeting.py 산출물 단계는 아직도 언어 게이트가 없다 — 3번째 재발

날짜: 2026-08-08 · 발견: 회의 `run_20260808_105736` (팀 `naver_blog_saas`) 품질 게이트

## 무슨 일이
산출물 5종 중 `user_flow_and_dashboard.md`(이도연)가 **중국어 724자 + `<|im_start|>` 특수토큰 누출**로 붕괴한 채 저장됐다.
표 첫 줄부터 `| 단계 | 사용자가 하는 일 | 화면 | 시스템이做的你认为呢？` 로 무너지고 이후 전체가 중국어 표로 바뀌었다.

## 왜 또 났나 — 수리가 절반만 됐다
- 2026-08-06 노트에서 "산출물 단계에 언어 게이트 없음"을 백로그로 남김
- 2026-08-07 노트대로 **`regen_artifact.py`에는 언어 게이트가 정식 편입됐다** (이번에 `[lang-gate] 1121 chars, foreign=0` 로그로 실제 동작 확인)
- **그런데 `meeting.py`의 산출물 생성 루프에는 여전히 없다.**
  - `check_language()` 호출 지점은 `meeting.py:529` — `speak()` 안 **한 곳뿐**
  - 산출물 생성은 `meeting.py:669` `call_lm(msgs, max_tokens=4000)` → 검사 없이 `out_file.write_text()`
- 즉 **1차 생성은 무방비, 보강 단계만 방어된다.** 오염을 만든 곳은 안 막고 치우는 곳만 막았다.

### 근본 수리 (백로그 → 승격 권고)
`meeting.py` 산출물 루프에 `regen_artifact.py`와 동일한 언어 게이트 + 재시도를 넣는다.
`<|im_start|>` / `<|im_end|>` 채팅 특수토큰 누출도 검사 항목에 추가할 것 — 기존 정규식은 CJK만 봐서 이건 못 잡는다.

## 이번 게이트 실적
| 산출물 | 1차 | 위반 | 최종 | 보강 |
|---|---|---|---|---|
| tech_decision_and_architecture.md | 2291자 | **내용 역전** | 2953자 | 1회 |
| user_flow_and_dashboard.md | 1407자 | 중국어 724자·특수토큰 | 1739자 | 1회 |
| pricing_and_mvp_scope.md | 1102자 | 분량 | 1670자 | 2회 |
| product_definition.md | 938자 | 분량 | 1089자 | 2회(미달 잔존) |
| risk_and_roadmap.md | 1900자 | — | 1900자 | 0 |

→ **5종 중 4종이 1차 실패.** 7B 기본 실패율이 높다는 08-06 관찰과 일치.

## ★기계 게이트가 못 잡은 것 — 규칙 역전 재발 (4번째)
`tech_decision_and_architecture.md` 1차본이 **회의 최대 의사결정 표의 안정성·차단위험 칸을 정반대로** 적었다.

> 안 A(브라우저 자동화) = "안정성 높음, 차단 위험이 없고 안정적", 안 B(공식 API) = "안정성 낮음, 차단 위험 존재"

briefing에 안 A의 함정을 5개나 구체적으로 박아뒀는데도 **정확히 뒤집어 썼다.** 게다가 같은 문서 안에서 "안 A는 탐지·캡차로 실패했다"고 자기 모순을 쓴다.
- 5종 기계 게이트(thinking·누락·분량·yaml·utf8)는 **전원 통과**시켰다. 글자 수도 충분하고 표도 다 찼기 때문.
- 이는 `reference_qa_gate_rule_inversion_blindspot_2026-08-07`과 동일 유형. **키워드·분량 검사로는 원리적으로 못 잡는다.**
- 이번엔 COO가 육안으로 잡아 "직전 초안은 정반대다, 사실은 이렇다"를 지시문에 명시해 재생성 → 교정 성공.

### 룰
**briefing에 사실을 넣는 것만으로는 부족하다.** 결론이 뒤집히면 치명적인 표는
① 지시문에 정답 방향을 미리 못 박거나
② 생성 후 COO가 방향성을 육안 대조한다.
분량·형식만 보는 게이트를 통과했다고 결재로 올리면 안 된다.

## 부수 발견
1. **[미확인]을 [확인됨]으로 승격시킨다.** briefing에 "미검증이니 단정 금지"라고 명시했는데도 1차본은 "글쓰기 API는 존재하지 않음"이라고 확정 서술했다. 그러면서 바로 아래 줄엔 [미확인]이라 적는 자기모순. **7B는 불확실성 표기를 잘 유지하지 못한다** — 사실 확실성 등급은 사람이 다시 봐야 한다.
2. **수치를 지어낸다.** "성공률 20% 미만"(briefing에 없음), 대행 시세를 문서마다 30~50 / 50~100 / 60만원으로 다르게 인용, "t3.micro(1GB) 1대에 크롬 10개" 같은 물리적으로 불가능한 계산. 교정 지시를 넣어도 "각 사용자당 최대 10개 브라우저" 같은 잔재가 남았다. **회의 산출물의 숫자는 전부 미검증으로 취급할 것.**
3. **`regen_artifact.py`의 기본 컨텍스트가 사주 토픽 하드코딩**(`사주_프로젝트3_페이즈2_사전입력.md`)이다. 무관한 안건에 그대로 쓰면 컨텍스트가 오염된다. 이번엔 회의 transcript로 `--context-file`을 만들어 우회. → 하드코딩 제거 백로그.
4. **분량 보강엔 "형식 채우기 + 최소 글자수"를 지시문에 박는 게 유효**(08-06 룰 재확인): 1102→1670자. 단 product_definition은 2회 보강 후에도 1089자로, 내용은 전 섹션 완비인데 서술이 짧았다. **구조가 완결되면 글자 수 미달은 통과시키는 게 낫다** — 더 돌리면 창작·오염 위험만 커진다.
5. **vault sync는 회의 종료 시점 스냅샷이다.** 보강 전 깨진 파일이 vault에 먼저 들어간다. 보강 후 **재복사 필수**(이번에 수동 재복사함). → 자동화 백로그.
6. 폴더명 토픽 절단은 이번에도 발생(`...포스팅 실행방식(` 로 끝남). 수동 rename.
7. `git push` wincredman 실패 재발 — `-c credential.helper='!gh auth git-credential'` 우회 필요.

## 페르소나 조합 기록 — `naver_blog_saas`
dohyun(진행) + seoa(제품) · seojin(기술) · doyeon(UX) · jinwoo(수익) · jiwon(리스크)

- **잘 작동**: `jiwon`(리스크·로드맵) — **유일하게 1차 통과**. 계정정보 보관 위험 → 대안 2개 → 조기 감지 지표까지 스스로 연결했고 Kill 기준도 날카로웠다. 리스크·QA 안건에 jiwon은 안정적(08-06에서도 1차 통과).
- **잘 작동**: `doyeon`(UX) — 붕괴만 걷어내면 플로우 7단계·필드 목록이 실물이었다. 화면·입력항목 설계에 강함.
- **주의**: `seojin` — 08-06에 이어 **또 아키텍처 안건에서 사실을 뒤집었다.** "7B는 익숙한 도구의 세부 절차를 그럴듯하게 지어낸다"는 08-06 관찰이 이번엔 "기술 비교의 우열을 통째로 뒤집는다"로 나타남. **seojin 산출물은 COO 육안 검증을 기본 절차로 삼는다.**
- **역할 재배치는 유효했다**: 음악 직함 페르소나(seoa=A&R, jinwoo=Suno 프롬프트 엔지니어)를 제품/수익 역할로 돌려 썼는데 **음악 오염 0건**. briefing 첫 줄에 "★음악 주제 절대 금지 + 이 회의에서 당신의 역할은 X다"를 박고 각 라운드 지시문에도 반복한 것이 먹혔다. `saas_us_10m`과 같은 패턴 — 재사용 가능.

## 관련
- `10_Wiki/Skills/auto-quality/2026-08-06_artifact_stage_has_no_language_gate.md`
- `10_Wiki/Skills/auto-quality/2026-08-07_language_gate_integrated_into_regen.md`
- `10_Wiki/Decisions/2026-08-08_naver_blog_saas_plan.md`
