# 2026-08-06 옵시디언 활용 극대화 — 칸반 진행상태 + 그래프 연결고리 + 읽기-쓰기 루프 (결재 대기)

회의: `run_20260806_142539` / 팀 `obsidian_ops_loop`
진행: 정도현(COO) · 패널 윤서진·이도연·박나래·박지원·서지안
산출물 6종: `moa-vault/00_Raw/2026-08-06/run_20260806_142539_obsidian_ops_loop_옵시디언_칸반_그래프연결_읽기쓰기루프/`

## 형 지시 (안건)
"프로젝트 진행상태 확인하는 기능도 있고, 업무 연결고리 기능도 있는데, 제대로 사용하는게 하나도 없어. 옵시디언의 최대한 활용 할 수 있는 방향으로 진행해 보자."

## 회의 전 실측한 사실 (추측 아님)
- 칸반 `01 Dashboard/TODO.Kanban.md` — 컬럼 6개(할 일/대기 중/진행 중/완료/보류/차단됨). 카드는 형 개인 건 6장뿐(할 일 1 + 완료 5). **모아 프로젝트 카드 0장. 대기중·진행중 컬럼 완전히 비어 있음.**
- 업무일지 `70 Record/*.md` 약 20개(06-26~08-06) — 전부 `> 관련: [[모아 시스템 가이드]] · [[모아 자산 목록]] · 지난 기록: [[전날]]` 한 줄만. 태그는 `#업무일지` 하나.
- **★근본 원인 확정: `02 Projects` 폴더에 모아 프로젝트 노트가 아예 없다.** 실제 내용물은 NanumN·Youtube PlayList·Suno How to·AI PlayList Helper·Side Project·Sample Project뿐. 일지가 링크를 걸고 싶어도 **걸 대상 노트가 존재하지 않는다.** 링크 습관의 문제가 아니라 링크 대상의 부재다.
- 이미 설치된 플러그인: **dataview · templater · quickadd · obsidian-kanban · obsidian-git.** 새 프로그램 설치 없이 자동 대시보드를 지금 만들 수 있다.

## 결정 (형 결재 필요)

### 1. 진실의 원천은 칸반이 아니라 프론트매터
카드를 사람이 손으로 옮기는 구조를 버린다. 각 노트의 YAML `status` 필드가 진실이고, 칸반·대시보드는 **dataview로 자동 집계된 표시**일 뿐이다.
`todo=할 일 / waiting=대기 중 / doing=진행 중 / done=완료 / hold=보류 / blocked=차단됨`

### 2. `02 Projects`에 모아 프로젝트 허브 노트 5개 신설
k-saju · 아투(american-todayz) · 덱스제나 · 쇼츠 자동발행 · 애드센스 블로그.
이것이 그래프 고립을 푸는 핵심 조치다. 허브가 생겨야 일지가 걸 링크가 생긴다.

### 3. 형 개인 칸반은 건드리지 않는다
`01 Dashboard/TODO.Kanban.md`는 형 개인 보드로 그대로 둔다. 모아 업무 보드는 **완전히 다른 파일명**으로 분리한다.
> ⚠️ 이도연 안의 `01 Dashboard/Todo.Kanban.md`는 대소문자만 다른 이름이라 윈도우에서 기존 파일과 충돌한다. **채택 불가 — 구현 시 다른 이름으로 교체할 것.**

### 4. 일지에 자동 백링크 — 단, 실재 확인 후에만
haru가 일지를 쓸 때 그날 건드린 프로젝트 허브 노트로 `[[링크]]`를 건다. 존재하지 않는 노트로의 링크는 만들지 않는다(깨진 링크 0개 유지).

### 5. 쓰기 전용 → 읽기-쓰기 루프
- **Pre-read**: 새 작업 시작 전, 관련 프로젝트 허브 노트와 매뉴얼을 먼저 조회하고 그 결과를 판단에 반영.
- **주간 회고**: 월요일 전략 리포트 시각에 지난 7일 일지에서 `막힌 것`·`클로 실수` 표기만 뽑아 회고에 반영.
- **반복 차단**: 같은 실수가 2회 이상 나오면 구조 수정 후보로 올린다.

### 6. 완료 판정에는 증거가 필요
카드를 `done`으로 옮기려면 커밋 해시 / 라이브 URL 재조회 값 / 파일 경로 중 하나 이상이 있어야 한다. 증거 없는 완료는 서지안이 반려.

### 7. 드리프트 감지
`doing` 상태로 7일 이상 변화 없음 → 경보. 최근 7일 일지 중 프로젝트 링크 0개 → 경보. 깨진 링크는 0개여야 한다. 경보는 `#drift-alert` 태그 + 디스코드.

### 8. 역할 분리 유지
moa-vault(vault-learn, 내부 학습) ↔ owenlab(work-journal, 형용 문서). 이 경계는 깨지 않는다.

## 구현 전 반드시 고칠 것 (7B 모델 산출물 결함 — COO 검토 지적)
1. **`Todo.Kanban.md` 파일명** — 기존 `TODO.Kanban.md`와 대소문자 충돌. 다른 이름으로.
2. **윤서진 안의 쓰기 권한 배분** — 프로젝트 허브 노트 쓰기를 qa-lead-jian에게 할당했다. **생성≠검증 원칙 위반.** 쓰기는 haru/담당 본부장, 지안은 판정만.
3. **git 절차 오류** — `git stash`를 백업으로, `git commit --allow-empty`를 원자적 쓰기로 서술한 것은 틀렸다. 원자적 쓰기는 임시파일 쓰기 후 rename으로 구현.
4. **dataview 문법** — `FROM "02 Projects/*"`의 와일드카드와 `DATENOW()`는 실제 문법이 아니다. 구현 시 검증 필요.
5. **정도현 플레이북의 주간 타임라인이 일일 타임라인과 동일**(복사), 야간 일지 시각이 `23:44`로 잘못 적힘(실제 `03:44`).
6. 플레이북의 "옵시디언 앱 UI 조작을 통한 자동화는 가능하지만" — 제약과 모순. UI 조작은 불가.

## 다음
- 형 결재 후 **Day 1 작업**: `02 Projects` 허브 노트 5개 + 모아 전용 보드 파일 + `01 Dashboard` dataview 대시보드 생성. 담당 archive-head-haru, 검수 qa-lead-jian.
- 위 결함 6개는 구현 담당자가 착수 전 반드시 반영.

---

## 실행 결과 (2026-08-06, archive-head-haru)

**★dataview 자동 집계 방식 채택. 칸반 카드를 손으로/CLI로 옮기는 방식은 폐기.**
진실의 원천 = 각 MOC 프론트매터 `status`. 대시보드는 그것을 읽어 그린 파생 화면일 뿐이다.

### 실제 만든 것 (owenlab, 커밋 `9ab2125` + 후속 커밋)
| 무엇 | 경로 |
|---|---|
| 프로젝트 MOC 6개 | `02 Projects/20 모아 스튜디오/{모아 스튜디오, k-saju, 아메리칸 투데이, 쇼츠 자동화, 덱스·제나 워커, 하네스 운영} MOC.md` |
| 모아 전용 대시보드 | `01 Dashboard/모아 프로젝트 현황.md` — dataview 3쿼리(진행중·최근7일완료·막힌것) |
| 일지 템플릿 | `90 Templates/일일 업무일지 템플릿.md` (`projects::`/`decisions::` 필수) |
| 기록 규칙 | `09 업무 가이드/옵시디언 기록 규칙.md` |
| 결정 기록(형용) | `05 회의·기획/2026-08-06 옵시디언 지식그래프 개편.md` |
| 소급 연결 | 일지 13편(06-26~08-05)에 연결 블록 추가 |

### 회의안에서 바꾼 것
- 허브 노트 **5개 → MOC 6개**(하네스 운영 추가, 애드센스는 k-saju·아투 MOC로 흡수). 폴더는 기존 번호와 안 겹치는 `02 Projects/20 모아 스튜디오/`.
- status 어휘를 영문 6값(todo/waiting/doing/done/hold/blocked) → **한글 4값 `진행중|대기중|완료|보류`** + 보조 필드 `blocked`(막는 것) · `waiting_on`(형·외부 회신 대기) · `updated`.
- 드리프트 자동 경보는 **미구현**. 지금은 `updated` 육안 확인만. (7일 박제 감지는 다음 과제)

### 결함 6개 처리
| # | 처리 |
|---|---|
| 1 | `Todo.Kanban.md` 폐기 → `01 Dashboard/모아 프로젝트 현황.md`. 형 개인 `TODO.Kanban.md`는 무수정 |
| 2 | qa-lead-jian 쓰기 권한 배정 삭제 — 검수는 **판정만**(생성≠검증) |
| 3 | git stash/빈 커밋 설명 폐기 → "임시파일 후 rename"으로 문서 정정(실구현엔 미사용) |
| 4 | dataview 문법 재작성 — `FROM "02 Projects/20 모아 스튜디오"` · `date(today) - dur(7 days)` · `TABLE WITHOUT ID`. **와일드카드·`DATENOW()` 제거** |
| 5 | 타임라인 시각을 실제 cron(13:48 오전분 / 03:44 오후·야간분)에 맞춤 |
| 6 | "앱 UI 자동 조작 가능" 서술 삭제 — 불가(GUI 접근 없음) |

### 미검증 (확인된 것으로 취급 금지)
- **옵시디언 화면에서 dataview 표가 실제로 렌더링되는지 확인 못 했다.** GUI 접근 불가. 쿼리 문법과 대상 파일·필드 존재만 텍스트 검증함.
- 그래프 뷰의 실제 개선 여부도 미확인. 링크 수만 실측(아투 17·쇼츠 15·모아 13·k-saju 12·하네스 10·덱스제나 6, 깨진 링크 0).
- `.obsidian/graph.json`이 `showTags:false`라 태그는 노드로 안 뜬다. 형 화면 설정이라 미변경.

### 부수 발견
회의 sync가 **moa-vault에 `01 Dashboard/TODO.Kanban.md`(빈 칸반)를 생성**했다. 칸반의 실체는 owenlab에 있으므로 잘못된 볼트 쓰기로 보인다. 타 에이전트 산출물이라 삭제하지 않고 보고만 함.

관련: work-journal 스킬, archive-head-haru, 2026-08-05 일지 cron 구조 수리.
