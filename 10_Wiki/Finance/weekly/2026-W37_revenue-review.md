# 2026-W37 주간 수익 리뷰 — CSO 한지영

기준일 **2026-09-07** · 목표 **월 순수익 ₩20,000,000** (형 확정 2026-06-26)
전략 리포트 `view_cso_2026-W37.md`와 겹치는 판단은 재논증하지 않는다. 여기선 **돈이 왜 안 되는가**만 다룬다.

---

## 1. 갭

`2026-W37_metrics.md` **산출됨**(`fin-w37`). 매출은 `/v2/sales`+`/v2/subscribers` 실조회로 확정한다 — **자동 원장이 아니라**(상충 ①·② 모두 종결).

| 항목 | 값 | 근거 |
|---|---|---|
| 9월 매출 | **₩0** | `ksaju-ledger-history.jsonl` — 08-23~09-07 매일 00:30Z, **23행 연속 `d_count:0 / d_usd:0`**. ★이건 **증분**이라 아래 절대치 결함의 영향을 받지 않는다 |
| 레코드 | **2줄 = 구독 1건** | 두 레코드의 **`subscription_id`가 동일**. 판매 "2건"은 고객 2명이 아니다 |
| 실징수 | **1회 · $7.99** (확정) | 06-26은 **무료 트라이얼 개시**(`invoice_url` 키 없음), 07-03만 실청구(`invoice_url`·`refunded` 있음). `free_trial_ends_at 07-03T05:40:56Z` → 청구 05:40:58 |
| 생애 총매출 | **$7.99 gross · 순수령 $6.16** | 수수료 $1.83 차감. **원장의 $15.98은 2배 과대계상** |
| ★**진성 외부 매출** | **생애 ₩0** (확정) | `purchase_email` = **형 본인 계정**. `burn_rate.md`의 "형 점검 결제" 서술이 [보고받음]→**[확인]**으로 승격 |
| 마지막 판매 | **2026-07-03** (확정) | 같은 원본 · 실청구 레코드 |
| 무매출 일수 | **66일** (확정) | 07-03 → 09-07 |
| 월 고정비 | **₩478,900 ~ ₩496,900** (확인분) | `2026-W37_metrics.md` — 환율 ₩1,346.56 오늘 실측. **8-12 값의 26일 stale은 해소됐다** |
| 월 순수익 | **−₩47.9만 ~ −₩49.7만** | 산출 |
| **갭** | **약 −₩2,048만 · 진척률 0.0%** | 산출 |
| 누적 순손실 6/27~9/7 | 약 **₩115만 ~ ₩119만** [추정] | 위 번레이트 73일 일할. **무매출 일수가 66이든 73이든 이 범위·갭 판정·액션은 안 바뀐다** |

### 상충 ① — **종결**
**SSOT = `/v2/sales` + `/v2/subscribers/{id}` 원본. 단, "매출"은 레코드 수가 아니라 `invoice_url`·`free_trial_ends_on`으로 걸러낸 실징수 건으로 센다.**
[보고받음: `fin-w37`, 전체 키 재조회 2026-09-07. **내가 재현하지 않았다.**]

```
06-26T05:40:56Z  무료 트라이얼 개시   invoice_url 키 없음
07-03T05:40:58Z  실청구 $7.99        invoice_url · refunded 있음
free_trial_ends_at = 07-03T05:40:56Z → 청구 05:40:58 (+2초)
두 레코드 subscription_id 동일 · purchase_email = 형 본인
→ 실징수 1회 $7.99 (순수령 $6.16). gross $15.98 은 2배 과대계상
```

**두 레코드 모두 `price:799`·`paid:true`를 달고 온다 — Gumroad가 트라이얼 개시 레코드에도 플랜 표시가를 채워 보낸다. 실징수 여부는 `price`로 갈리지 않는다.**

**원장 결함 3건** (`ksaju_ledger_snapshot.mjs`)

| 줄 | 결함 |
|---|---|
| **L58** | `price` 단순 합산 → **트라이얼을 매출로 셈. 금액 2배 과대** |
| **L74** | 정렬 미보장 `sales[length-1]` → 마지막 판매일이 **7일 이르게** 기록(Gumroad는 최신순으로 준다) |
| **L72** | `sale_count: sales.length` 는 레코드 수일 뿐 — **고객 수도 징수 건수도 아니다** |

**정본에 가까운 쪽은 `burn_rate.md`다** — 금액($7.99)도 날짜(07-03)도 맞았다. 자동 원장이 아니라 **수동 실측이 맞았다.**

**반증조건(형만 확인 가능)** — Gumroad **페이아웃 명세에 2026-06-26자 $7.99 입금 라인이 있으면** 이 판정이 틀리고 $15.98이 맞다.

**★"66일"이 두 번 나오는데 같은 66일이 아니다.** W36도 "66일"로 적었지만 **당시 실제는 59일**이었다 — W36은 버그 날짜(06-26)로 계산했고, 그 숫자가 우연히 오늘 값과 같아졌다. **"W36과 같으니 안 변했네"로 읽으면 안 된다.**

### 상충 ② (고정비 범위) — **종결**
내가 미해결로 남긴 것을 `fin-w37`이 갈랐다. **같은 원본이 아니었다** — ⓐ환율 ₩1,412.87 실측 vs ₩1,330 추정 ⓑNeon $0 확정 여부 ⓒ**범위 정의**: 한쪽은 확인분 밴드, 다른 쪽은 확인분 하한~미확인 상한을 한 줄로 합친 것. 다음 회차부터 `확인분` / `미확인 포함 상한`을 **두 줄로 분리 보고**한다. [보고받음: fin-w37, 내가 재현하지 않았다]

★L58·L72·L74 수정은 **액션이 아니라 기록으로 남긴다** — owner는 제나, 이번 주 액션 3개(§3)를 늘리지 않는다.

★**병목 진단(§2)은 안 바뀐다. 다만 근거가 세졌다** — 생애 매출이 전부 형 본인 결제였다면, **바깥 사람이 계산대까지 온 적이 한 번도 없다.** "발견 구간 미도달"과 같은 방향이다.

---

## 2. 병목 — "유입 부재"의 아래를 좁힌다

**(b) 타깃 불일치 → 기각.** [확인: `blog.k-saju.me` 라이브 HTML 직접 조회 09-07]
`<html lang="en">` · 제목 `K-Saju Journal — Korean Astrology & Four Pillars of Destiny` · 최신 글 전부 영문에 주제 적중(`What Is Gongmang?`, `Do Twins Have the Same Saju?`). **타깃(외국인)에 언어·주제 둘 다 맞다.**

**(a) 안 찾는 주제 / (c) 수요 없음 → 지금 판정 불가.** 데이터가 부족해서가 아니라 **시험이 돌아간 적이 없어서**다.
GSC 90일 평균순위 **k-saju 44.8 · blog 59.2** = 5~6페이지. 노출 731·486은 그 순위에서 나올 값이다. **한 번도 노출 구간에 선 적이 없으니 수요를 잰 적이 없다.**

> **좁힌 결론: 병목은 콘텐츠도 타깃도 아니라 "발견 구간 미도달"이다.**

**반증조건(사전 확정 · W38에 판정)** — `gsc_query_report.mjs`로 순위대별 쿼리 분포를 낸다.
- 평균순위 ≤10 쿼리가 **0개** → 결론 지지. 수요 논쟁 중단, W38은 순위·권위로 간다
- ≤10 쿼리가 **있는데** CTR<1% → **결론 틀림.** (a)/(c)로 넘어간다
- ≤10에 클릭도 있는데 결제 0 → 병목은 유입 아니라 제품·가격

---

## 3. 다음 주 액션 3개

**① [제나] 게스트 체크아웃 병합·배포** — 전략 §4와 동일 건. 수익 관점 근거만 덧붙인다: **계산대가 닫혀 있으면 §2 반증시험의 마지막 칸을 읽을 수 없다.**
- 완료: 비로그인 `POST /api/gumroad/checkout {"kind":"onetime"}`이 **401이 아닌** 응답
- 판정: 클로가 매일 1회 찔러 원장 1줄 (전략 §4)
- 하지 말 것: 워크트리 미커밋 33건 일괄 커밋 — 게스트 경로 파일만 고를 것
- 상한: **09-10**까지 라이브 불변이면 덱스가 중단·재설계

**② [제나] 퍼널 원장 소생** — ★이번 회차 신규 발견
사실표는 W36 액션③을 "미착수"로 채점했으나 **틀렸다.** 원장이 스스로 원인을 적어두고 있다: `{"skipped":true,"reason":"DATABASE_URL is not the production host","db_host_seen":"127.0.0.1"}`(08-23, 2행). 같은 00:30Z 슬롯의 다른 두 계측(`ksaju-ledger-history`·`instrument-health`)은 **오늘까지 매일 정상 기록**된다. 즉 안 한 게 아니라 **환경변수 하나로 거부되고 배선이 끊긴 것**이다 — "돌긴 하는데 세어지지 않는" 그 병.
- 완료: `ksaju-funnel-history.jsonl`에 `skipped` 없는 **실수치 3줄 이상**(09-14까지)
- 판정: 클로 읽기만. **줄 수로 센다**
- 하지 말 것: 스크립트 로직 재작성 / **거부 로직 제거 금지 — 그 거부는 옳다**
- 상한: 2회. 실패 시 덱스 이관

**③ [클로] 순위대별 쿼리 분포 1회 조회** (§2 반증시험 집행)
- 완료: k-saju·blog 각각 ≤10 / 11~20 / 21+ 쿼리 수 + 상위 10 쿼리 표 1장
- 판정: §2 3분기를 그대로 적용
- 하지 말 것: **해석 붙이지 말 것.** 표와 판정선 적용 결과만
- 상한: 조회 2회. 아투는 `gsc_query_report.mjs:19`의 `SITES`에 없어 이번에도 제외(추가는 제나 몫, 액션 아님)

**★배분 실패 기록** — ①②의 owner(제나)·마감(덱스)에게 **이번 회차에도 못 넘겼다.** CSO `SendMessage`는 W36에 이미 안 닿았고, 클로 디스코드 송신은 `not allowlisted`로 막혀 있다. **액션은 발행됐고 손이 붙지 않았다.**

---

## 4. 형 결재 요약

**① [필수·5분] 배분 경로** — 이번 주 액션 3개 중 2개는 제나가 손을 대야 하는데, 클로 세션에 제나·덱스로 보낼 수단이 없습니다. allowlist를 풀어 주시거나, 아래를 형이 직접 전달해 주시면 됩니다.
> **제나** — ⓐ `guest-checkout-seojin` 병합·배포(09-10 마감) ⓑ 퍼널 스냅샷 `DATABASE_URL`을 운영 DB로(09-14까지 실수치 3줄)
> **덱스** — 위 2건 마감·중간결재
3주 연속 **바깥 상태를 바꾸는 일만** 실패했는데, 그 자리에 이 끊긴 경로가 있습니다.

**② [이번 주 안 올림] GA4** — 아침에 올리려던 건은 API 자체가 GCP에서 꺼져 있어(403 `SERVICE_DISABLED`) 뷰어 추가만으론 안 풀립니다. **먼저 우리가 확인합니다** — 서비스계정으로 `gcloud` enable이 되는지. 안 되면 그때 "API 사용설정 + 뷰어 추가"를 한 장으로 올립니다.

---

## 5. 내가 못 본 것 · 내가 틀린 것

**★내가 틀린 것 (팀리드 지적, 수용)** — 이 리뷰 초판에서 **SSOT를 선언했다가 철회했다.** 근거가 "23일 무결번"이었는데 **그건 일관성이지 정확성이 아니다.** 그리고 `burn_rate.md`를 "1건/수기 2차 기록"으로 요약한 것도 틀렸다 — **2건이고, 8-12 API 직접 조회 실측**이다. 상충 ②는 미해결로 남기면서 상충 ①만 종결한 것은 **같은 사안에 다른 잣대**였다.

**★내가 두 번째로 틀렸다고 적었던 것은 — 철회한다.** L58 `price` 합산이 결함이라고 쓴 내 초판이 **맞았다.** 1차 실조회를 근거로 "그건 내 추측이었고 틀렸다"고 자기정정했는데, **그 1차 조회가 틀렸다.** 2차에서 뒤집혔다.
> **자기정정도 근거가 틀리면 틀린다.** 나는 남의 실측 앞에서 내 판단을 너무 빨리 접었다. **"내가 틀렸다"는 쪽이 언제나 더 안전한 답인 것은 아니다.**

**★이번 회차에서 제일 값나가는 문장** — **매일 자동으로 돌며 `ok:true`를 찍던 원장이, 수동 실측보다 틀렸다.** 결함 3건이 전부 그 안에 있었고 23일간 아무도 몰랐다. **`ok:true`는 "수집에 성공했다"이지 "값이 맞다"가 아니다.**
그리고 1차 조회가 틀린 이유도 같은 종류다 — **뽑을 키를 지정해서 조회하는 바람에 판정을 가르는 필드(`invoice_url`·`free_trial_ends_on`)가 출력에 아예 안 잡혔다.** 없는 것을 안 본 게 아니라, **안 물어봐서 없어 보였다.**

- **매출은 확정됐다** — 단, 근거는 `fin-w37` 보고이고 **내가 재현하지 않았다.** 반증조건(06-26 페이아웃 입금 라인)은 §1에 있고 **형만 확인 가능하다**
- ~~`2026-W37_metrics.md` 미산출~~ → **산출됐다**(`fin-w37`, 같은 폴더). 고정비 ₩478,900~496,900(확인분), 환율 **₩1,346.56**(오늘 실측, `open.er-api.com` + `api.frankfurter.app` 교차검증). 내가 쓴 *"8-12 값 그대로 26일 stale"* 은 **해소됐다** [보고받음: fin-w37, 내가 재현하지 않았다]
- ~~상충 ②(고정비 범위) 미해결~~ → **종결됐다.** 내가 미해결로 남긴 것을 `fin-w37`이 갈랐다. **같은 원본이 아니었다** — (a)환율 ₩1,412.87 실측 vs ₩1,330 추정 (b)Neon $0 확정 여부 (c)**범위 정의**: 한쪽은 확인분 밴드, 다른 쪽은 확인분 하한~미확인 상한을 한 줄로 합친 것. 다음 회차부터 `확인분` / `미확인 포함 상한` **두 줄로 분리 보고**한다 [보고받음: fin-w37, 내가 재현하지 않았다]
- 퍼널 스냅샷이 **지금도 도는데 no-op인지, 아예 멈췄는지** — 원장에 08-23 이후 행이 없다는 것까지만 봤다. 스케줄 배선은 안 봤다
- **런웨이를 못 낸다** — 번레이트(분모)는 확정됐으나 **가용자금 Y가 없다.** 2026-08-12 요청 이후 **26일째 미회신**. 추정으로 채우지 않았다 — Y만 들어오면 즉시 읽는 표가 `2026-W37_metrics.md` §6에 있다
- 아투 GSC 전체 — `SITES` 배열 밖
- 브랜치 코드가 실제로 맞는지 (병합 전이라 검증 불가, W36과 동일)
- Gumroad 상품 페이지 자체의 조회·전환 (대시보드 미조회)
