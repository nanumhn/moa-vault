# 주간 지표 스냅샷 — 2026-W37 (9/1~9/7)

> **작성:** data-finance-jiwon (측정 담당) | **작성일:** 2026-09-07 (월)
> **직전 스냅샷:** `2026-W34_metrics.md` (2026-08-20) — W35·W36에는 metrics 문서가 없다(`weekly/` 디렉터리 직접 조회, 2026-09-07). W36은 리뷰 문서만 존재.
> **목표 기준:** 월 순수익 **₩20,000,000** — 형 확정 2026-06-26 (CLAUDE.md 하단 "핵심 KPI 정정" 절).
> **역할 경계:** 나는 측정만 한다. 병목 진단·처방·액션 제안은 하지 않는다(CSO 몫).

**표기:** `[확인]` = 이번 회차에 내가 직접 조회 / `[승계]` = 과거 실측을 날짜 명시하고 이어씀(이번 회차 재측정 안 함) / `[추정]` = 계산·모델 기반 / `[권한없음]` = 접근 권한이 없어 못 봄 / `[못 찾음]` = 내가 조회한 범위에서 찾지 못함

---

## 1. 한 줄 요약

```
이번 주(9/1~9/7) 매출   ₩0            [확인]
9월 누적 매출            ₩0            [확인]
월 고정비(확인분)        ₩478,900 ~ ₩496,900   [확인]+[승계]+[추정]
월 고정비(미확인 상한)   ₩610,500 ~ ₩628,400   [추정]
월 순손익                −₩478,900 ~ −₩496,900 (상한 시 −₩628,400)
목표 대비 갭             −₩20,478,900 ~ −₩20,496,900 (상한 시 −₩20,628,400)
목표 진척률              0.0%
마지막 실판매 이후       66일 (2026-07-03 → 2026-09-07)  ★날짜 정정됨, §3 참조
생애 실징수              1회 · $7.99 gross / $6.16 순수령 · 구매자=형 본인
                        ★자동 원장의 $15.98은 2배 과대계상이었다, §3-2 참조
진성 외부 매출 (생애)    ₩0
런웨이                   산출 불가 — 가용자금 Y를 내가 조회한 범위에서 찾지 못함 (§6)
```

---

## 2. 라인별 매출

| # | 라인 | 이번 주 | 9월 누적 | 생애 누적 | 구분 |
|---|---|---|---|---|---|
| ① | **k-saju 결제 (Gumroad)** | **₩0** | **₩0** | **실징수 $7.99 (gross) / 순수령 $6.16** — 전액 형 본인 테스트 | **[확인]** Gumroad `/v2/sales`·`/v2/subscribers` 직접 조회 2026-09-07 + `ksaju-ledger-history.jsonl` 09-01~09-07 전 스냅샷 `d_count:0`·`d_usd:0`. ★생애 누적은 §3에서 $15.98 → **$7.99로 정정됨**(자동 원장이 무료 트라이얼을 매출로 셈) |
| ② | **쇼츠 B2B 납품** | **₩0** | ₩0 | ₩0 | **[확인]** 매출 원장 자체가 존재하지 않음 — `10_Wiki/Finance/` 아래에 쇼츠 관련 원장 파일 없음(디렉터리 직접 조회) |
| ③ | **애드센스 / 유튜브** | **[권한없음]** | [권한없음] | [권한없음] | §5 |
| ④ | **외주 (픽셀 오피스 등)** | **[못 찾음]** | [못 찾음] | [못 찾음] | 입금 원장 파일을 조회 범위에서 찾지 못함 |
| (외) | nblog-saas | ₩0 | ₩0 | ₩0 | KPI 포트폴리오 외 (2026-06-26 확정 포트폴리오 = k-saju·쇼츠·애드센스) |

> **라인별 합 = 총합 검증:** ①0 + ②0 + ④0 = ₩0 = 총 매출 ₩0. ③은 측정 자체가 안 되므로 **합계는 하한이다** — 애드센스에 실적립이 있었다면 실제 매출은 그만큼 크다.

---

## 3. ★ 원장 상충 ① 판정 — **가름. SSOT 확정.** (2차 조회로 **1차 판정을 뒤집었다**)

W36 리뷰가 "미규명 — 확정 안 함"으로 남긴 건이다(`2026-W36_revenue-review.md` §데이터 상충, `resolution: "미규명"`). 이번에 **Gumroad API를 읽기전용으로 직접 조회해 원인을 특정했다.**

> ⚠️ **이 절은 두 번 썼다.** 1차 조회로 "gross $15.98"을 SSOT로 선언했다가, 팀리드의 이의제기를 받고 전체 필드를 다시 조회해 **"실징수 $7.99"로 정정했다.** 1차 판정 내용과 왜 틀렸는지를 §3-1b·§3-2에 지우지 않고 남긴다.

### 3-1. 오늘 실제로 본 것 (2026-09-07, `/v2/sales?after=2020-01-01`)

| idx | created_at (UTC) | product | price | recurring_charge | refunded | subscription_id |
|---|---|---|---|---|---|---|
| 0 | **2026-07-03T05:40:58Z** | K-Saju Premium | 799¢ | **true** | false | 있음 |
| 1 | **2026-06-26T05:40:56Z** | K-Saju Premium | 799¢ | **false** | false | 있음 |

- `sales.length = 2` · `sum(price)/100 = 15.98` · `max(created_at) = 2026-07-03T05:40:58Z` — 전부 **[확인]**
- `/v2/products` 응답: `K-Saju Premium` → `price: 0`, `sales_count: 0`, **`sales_usd_cents: 799`**, `is_tiered_membership: true` / `K-Saju Full Reading` → `sales_count: 0`, `sales_usd_cents: 0` — **[확인]**
- `/v2/products/{id}/subscribers` → 두 상품 모두 **count 0**. **현재 활성 구독자 0명 [확인]**

### 3-1b. ★★ 2차 조회 — 전체 필드를 열자 내 1차 판정이 뒤집혔다

**1차 조회에서 나는 `price: 799` 두 건만 보고 "생애 총매출 gross $15.98"을 SSOT로 선언했다. 그 선언은 틀렸다.**
팀리드가 *"①이 매출을 2배로 세고 있는 것 아니냐"* 고 반증조건과 함께 이의를 제기해, `/v2/sales` · `/v2/sales/{id}` · `/v2/subscribers/{id}` 의 **전체 키 목록**을 다시 받았다. **판정을 가르는 필드 4개가 1차 출력에는 안 잡혀 있었다**(내가 키를 지정해 뽑았고, 그 목록에 없던 필드들이다 — 조회 설계 실수다).

| 필드 | **06-26 레코드** | **07-03 레코드** | 뜻 |
|---|---|---|---|
| `free_trial_ends_on` | **"Jul 3, 2026"** | "Jul 3, 2026" | 06-26 ~ 07-03 구간 **전체가 무료 트라이얼** |
| **`invoice_url`** | **키 자체가 없음** | **있음** | ★**인보이스는 돈이 실제로 움직인 건에만 생긴다** |
| `refunded` | 키 자체가 없음 | `false` | 환불 판정 대상 자체가 아니었음 |
| `gumroad_fee` | 183 | 183 | 수수료 $1.83 (= 22.9%) |

구독 상세 `/v2/subscribers/{id}` — **[확인]**
```
subscription_id 유니크 개수 = 1   |  두 레코드 동일? true
created_at                     2026-06-26T05:40:59Z
free_trial_ends_at             2026-07-03T05:40:56Z   ← 07-03 청구가 이 시각 +2초
user_requested_cancellation_at 2026-07-03T06:05:38Z   ← 첫 청구 25분 뒤 해지
cancelled_at                   2026-08-03T05:41:00Z
status                         cancelled  ·  recurrence  monthly
```
- **두 레코드의 `subscription_id`가 동일**함을 확인했다 → 판매 "2건"은 **고객 2명이 아니라 동일 구독 1건의 레코드 2줄**이다. (1차에서 [추정]으로 남겼던 항목 — **[확인]으로 승격**)
- `purchase_email` = **형 본인 계정**(두 레코드 동일) → `burn_rate.md`의 "형 본인 점화 테스트" 기록이 **[보고받음]에서 [확인]으로 승격**된다.

### 3-2. 왜 두 원장이 달랐나 — 원인 2개 (개정판)

**원인 A — 마지막 판매일 불일치(06-26 vs 07-03)는 수집 스크립트의 버그다.**

`C:\Users\user\.moa\ksaju_ledger_snapshot.mjs` 74번째 줄이 `last_sale_at` 을 **`sales[sales.length - 1].created_at`** 으로 뽑는다(파일 직접 읽음). 그런데 Gumroad `/v2/sales` 는 **최신순(내림차순)** 으로 준다(위 표 idx 0이 07-03, idx 1이 06-26 — 실측). 따라서 이 코드는 **배열의 마지막 = 가장 오래된 레코드**를 집는다.

→ **`burn_rate.md`의 07-03이 맞고, 자동 원장의 06-26이 틀렸다.** 최대값을 쓰려면 `max(created_at)` 이어야 한다.
→ ★ **W36 리뷰의 "66일 무매출"(2026-08-31 시점)은 이 버그 날짜로 센 값이다.** 07-03 기준으로 그날은 **59일**이었다. 오늘 09-07이 66일이다.

**원인 B — 총액 불일치($15.98 vs $7.99)는 ①이 트라이얼을 매출로 세기 때문이다. ★$15.98은 2배 과대계상이다.**

| 값 | 출처 | 판정 |
|---|---|---|
| **$15.98** | `/v2/sales` 레코드 2건의 `price` 단순합 (799+799) — 자동 원장 `ksaju_ledger_snapshot.mjs:58` | **틀림 — 2배 과대** |
| **$7.99** | `/v2/products.sales_usd_cents` — `burn_rate.md` §3이 쓴 값 | **맞음** |

**근거 4개가 모두 "실징수 1회"를 가리킨다:**

| # | 근거 | 구분 |
|---|---|---|
| 1 | 구독 `free_trial_ends_at = 2026-07-03T05:40:56Z`, 구독 개시 `2026-06-26T05:40:59Z` → **06-26~07-03 구간 전체가 무료 트라이얼.** 트라이얼 구간에는 돈이 움직이지 않는다 | **[확인]** 필드 직접 조회 |
| 2 | **06-26 레코드에는 `invoice_url` 키가 아예 없고, 07-03 레코드에만 있다.** 인보이스는 실제 결제 건에만 발급된다 | **[확인]** 두 레코드 전체 키 목록 대조 |
| 3 | 07-03 청구 시각(05:40:58)이 **트라이얼 종료 시각(05:40:56)의 +2초** — 트라이얼 종료 직후 첫 실청구라는 뜻 | **[확인]** |
| 4 | Gumroad 자신의 상품 매출 집계 `sales_usd_cents = 799` = **$7.99** (한 건 분) | **[확인]** |

> ★ **1차에서 내가 "`/v2/products`는 `sales_count:0`을 같이 보고하므로 자기모순이라 SSOT 부적격"이라고 쓴 것도 과단정이었다.** 티어드 멤버십에서 `sales_count`가 구독 건을 어떻게 세는지 나는 확인하지 못했다. **`sales_count`를 근거로 이 엔드포인트 전체를 기각하면 안 됐다.** `sales_usd_cents` 쪽은 위 근거 1~3과 정확히 일치한다.
>
> **잔여 불확실성(정직하게 남긴다):** 두 레코드 모두 `paid: true`, `price: 799`, `gumroad_fee: 183`을 달고 온다. 즉 **Gumroad는 트라이얼 개시 레코드에도 플랜 표시가와 수수료를 채워 보낸다.** 실제 입금 명세(페이아웃)는 형만 볼 수 있다.
> **반증조건:** Gumroad 페이아웃/명세에 **2026-06-26자 $7.99 입금 라인이 존재하면** 위 판정은 틀렸고 $15.98이 맞다.

### 3-3. 판정 (SSOT 선언) — **개정판**

> **★ k-saju 매출의 단일 진실 출처(SSOT) = Gumroad `/v2/sales` + `/v2/subscribers/{id}` 원본 레코드.**
> **단, "매출"은 레코드 수가 아니라 `invoice_url`·`free_trial_ends_on`으로 걸러낸 실징수 건으로 센다.**
>
> | 항목 | 확정값 | 근거 |
> |---|---|---|
> | 생애 판매 **레코드** | 2줄 | `/v2/sales` |
> | 생애 **고객 / 구독** | **1명 / 1건** | `subscription_id` 유니크 1개, 두 레코드 동일 |
> | **생애 실징수** | **1회 · $7.99 (gross)** | 위 근거 1~4 |
> | **생애 순수령** | **$6.16** | $7.99 − `gumroad_fee` $1.83 (필드값 직접 확인) |
> | **마지막 실징수일** | **2026-07-03T05:40:58Z** | `max(created_at)` + `invoice_url` 보유 |
> | 구매자 | **형 본인 계정** | `purchase_email` |
> | **진성 외부 매출 (생애)** | **₩0** | 위 항목의 논리적 귀결 |
> | 현재 활성 구독자 | **0명** | subscribers count 0, `status: cancelled` |
>
> **★어느 원장도 통째로는 맞지 않았다.**
> - 자동 원장(`ksaju-ledger-history.jsonl`): **금액 2배 과대**(트라이얼을 매출로 셈) + **날짜 틀림**(정렬 미보장). 건수 "2"는 레코드 수로는 맞으나 **고객 수도 징수 건수도 아니다.**
> - `burn_rate.md`: **금액 $7.99 맞음 · 날짜 07-03 맞음.** 다만 §3 표에서 `/v2/products`(금액)와 `/v2/sales`(건수)를 섞어 쓴 탓에 읽는 쪽이 "1건/$7.99"로 오독했다.
> - **정본에 가까운 쪽은 `burn_rate.md`다.** 매일 자동으로 도는 원장이 수동 실측보다 틀렸다.

**무매출 기준일 확정: 2026-07-03.** → 오늘 2026-09-07 기준 **66일**. (06-26 기준이면 73일이나, 그날은 트라이얼 개시일이지 매출일이 아니다.)

### 3-4. ★ 자동 원장 결함 3건 — 별도 항목 (판정만 한다, 코드 수정은 내 소관 아님)

대상 파일 `C:\Users\user\.moa\ksaju_ledger_snapshot.mjs` · 예약작업 `MoaKsajuDailyMetrics`

| # | 위치 | 결함 | 영향 |
|---|---|---|---|
| **1** | **L58** `sales.reduce((a,s)=>a+Number(s.price\|\|0),0)` | 무료 트라이얼 개시 레코드도 `price:799`를 달고 오는데 그대로 합산한다. **`free_trial_ends_on` / `invoice_url` 유무를 안 본다** | **매출 2배 과대계상.** $7.99 → $15.98 |
| **2** | **L74** `sales[sales.length-1].created_at` | 정렬을 보장하지 않고 배열 마지막 원소를 "마지막 판매"로 쓴다. 실측상 Gumroad는 **최신순**이라 **가장 오래된 건**이 잡힌다 | **마지막 판매일 7일 이르게 기록.** 무매출 일수가 7일 과대 |
| **3** | **L72** `sale_count: sales.length` | 레코드 수일 뿐 **고객 수도 징수 건수도 아니다** | "2건"이 고객 2명으로 오독됨 |

> **★이 셋의 공통 성질:** 매일 자동으로 돌고, `ok:true`를 찍고, 23줄이 쌓이는 동안 **아무도 의심하지 않았다.** 초록불이 뜨는 것과 맞는 값을 세는 것은 다르다.
> **안 고치면 내일 00:30 수집부터 같은 오차가 그대로 다시 쌓인다.** (수정 owner 지정은 CSO/클로, 코드는 제나 몫.)

---

## 4. 비용 — 이번 회차 실측 + 상충 ② 판정

### 4-1. ★ 원장 상충 ② 판정 — **가름. 같은 원본이 아니었다.**

W36 리뷰가 "같은 8/12 실측인데 고정비 숫자가 다르다"로 신고한 건이다. 두 문서를 직접 대조한 결과 **원본이 같지 않았고, 범위의 정의도 달랐다.** 원인 3개다.

| # | 원인 | `2026-W34_revenue-review.md` (₩50.1~52.0만) | jiwon의 `burn_rate.md` 판독 (₩47~65만) |
|---|---|---|---|
| 1 | **환율** | **₩1,412.87** [실측] — `2026-W34_metrics.md` §7-2가 "환율 실측 교체"라고 명시하고 갱신함 | **₩1,330** [추정] — `burn_rate.md` 머리말의 가정 환율 |
| 2 | **Neon 요금제** | **$0 확정** — `2026-W34_metrics.md` §7-1이 `neon.max_cluster_size = 512MB` 실조회로 Free 확정, 미확인 상한에서 $19 제거 | **$0~$19 미확정** — `burn_rate.md` §2-4 |
| 3 | **범위의 정의** | **확인분만**의 하한~상한 (Lightsail $5~7 · 전기 ₩13k~28k 폭) | **확인분 하한(₩47.3만) ~ 미확인분 전부유료 상한(₩64.9만)** — **서로 다른 두 시나리오를 한 범위로 합침** |

> **판정:** W34 쪽이 더 최신·더 좁은 원본이다(환율 실측 + Neon 확정). jiwon의 "₩47~65만"은 **틀린 값이 아니라 다른 것을 잰 값**이다 — 확인분 밴드와 미확인 상한을 한 줄에 붙여 쓴 표기라, 읽는 쪽에서 "확인분의 범위"로 오독되기 쉽다.
> **→ 이번 회차부터 비용은 반드시 두 줄로 분리해 보고한다: `확인분 [a~b]` / `미확인 포함 상한 [c~d]`.** 한 범위로 합치지 않는다.
> W36 리뷰는 "jiwon 값 채택"이라고 적었는데, 그건 결과적으로 **더 오래된 환율(₩1,330)을 채택한 셈**이었다.

### 4-2. 이번 회차 확정값 (2026-09-07)

**이번 주 새로 실측한 것:**
- **환율 ₩1,346.5649/USD** [확인] — `open.er-api.com/v6/latest/USD` 직접 호출, 응답 기준시각 `Mon, 07 Sep 2026 00:02:31 +0000`. 교차검증: `api.frankfurter.app` → **₩1,350.35** (기준일 2026-09-04). 두 값 차이 0.3%(총액 영향 약 ₩1,200) → 본 표는 **₩1,346.5649** 로 계산.
  ★ **W34의 ₩1,412.87 대비 −4.7%.** 지출 구조가 아니라 환율만으로 원화 고정비가 내려갔다.
- **구독 등급 2건 재확인** [확인] — `~/.claude/.credentials.json` → `subscriptionType: max`, `rateLimitTier: default_claude_max_5x` / `~/.codex/auth.json` id_token 클레임 → `chatgpt_plan_type: pro`, `OPENAI_API_KEY` 비어 있음(= API 종량과금 없음). **비밀값은 읽지도 출력하지도 않았다.**

| 항목 | $/월 | ₩/월 | 구분 |
|---|---|---|---|
| ChatGPT Pro (덱스 Codex + GPT 이미지 자동조종) | 200 | 269,313 | **[확인 2026-09-07]** 등급 클레임 재확인 |
| Claude Max 5x (클로 + 전 본부장 에이전트) | 100 | 134,656 | **[확인 2026-09-07]** 등급 클레임 재확인 |
| AWS Lightsail (nanumn·toastdm·nblog-saas·suno-helper) | 5~7 | 6,733~9,426 | **[승계 2026-08-12]** 스펙 실측 / 번들 세대 미확정 |
| AWS Route 53 (toastdm.com 호스팅영역) | 0.50 | 673 | **[승계 2026-08-12]** |
| Neon Postgres | 0 | 0 | **[승계 2026-08-20]** Free 확정 |
| **소계 (해외)** | **305.50~307.50** | **411,376~414,069** | |
| VAT 10% + 카드 해외이용수수료 ~1% (×1.11) | | **456,627~459,616** | **[추정 — 계산 방식]** 실결제 명세 미확인 |
| 도메인 4개 | | 9,300 | 만료일 [승계 실측] / 갱신가 **[추정]** |
| 전기 (24/7 PC 증분) | | 13,000~28,000 | **[추정]** |
| **확인분 합계** | | **₩478,900 ~ ₩496,900** | |

**미확인분 (형만 확인 가능)** — Vercel $0~20 · Higgsfield $0~49 · Hugging Face $0~9 · fal.ai / Suno / GCP 결제계정. 전부 유료 상단이면 **+$88 → +₩131,500**(오늘 환율·VAT 포함).

| | 값 |
|---|---|
| **확인분** | **₩478,900 ~ ₩496,900** |
| **미확인 포함 상한** | **₩610,500 ~ ₩628,400** |

**이번 회차에 재측정하지 않은 것(정직하게 밝힌다):** Lightsail 서버 생존·번들 세대, 도메인 4개 등록 상태, 전기 소비량. 전부 08-12 실측의 [승계]다. 이 3개를 합쳐도 확인분의 **약 6~8%**라 총액 판정을 뒤집지 않는다.

### 4-3. 비용 쏠림 (사실만)

| 묶음 | ₩/월 (확인분 하한 기준) | 비중 |
|---|---|---|
| **AI 구독 2건** | **₩448,406** (VAT·수수료 포함 환산) | **약 93%** |
| 인프라 (Lightsail+Route53+도메인) | ₩17,500~20,500 | 약 4% |
| 전기 | ₩13,000~28,000 | 약 3~6% |

---

## 5. 아투 · 애드센스 — 못 본 이유

| 항목 | 결과 | 왜 못 봤나 |
|---|---|---|
| **AdSense 사이트별 실적립액** | **[권한없음]** | 퍼블리셔 계정 `ca-pub-6268517976287068` 의 소유자가 우리 서비스계정과 불일치. W34에서 확인되고 **미해소 상태로 유지 중** [보고받음: `2026-W34_revenue-review.md`·`2026-W36_revenue-review.md` — 이번 회차에 내가 재시도하지 않음]. 같은 판정을 **2026-09-07 W37 리서치도 `[권한없음]`으로 남겼다** [보고받음: 팀 리드 전달 · 내가 재현 안 함] |
| **아투(american-todayz) 수익** | **[권한없음]** | 위와 **같은 AdSense 계정을 k-saju 블로그와 공유**한다. 계정 접근이 열려도 **사이트 측정기준 분리 없이는 어느 라인 매출인지 못 가른다**(W33에서 확인) |
| **유튜브 채널 6개 수익** | **[못 찾음]** | 실입금 기록 파일을 조회 범위에서 찾지 못함 |
| **아투 검색 실적** | **[못 찾음]** | `C:\Users\user\.moa\gsc_query_report.mjs` 의 SITES 목록에 아투 도메인이 없어 조회 자체가 불가 [보고받음: 오늘 아침 클로 실측 · 내가 재현 안 함] |

> ★ 애드센스는 **③라인 매출**인 동시에 **블로그 트래픽의 유일한 계측기**다. 이게 [권한없음]인 한, 위 §2의 총 매출 ₩0은 **하한값**이고 블로그 유입도 계속 미측정으로 남는다. (W33부터 5회차 연속 동일 상태.)

---

## 6. 런웨이 — 산출 불가

**런웨이(개월) = 가용 운영자금 Y ÷ 월 번레이트.** 분모는 위 §4에서 확정됐다. **분자 Y를 내가 조회한 범위에서 찾지 못했다.**

조회한 범위: `moa-vault/10_Wiki/Finance/**`, `moa-vault/30_Learning/**` 에서 `가용자금`·`운영자금`·`잔고`·`런웨이` 검색. 나온 것은 **전부 "형이 넣어야 할 값"이라는 요청 기록**뿐이고, 실제 값이 기입된 곳은 없었다.
- `burn_rate.md` §6 — "현재 가용 운영자금 Y는 형 개인 재무정보이고 우리 어디에도 없다"
- `burn_rate.md` §7 체크리스트 — `[ ] ★ 현재 가용 운영자금 Y` (**미체크**)
- `2026-W34_metrics.md` 액션 #10 — "가용 운영자금 Y 입력 / owner 형" (**회신 기록 없음**)

> **즉 이 항목은 2026-08-12에 요청된 뒤 오늘까지 26일째 미회신이다.** 값이 없는 게 아니라 **우리가 볼 수 없는 곳에 있다.** 추정으로 채우지 않는다.

**Y가 들어오면 즉시 나오는 표 (오늘 번레이트 기준):**

| 가용자금 Y | X=₩47.9만 (확인분 하한) | X=₩49.7만 (확인분 상한) | X=₩62.8만 (미확인 포함 상한) |
|---|---|---|---|
| ₩100만 | 2.1개월 / 9주 | 2.0개월 / 9주 | 1.6개월 / 7주 |
| ₩300만 | 6.3개월 / 27주 | 6.0개월 / 26주 | 4.8개월 / 21주 |
| ₩500만 | 10.4개월 / 45주 | 10.1개월 / 44주 | 8.0개월 / 35주 |
| ₩1,000만 | 20.9개월 / 91주 | 20.1개월 / 87주 | 15.9개월 / 69주 |
| ₩2,000만 | 41.8개월 / 181주 | 40.3개월 / 175주 | 31.8개월 / 138주 |

> 전제: **매출 0 지속**. 매출이 붙으면 분모가 줄어 런웨이는 그만큼 늘어난다.

---

## 7. 갭 분해 — 목표 대비 현재 위치

```
목표          월 순수익 ₩20,000,000
현재          매출 ₩0 − 비용 ₩478,900~496,900 = 순손익 −₩478,900 ~ −₩496,900
갭            −₩20,478,900 ~ −₩20,496,900   (미확인 상한 시 −₩20,628,400)
진척률        0.0%
```

**갭의 퍼널 단계별 귀속 — 이번 회차에는 못 한다.**

퍼널 원장 `ksaju-funnel-history.jsonl` 은 **총 3줄뿐**이고(파일 직접 조회 2026-09-07):
- 실수치 라인 **1줄** — `2026-08-12T04:13:27Z` (users 9 / subs 4 / readings 0 / daily_cards 2 / last_signup **2026-06-17**)
- 나머지 **2줄은 `skipped:true`** — `"DATABASE_URL is not the production host — refusing to record non-production numbers"`, `db_host_seen: "127.0.0.1"` (2026-08-23)
- **2026-08-24 이후 라인 증가 0줄. 26일째 정지.**

> ★ **원장이 "돌긴 하는데 세어지지 않는" 상태다.** 스크립트는 실행됐지만 로컬 DB를 보고 **기록을 거부**했다. 거부는 옳은 동작이다(비운영 숫자를 원장에 넣지 않았다). 문제는 **거부된 뒤 아무도 운영 DB로 연결을 바꾸지 않았다**는 것이다.
> **따라서 방문→입력→미리보기→결제 단계별 전환율과 "최대 누수 단계"를 이번 회차에도 산출할 수 없다.** 추정치로 메우지 않는다. (W36에 이어 2회차 연속 미산출.)

**대신 산출 가능한 것만 적는다** (오늘 아침 클로 실측, 재조회 안 함 — [보고받음: 클로 · 근거파일 `moa-studio/_workspace/weekly-strategy/facts_2026-W37.md`]):

| 단계 | 값 | 기간 |
|---|---|---|
| 검색 노출 → 클릭 (k-saju.me) | 731 → 12 (**CTR 1.64%**, 평균순위 44.8) | 90일 (06-08~09-04) |
| 검색 노출 → 클릭 (blog.k-saju.me) | 486 → 2 (**CTR 0.41%**, 평균순위 59.2) | 동일 |
| 클릭 → 결제 | **12 → 0** (90일 전환 0%) | 동일 |
| 비로그인 결제 도달 | **불가** — `POST /api/gumroad/checkout` **401** (8/31과 동일) | 09-07 시점 |

---

## 8. 추세 (WoW / 직전 대비)

| 지표 | W34 (08-20) | W36 (08-31) | **W37 (09-07)** | 변화 |
|---|---|---|---|---|
| 주간 매출 | ₩0 | ₩0 | **₩0** | **0 (변화 없음)** |
| 월 고정비 확인분 | ₩501,412~519,548 | (미재측정, jiwon 판독 승계) | **₩478,900~496,900** | **−4.5%** — **전액 환율 하락분**, 지출 구조 변화 아님 |
| 갭 | −₩20,501,000~20,520,000 | −₩20,470,000~20,650,000 | **−₩20,478,900~20,496,900** | 실질 동일 |
| 마지막 판매 이후 | — | 66일(표기) / **실제 59일** | **66일** | +7일 |
| 퍼널 원장 실측 라인 | 1줄 (08-12) | 1줄 | **1줄** | **+0줄 (26일째 정지)** |
| 활성 구독자 | 0 | 0 | **0** | 0 |
| 환율 (USD→KRW) | 1,412.87 | (미측정) | **1,346.56** | −4.7% |

---

## 9. 측정 부채 — 이번 회차 현황

| # | 항목 | 상태 | 변화 |
|---|---|---|---|
| 1 | **매출 원장 SSOT 미확정** | **★해소** — §3에서 `/v2/sales` 로 확정 | W36 대비 해소 |
| 2 | **고정비 상충 원인** | **★해소** — §4-1에서 3개 원인 특정 | W36 대비 해소 |
| 3 | **자동 원장 결함 3건** (`:58` 금액 2배 과대 · `:74` 날짜 정렬 미보장 · `:72` 건수 의미 혼동) | **신규 발견 · 전부 미수정** | 이번 회차 신규, §3-4 |
| 4 | 퍼널 원장 정지 (운영 DB 미연결) | **미해소** | 26일째 |
| 5 | AdSense 적립액 | **[권한없음] 미해소** | 5회차 연속 |
| 6 | 아투 GSC 조회 불가 (SITES 목록 부재) | **미해소** | — |
| 7 | 가용자금 Y | **미회신** | 26일째 |
| 8 | Vercel/Higgsfield/HF/fal/Suno/GCP 요금제 | **미확인** | 26일째 |
| 9 | Gumroad **페이아웃 입금 명세** (06-26자 입금 라인 유무 = §3-2 판정의 반증조건) | **[권한없음]** — 형만 조회 가능. 수수료율은 `gumroad_fee` 필드로 확인됨 | 이번 회차 신규 |
| 10 | 쇼츠·외주 매출 원장 부재 | **미해소** | — |

⚠️ **기한 항목:** `toastdm.com` 도메인 만료 **2026-10-26** (오늘로부터 **D-49**). 이 도메인에 Lightsail 서버(nblog-saas 포함) DNS가 물려 있다. [승계 — 만료일은 08-12 RDAP 실측, 이번 회차 재조회 안 함]

---

## 10. 재현 방법 (이번 회차에 내가 실제로 돌린 것)

| 항목 | 방법 |
|---|---|
| k-saju 매출 원본 (1차) | `GET https://api.gumroad.com/v2/sales?after=2020-01-01` · `/v2/products` · `/v2/products/{id}/subscribers` — **읽기 전용**, 토큰은 `saju-studio` 의 env에서 참조하며 출력하지 않음 |
| k-saju 매출 원본 (**2차 — 판정을 뒤집은 조회**) | 위 3개 + `/v2/sales/{id}` · `/v2/subscribers/{subscription_id}`. ★**키를 지정하지 말고 `Object.keys()`로 전체 키 목록을 먼저 찍을 것** — 1차에서 `free_trial_ends_on`·`invoice_url`을 놓친 것이 오판의 원인이었다. PII(email·이름·주소·카드)는 값 출력에서 제외 |
| 매출 원장 이력 | `moa-vault/10_Wiki/Finance/ksaju-ledger-history.jsonl` 전문 조회 (09-07 00:30Z 라인까지) |
| 날짜 버그 특정 | `C:\Users\user\.moa\ksaju_ledger_snapshot.mjs` 74번째 줄 직접 읽음 |
| 환율 | `open.er-api.com/v6/latest/USD` + `api.frankfurter.app/latest?from=USD&to=KRW` 2곳 대조 |
| 구독 등급 | `~/.claude/.credentials.json` 의 `subscriptionType`·`rateLimitTier`, `~/.codex/auth.json` id_token 의 `chatgpt_plan_type` 클레임 — **비밀값 미출력** |
| 상충 ② 대조 | `2026-W34_metrics.md` §7-1~7-4 vs `burn_rate.md` §2-3·§2-4 직접 대조 |
| 퍼널 원장 | `ksaju-funnel-history.jsonl` 전문 조회 (3줄) |
| 가용자금 Y | `10_Wiki/Finance/**`, `30_Learning/**` 키워드 검색 |

### 자동화 요청 (cto-seojin / 코드 담당자 — 실행은 내 소관 아님)
1. **`ksaju_ledger_snapshot.mjs` 결함 3건 수정** (우선순위 1, §3-4) — `:58` 합산에서 무료 트라이얼 레코드 제외(`invoice_url` 부재 또는 `free_trial_ends_on` 미도래 건) · `:74` `last_sale_at` 을 `max(created_at)` 으로 · `:72` `sale_count` 를 레코드 수/징수 건수/유니크 구독 수로 분리. **안 고치면 매일 2배 매출과 7일 이른 날짜가 계속 쌓인다**
2. `growth-funnel-snapshot` 의 `DATABASE_URL` 을 운영 호스트로 연결 — 퍼널 원장 26일 정지 해소
3. AdSense Management API OAuth (형 인증 필요) — ③라인 매출 + 블로그 트래픽 대용치 동시 해결
4. 아투 도메인을 `gsc_query_report.mjs` SITES 목록에 추가

---

*[확인] = 2026-09-07 직접 조회 / [승계] = 과거 실측을 날짜 명시하고 이어씀 / [추정] = 계산·모델 기반 / [권한없음] = 접근 권한 부재 / [못 찾음] = 조회 범위에서 미발견*
*본 문서: `moa-vault/10_Wiki/Finance/weekly/2026-W37_metrics.md`*
*관련: `burn_rate.md` · `ksaju-ledger-history.jsonl` · `ksaju-funnel-history.jsonl` · `weekly/2026-W34_metrics.md` · `weekly/2026-W36_revenue-review.md`*
